-- دوال التريجر مالهاش لزمة تتنادى من الويب — التريجر بينفّذها بصلاحيات
-- صاحب الجدول مش بصلاحيات اللي بعت الطلب، فسحب EXECUTE ما بيأثرش عليها.
--
-- (بالعكس من دوال الصلاحيات fn_is_admin / fn_has_permission: دي بتتنادى
--  جوه سياسات RLS اللي بتتنفّذ بصلاحيات الزائر نفسه، فسحبها منه بيكسر
--  الموقع — وده حصل فعلًا واتصلّح في هجرة قبل دي.)

revoke execute on function public.fn_guard_ban_columns() from public, anon, authenticated;;
