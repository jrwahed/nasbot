# نسبوط — الملف الكامل

> كل حاجة عن المشروع في مكان واحد: هو إيه، مبني إزاي، عدّى بإيه، وفاضل إيه.
>
> ⚠ الملف ده **ملخّص للمالك**. المرجع العامل لأي إيجنت بيشتغل على الكود هو
> `CLAUDE.md` — لو اتخالفوا، `CLAUDE.md` هو الصح.
>
> نسخة حيّة تتعدّل وتتعلّق عليها:
> https://claude.ai/code/artifact/9275eb27-28a4-4b19-8dc9-a6bef365982a
>
> **آخر تحديث: ٢٠٢٦-٠٩-٢١** — بقراية مباشرة من قاعدة الإنتاج، مش من الذاكرة.

---

## ١. نسبوط إيه

نادي خروجات مع ناس جُدد في القاهرة. المجموعة صغيرة، وبتتكشف قبل الخروجة بيوم.
**مفيش عضوية.** وفيه طبقة تانية اسمها «الشغل» للفريلانسرز: سبوطات نهارية +
كارت مدفوع مقدم + يوم أسبوعي ثابت.

المنتج اتحوّل في ٢٠٢٦-٠٩-١٢: نسبوط ما بقاش قايم على الكابتن والتنظيم.
**العضو العادي بيفتح خروجته بنفسه**، والناس اللي ما تعرفهوش تنضم ليها.
سبوطات نسبوط وسبوطات الأعضاء في نفس القايمة، وعمود `sbotat.origin` هو اللي
بيفرّق. الكابتن بقى اختياري، و«صاحب الخروجة» (`sbotat.host_id`) هو اللي ماسك
المجموعة — عضو زي أي حد، مش موظف مدفوع.

| | |
|---|---|
| منشور على | Vercel — `nasbot.vercel.app` |
| القاعدة | Supabase · مشروع `nasbot-prod` = `nutmgtulrqrfaysrigfi` · `eu-central-1` |
| الفرع | التطوير على `claude/beautiful-euler-es0a2h`، والنشر من `main` |
| المستودع | `jrwahed/nasbot` |

في نفس مؤسسة Supabase فيه مشروع تاني فاضي (`vohmnfaidwgvuusezktc`) — **متلمسهوش**.

### السبع قواعد اللي محدش يكسرها

1. **ممنوع نص عربي في أي مكوّن** تحت `src/app` أو `src/components` (ما عدا
   `src/app/admin`). كل نص بيمر بـ`useT()` ومفتاحه في `copy_strings`.
   حارس `check-copy.mjs` بيفشل البناء لو خالفت.
2. **كل رقم في `settings` مش في الكود** — سعر، نسبة، ميعاد، حد. رقم في الكود = باج.
3. **الأمان من RLS مش من إخفاء الأزرار.** أي فعل حساس لازم يترفض على الخادم.
   مفتاح الخدمة ممنوع يوصل المتصفح.
4. **الفلوس بالقروش** في القاعدة وبالجنيه في الواجهة.
5. **الدفع تحويل يدوي** (فودافون كاش / إنستا باي) + اعتماد من اللوحة. مفيش بوابة دفع.
6. **كلمات ممنوعة:** تعارف · شريك · إعجاب · فعالية · تذكرة · باقة · منصة ·
   مستخدم · اكتشف. النبرة عامية مصرية.
7. **الهوية:** مش تطبيق تعارف (مفيش صور قبل الكشف، والاختيار سري)، ومش موقع
   تذاكر — **المجموعة** هي المنتج مش المكان ولا النشاط. والسرية ما اتكسرتش:
   حتى صاحب الخروجة بيشوف مجموعته بعد الكشف بس، زي أي حد حاجز بالظبط.

الألوان: `#F4632A` برتقالي · `#14161A` غامق · `#FBF7EF` كريمي · `#2B4CFF`
كوبالت · `#EFE3CF` رملي. مفيش ظلال ناعمة ولا تدرجات ولا أبيض ناصع.

---

## ٢. المكدّس والبنية

Next.js 15.1.12 (App Router) · React 19 · TypeScript strict · Tailwind ·
Supabase (Postgres 17 + Auth + Storage + RLS + pg_cron + pg_net) · Node 22.
الإيميل SMTP عبر nodemailer وشغّال. **الواتساب التلقائي مش متفعّل** — اللوحة
بتفتح المحادثة والمالك بيبعت بإيده.

### الأوامر

| الأمر | بيعمل إيه |
|---|---|
| `npm run dev` | تطوير محلي |
| `npm run verify` | **قبل أي رفع** — الخمس حراس + `tsc` + `next build` |
| `check:copy` | مفيش نص عربي في المكونات |
| `check:notify` | متغيّرات قوالب الإشعارات = الكود |
| `check:content` | مفيش صفحة محتوى بتطلع فاضية |
| `check:bundles` | حزم `WORK_MIGRATION_*` = ملفات الهجرة حرف بحرف |
| `check:map` | مطابقة مناطق الخريطة — ٢١ حالة |

### الملفات المفتاحية

| الملف | مسؤول عن |
|---|---|
| `src/lib/api.ts` | طبقة البيانات الأساسية — كل قراية/كتابة للموقع العام |
| `src/lib/map-db.ts` | تحويل صفوف القاعدة ↔ أنواع الواجهة، والفلوس |
| `src/lib/fields.ts` | قوايم التسجيل + كتل الخريطة + `blockKeyFor` |
| `src/lib/copy.ts` · `content.ts` · `flags.ts` | النصوص والمحتوى ومفاتيح المزايا، كلها بكاش موسوم |
| `src/lib/server/admin-auth.ts` | `requirePermission` — الحد الأمني الحقيقي للوحة |
| `src/lib/server/pay-guard.ts` | بيمسك رقم الدفع الوهمي ويوقف الدفع قبل الحجز |
| `src/lib/wa-message.ts` | ترتيب متغيّرات رسالة الواتساب |
| `src/middleware.ts` | حارس `/admin/*` على الحافة — طبقة أولى مش الحد الأمني |

### الصفحات

**عام:** `/` · `/one` · `/map` · `/captains` · `/join` · `/login` · `/me` ·
`/game` · `/rules` `/faq` `/about` `/terms` `/aman` (محتواهم من `content_blocks`).

**الحجز:** `/s/[slug]` ← `/pay` ← `/done` ← `/my/[id]` (+ `/chat` و `/review`).

**العضو بيفتح خروجة:** `/new` · `/me/sbotati` · `/captain/[id]`.

**مالهاش لينك عن قصد** (بتتنسخ بالإيد): `/tamenny/[token]` رابط الاطمئنان اللي
العضو بيبعته لحد يثق فيه · `/zakki/[code]` صفحة التزكية · `/me/chat/[id]` الشات
الخاص اللي بيتفتح بعد التبادل.

**الشغل:** `/shoghl` · `/shoghl/[slug]` · `/shoghl/pass` · `/shoghl/amaken` · `/me/shoghl`.

**اللوحة:** `/admin` وتحتيها ٢٠ قسم.

---

## ٣. القاعدة

### الجداول

`profiles` · `sbotat` (+ الفيو العام `sbotat_public`) · `sbota_templates` ·
`venues` · `bookings` · `payments` · `wallet_ledger` · `coupons` ·
`pair_affinity` · `matching_runs` · `chat_rooms`/`chat_members` ·
`notifications`/`notification_templates` ·
`admin_users`/`admin_roles`/`role_permissions` · `settings` (صف واحد) ·
`copy_strings` · `content_pages`/`content_blocks` · `map_areas` ·
`safety_links` (روابط الاطمئنان) · `endorsements` (التزكيات) · `audit_log`.

**طبقة الشغل:** `professions` · `work_venues` · `work_passes` ·
`pass_redemptions` · `recurring_bookings` · `work_affinity` · `venue_reports` ·
`leads` · `work_metrics`.

### الدوال المهمة

`fn_can_book` · `fn_capacity_guard` (قفل السعة) · `fn_booking_paid` ·
`fn_cancel_booking` · `fn_approve_transfer` · `fn_redeem_pass` ·
`fn_build_matching` · `fn_reveal` · `fn_pair_want`/`fn_work_want` ·
`fn_is_admin`/`fn_has_permission` · `fn_create_sbota`.

**المكان — الحارس والبيانات كل واحد لوحده (0105):** `fn_sbota_place` (البيانات،
من غير أي حارس، مقفولة على الكل) · `fn_can_see_place` (الحارس لوحده) ·
`fn_sbota_address` (غلاف رفيع بينادي التنتين). أي سياق جديد محتاج العنوان بينادي
**نفس** الحارس — مستحيل ينساه حد.

**رابط الاطمئنان:** `fn_safety_link` · `fn_safety_my` · `fn_safety_view`
(الوحيدة المفتوحة للزائر المجهول) · `fn_safety_mark` · `fn_safety_revoke`.

**الوصول:** `fn_sbota_arrival` · `fn_set_sbota_sign`.

**بوابة الدخول:** `fn_gate_state` · `fn_gate_decide` · `fn_use_invite` ·
`fn_endorse` · محفّز `fn_gate_on_signup`.

### أربع قواعد أمان اتكسروا قبل كده

1. **كتابة التبادل لازم عبر `fn_pair_want`/`fn_work_want`.** الكتابة المباشرة
   على جداول affinity كانت ثغرة تزوير تبادل — اتسدّت في `0052` بسحب سياسات
   الكتابة خالص.
2. **سياسات الكتابة الحساسة لازم `fn_has_permission('<الصلاحية>')` مش
   `fn_is_admin()`.** `fn_is_admin` معناها «أي صف نشط في `admin_users`»، يعني
   حتى `support` كان بيغيّر الأسعار (`0056`).
3. **ممنوع `current_user` كحارس جوّه `security definer`.** بوستجرس بيخليه =
   صاحب الدالة مش اللي بينادي، فالشرط عمره ما بيبقى true والحارس بيبقى ديكور.
   ده حصل في **تلات دوال**. استعمل `fn_caller_is_browser()`.
4. **`auth.uid() is not null` مش حارس.** الزائر المجهول `auth.uid()` بتاعه
   null، فشرط زي `if auth.uid() is not null and not fn_has_permission(...)`
   **بيعدّي المجهول** بدل ما يمنعه. ده اللي فتح `fn_reveal` للنت كله (`0062`).

وكمان: أي دالة جديدة بتبقى `execute` لـPUBLIC افتراضيًا. كل دالة حساسة لازم
وراها `revoke execute … from public, anon;` صريح.

ولو العضو بيعمل حاجة جديدة، اعملها **بدالة مش بسياسة `insert`**.
`fn_create_sbota` مثال: لو سبنا باب كتابة مباشر، العضو كان هيكتب `price = 0`
أو `origin = 'nasbot'` وينتحل صفة الموقع. الدالة أصلًا مفيهاش parameter للسعر.

### طريقة تغيير القاعدة

1. يتكتب ملف هجرة في `supabase/migrations/` باسم
   `<timestamp>_<NNNN>_<وصف>.sql`، ومعاه دالة `test_*` بترجّع صفوف «نجح/فشل».
2. يتجمّع في حزمة `WORK_MIGRATION_N.sql` في جذر المشروع.
3. **صاحب المشروع** بيلزق الحزمة في Supabase ← SQL Editor ← Run.

محرر SQL بيشغّل الملف كـ**معاملة واحدة**، وPostgres ما بيسمحش تستخدم قيمة
`enum` جديدة في نفس المعاملة اللي أضافتها — عشان كده حزمة `14` اتقسمت `14A` و`14B`.

**الفحص الشامل:** الزق `CHECK_DB.sql` في SQL Editor — بيشغّل كل دوال الاختبار
(بيلاقيها لوحده) ويطلّع اللي فشل بس.

---

## ٤. اللوحة

الدخول: إيميل + باسورد + تطبيق مصادقة (TOTP).
الأدوار: `owner` · `admin` · `ops` · `finance` · `support`.

الأقسام العشرين: الرئيسية · النصوص · اللعبة · حقول التسجيل · القوالب ·
السبوطات · الحجوزات · المطابقة · الناس · الكباتن · الفلوس · البلاغات ·
الرسايل · الخريطة · المحتوى · الإعدادات · الشغل · الفريق · التسعير · السجل.

### المهم — مين بيعمل إيه منين

| عايز تعمل إيه | روح فين |
|---|---|
| تعتمد تحويل وتأكد حجز | **الفلوس ← التحويلات المستنية** ← «أكّد» |
| تبعت واتساب للحاجز | نفس المكان، جنب «أكّد» · أو من الحجوزات |
| تفتح سبوطة وتكتب مكانها وعنوانها | السبوطات |
| تسعّر قالب أو ترفع صوره | القوالب |
| تحرّك كتل الخريطة وتزوّد أسماء مناطق | الخريطة |
| تغيّر أي نص في الموقع | النصوص |
| تغيّر فقرات القواعد/الأسئلة/مين إحنا/الشروط | المحتوى ← صفحات الموقع |
| تغيّر أي رقم (سعر، نسبة، حد، ميعاد) | الإعدادات |
| تقفل ميزة مؤقتًا | الإعدادات ← مفاتيح المزايا |
| تعدّل نص إيميل أو رسالة واتساب | الرسايل |
| تكتب «العلامة» اللي المجموعة تعرف بعضها بيها | السبوطات ← أي سبوطة ← «العلامة» |
| تظبط مدة رابط الاطمئنان | الإعدادات ← الساعات والمواعيد |
| تحط رقم الطوارئ | الإعدادات ← العمولة والدفع |
| تقفل النادي (دعوة/اعتماد/تزكية) | الإعدادات ← بوابة الدخول |

### نمط الكتابة الصح في اللوحة

بعد أي `update`/`insert`/`delete` لازم `.select('id')` ثم تمرير النتيجة على
`rejected()` — **المصفوفة الفاضية معناها القاعدة رفضت، مش إن الحفظ نجح.**
من غير الحارس ده الصفحة بتقول «اتحفظ ✓» وهي كذبة.

---

## ٥. الرحلة

من ٢٠٢٦-٠٩-٠٨ لحد ٢٠٢٦-٠٩-٢١: **١١٠ كوميت · ١٠٧ ملف هجرة · ٢٧ حزمة لزق**.
كل صف دفعة اتلزقت في SQL Editor.

| الحزمة | الهجرات | إيه اللي حصل |
|---|---|---|
| `1`–`2` | 0001–0046 | الأساس كله + طبقة «الشغل». اتقسمت لأن `alter type … add value` ما بينفعش في نفس المعاملة |
| `3` | 0047–0049 | مطابقة الشغل + إصلاح «عايز تشوف مين تاني؟» |
| `4` | 0050–0051 | أغلفة صلاحيات اللوحة و«شغالين معاك» والشات |
| `5` | 0052–0064 | **الأحمر كله من المراجعة الأمنية** — التبادل المزيّف، الحجز المجاني المؤكد، `payout_method` المكشوف، الصلاحيات الدقيقة، قيود الفلوس السالبة |
| `6` | 0065–0074 | قوايم التسجيل · كتل الخريطة · تخمين رمز الدخول · الصيانة توقف الحجز · صلاحيات رفع الصور |
| `7` | 0075–0077 | نشاطات المهارة بقت بحث حقيقي · «عجل» رجعت · نصوص «مقفول» |
| `8` | 0078–0081 | **نقلة المنتج: العضو بيفتح خروجته بنفسه** — الأعمدة والدوال والحدود و٥٢ نص |
| `9` | 0082 | صفحات الذيل بقت شغّالة. كانت ٥ روابط **وأربعة منهم بيروحوا `/rules`** |
| `10` | 0083 | تصليح ترتيب لزق: هجرتين كانوا بيكتبوا على نفس الصف |
| `11` | 0084 | المحتوى: ١٧ سؤال · ٦ فقرات «مين إحنا» · ١٢ فقرة شروط · ٢١ قالب رسايل |
| `12` | 0085 | مدخل اللعبة — كانت مبنية ومترجمة ومفيش ولا لينك ليها |
| `13` | 0086–0087 | العضو بيكتب خروجته بنفسه — الحجز فيها **ببلاش**، والعنوان مع الكشف |
| `14A`+`14B` | 0088–0093 | **بيانات العرض اتمسحت و٨٥ قالب حلّوا محلها** في ٨ تصنيفات. `14A` لوحده الأول |
| `15` | 0094 | «غير كده» و«حاجة تانية» بقوا يتكتبوا — كانوا طريق مسدود |
| `16` | 0095 | قوالب نسبوط الأصلية رجعت (بادل · عشا · لمة قهوة · هايك · الغامضة) |
| `17` | 0096 | نص زرار قايمة الموبايل |
| `18` | 0097 | 🔴 اسم المكان كان بيبان قبل الدفع |
| `19` | 0098 | 🔴 العنوان ما كانش بيوصل للدافع خالص + قالب الواتساب |
| `20` | 0099 | 🔴 الخريطة كانت فاضية — كتل جديدة وأسماء مرادفة |
| `21` | 0100 | الشات بقى **لحظي فعلًا** — `subscribeChat` كانت كود ميت، والشات الخاص كان مزيّف بالكامل |
| `22` | 0101 | **بوابة الدخول** — دعوة · اعتماد · تزكية · حظر. كلها **مقفولة افتراضيًا** |
| `23` | 0102 | إشعار قرار البوابة |
| `24` | 0103 | 🔴 درجات التقييم كانت بتتحفظ بمفتاح هو **النص العربي** للسؤال — أول ما تغيّر كلمة، كل الدرجات `null` |
| `25A` | 0104 | **صفحة `/aman`** «إزاي بنأمّنك» + السن بقى من `settings` بدل ما هو مكتوب في الكود |
| `25B` | 0105 | **«معايا حد يعرف»** رابط الاطمئنان + تقسيم الحارس عن البيانات |
| `26A` | 0106 | **«جايين منين»** — المنطقة قبل الحجز وبعد الكشف |
| `26B` | 0107 | **«أول ربع ساعة»** — العلامة ومين بيستقبل وتلات أسئلة |

### خمس محطات غيّرت المشروع

**المراجعة الشاملة.** لقت ١٥٠ نتيجة، ٣٤ منهم حمرا. واللي بعدها أهم: الدفعة
`0052..0060` اتكتبت **من غير دوال اختبار**، ولما اتكتبت `test_review_fixes()`
لقت **اتنين من التصليحات نفسهم مكسورين**.

**نقلة المنتج.** الكابتن اتشال من قلب المنتج، والعضو بقى يفتح خروجته. ده غيّر
القاعدة والواجهة واللوحة والنصوص مع بعض.

**دليل السبوطات.** ٣٩ صف بيانات عرض (٩ سبوطات · ٩ حجوزات · ٤ كباتن · ٥ أماكن ·
١٢ عضو وهمي) اتمسحوا، و٤٨٥ قالب حقيقي حلّوا محلها.

**أول حجز حقيقي.** كشف أربع باجات ما كانش ممكن تبان إلا بحجز حقيقي: اسم المكان
مكشوف قبل الدفع · العنوان ما بيوصلش بعد الدفع · روابط 404 · صفحة الفلوس مقفولة
من أول يوم.

**الوجع بدل الميزة (٢٠٢٦-٠٩-٢٠).** بدل «إيه اللي نزوّده»، السؤال بقى «إيه اللي
بيوجع الناس ومحدش بيرد عليه». طلعوا أربعة: **رايحة فين ومع مين؟** (نص الجمهور
مقفول عليه) · **هوصل إزاي؟** (أكتر سبب إلغاء في القاهرة) · **هروح ألاقي نفسي
قاعد ساكت** (اللي بيفرّق بين «جربت مرة» و«بقيت أخرج») · **وانبسطت وبعدين؟**.
التلاتة الأولانيين اتبنوا في `0104`–`0107`.

---

## ٦. الدروس

كل درس هنا باج وقع فعلًا على الإنتاج، مش نصيحة عامة. مكتوبين بطولهم في `CLAUDE.md`.

| # | الدرس | الباج اللي علّمنا إياه |
|---|---|---|
| ١ | التصليح نفسه محتاج يتفحص | `0052..0060` اتكتبت من غير اختبار، ولما اتفحصت لقينا تصليحين مكسورين |
| ٢ | «آمنة تتكرر» لازم تتجرّب مرتين | `create policy` من غير `drop policy if exists` وقّف اللزق على المالك |
| ٣ | هجرتين على نفس الصف = قنبلة موقوتة | `0065` تقفل «عجل» و`0075` ترجّعها — اللي يتلزق آخر يكسب، **بالصمت** |
| ٤ | نص القالب والكود اللي بيملاه يتفحصوا مع بعض | «وصلنا تحويلك لـ .» — متغيّر ناقص بيوصل فراغ ومفيش حاجة بتفشل |
| ٥ | الاحتياطي يملا الناقص مش الغايب بس | `/about` كان بيقول «بنكتب الصفحة دي» والمحتوى قدامنا في الكود |
| ٦ | التصليح اللي ما وصلش للملف اللي بيتلزق = تصليح مش موجود | اتصلحت `0065` والحزمة ما اتجددتش، فالباج فضل حي وإحنا كاتبين إنه اتقفل |
| ٧ | المسح بالبادئة بيمسح اللي ما عملتهوش | دالة بذرة مسحت **حساب لوحة نشط** على الإنتاج |
| ٨ | `prosrc` فيها التعليقات كمان | فاحص خبط في **التعليق** اللي بيشرح الباج وقال «فشل» على دالة سليمة |
| ٩ | الفاحص بيدوّر على كلمة والفشل بيتقال بكلمتين | `CHECK_DB` كان بيفلتر «فشل» بس، و٤ دوال RLS بتكتب «رسب» — ٦٦ صف فشل كانوا هيعدّوا |
| ١٠ | الفاحص اللي بيقول «مفيش غير اللي حطيته» بيبقى أحمر أول ما تشتغل | حصلت **تلات مرات في يوم واحد** |
| ١١ | عمود اتعمل لسياق، يتنقل لسياق تاني ويتحوّل لتسريب | `venue_name_ar` اتعمل لخروجة العضو، وبقى يكشف مكان سبوطة نسبوط لأي زائر |
| ١٢ | الحارس في القاعدة مش بيغني عن حارس في الواجهة | حد يرفع أي صورة على إنها تحويل ويستنى الكشف — والصفحة تتصرف كأنه داخل |
| ١٣ | العمود اتنقل والدالة ما مشيتش وراه | `venue_id` بقى null و`fn_sbota_address` فضلت بتلف على `venues` — الدافع ما شافش العنوان |
| ١٤ | الرابط اللي في الإيميل محدش بيدوس عليه غيره هو | `/sbota/<slug>` مسار مش موجود، و`/my/b1` رقم حجز من بيانات عرض اتمسحت |
| ١٥ | فاصلة واحدة مكتوبة بالإيد قفلت صفحة الفلوس كلها | `created_atbookings!…` — ومحدش اكتشفها غير أول ما المالك راح يعتمد تحويل |
| ١٦ | الحارس والبيانات في دالة واحدة = نسخة تانية من الحارس | أول ما احتجنا العنوان من سياق تاني كان لازم ننسخ الحارس — والنسخة بتتنسى |
| ١٧ | الحزمة الكبيرة بتوصل مقصوصة، والخطأ بيشاور على حتة تانية | ملف ١٠٢١ سطر وصل منه ١٠٠، والمحرر قال `unterminated dollar-quoted string ... LINE 67` |
| ١٨ | الشيم المحلي اللي مش شبه الإنتاج بيكدب عليك بثقة | `pgcrypto` في `extensions` على سوبابيس وفي `public` محليًا — ١٤ من ١٤ «نجح» ووقع على الإنتاج |

### خيط واحد بيربطهم كلهم

أكتر من نص الدروس دي باجات **ما كانش بيطلع منها أي خطأ**: متغيّر بيوصل فراغ،
سبوطة بتختفي من الخريطة، صفحة بترجّع فاضية، عنوان ما بيوصلش. عشان كده كل تصليح
في المشروع ده معاه **حارس بيفشل بصوت عالي**، والحارس نفسه بيتجرّب **بفخ متعمّد**
قبل ما نصدقه. فاحص بيقول «تمام» على طول أسوأ من مفيش فاحص.

---

## ٧. الحالة دلوقتي

**تاريخ القراية: ٢٠٢٦-٠٩-٢١، مباشرة من قاعدة الإنتاج.**

الموقع منشور وشغّال، والكود كله متدمج على `main`. القاعدة خضرا:
الفحص الشامل رجّع **٤٢ دالة اختبار · صفر فشل**، وكل دالة منهم اتجرّبت بفخ
متعمّد قبل ما نصدّقها.

### إيه شغّال

- دخول إيميل + باسورد · لوحة كاملة بـTOTP · طبقة الشغل (المراحل ١-٦) ·
  إيميل بجدولة · **١٣ مهمة مجدولة نشطة** جوّه Supabase (منها `nasbot-reveal`).
- العضو بيفتح خروجته ويكتبها بنفسه · صفحات المحتوى من اللوحة · الخريطة بمحرر
  سحب وإفلات · زرار واتساب برسالة جاهزة · الشات لحظي.
- **الجديد:** `/aman` · رابط الاطمئنان · «من ناحيتك» · «أول ربع ساعة».
- **بوابة الدخول مبنية بالكامل و‍مقفولة** — دعوة/اعتماد/تزكية كلهم `false`.
- خمس حراس جوّه `npm run verify` بيفشلوا البناء لو اتكسرت قاعدة.

### 🔴 والموقع **فاضي من البيانات** — دي المشكلة الحقيقية دلوقتي

مفيش ولا صفحة مخفية: المفاتيح التمنية كلها مفتوحة، الصفحات شغّالة، الروابط
موجودة، ومفيش وضع صيانة. اللي ناقص **محتوى** مش كود.

| | الرقم | اللي بيترتّب عليه |
|---|---|---|
| سبوطات **مفتوحة للناس** | **١** | الرئيسية بتوري كرت واحد |
| سبوطات **مسوّدة** | **٦** | عملتها وسيبتها `draft` — محدش شايفها، وكلها ناقصة سعر ومكان ومنطقة وصورة |
| سبوطات شغل | ٠ | `/shoghl` مبنية بالكامل و**فاضية** |
| سبوطات غامضة | ٠ | `/s/mystery` فاضية |
| كباتن نشطين | ٠ | `/captains` فاضية |
| سبوطات عليها منطقة | ١ | الخريطة فيها كتلة واحدة ملوّنة |
| سبوطات عليها علامة | ٠ | «أول ربع ساعة» هيوري الأسئلة من غير علامة |
| قوالب سعرها ٠ | **٨٧ من ٩٧** | لازم تسعّر القالب قبل ما تفتح منه سبوطة |
| قوالب عليها صور | ٦ من ٩٧ | الدليل كله من غير صور |
| أعضاء | ٣ | انت · واحد من غير اسم · Ganna |
| حجوزات | ٤ | كلها تجربة |

### أرقام القاعدة

| | |
|---|---|
| قوالب السبوطات | ٩٧ |
| السبوطات | ٩ — واحدة مفتوحة · ٦ مسوّدة · اتنين خلصوا |
| حسابات اللوحة | ١ نشط |
| دوال الاختبار | ٤٢ |
| ملفات الهجرة | ١٠٧ |
| طبقة الشغل | ٣ قوالب + ٤ أماكن — ما اتلمستش |

### ⚠ درس اللزق (٢٠٢٦-٠٩-٢١)

اللزق من الشات **اتقطع أربع مرات**، وكل ملف وقف في مكان مختلف. القاعدة قعدت
في حالة مخلوطة: صفحة `aman` موجودة بفقراتها بس `fn_can_book` ما اتحدّثتش ·
`fn_sbota_place` موجودة بس **جدول `safety_links` كله مش موجود** · و`0107` ولا
حاجة. والمحرر بيشتغل كمعاملة واحدة، فالمفروض ده ما يحصلش — بس القص جه على
حدود جُمَل، فاللي وصل اتنفّذ واتحفظ.

ولما فحصنا **كل** الـ١٥٢ دالة والـ٧٣ جدول اللي الهجرات بتعملهم ضد اللي فعلًا
في القاعدة، طلع إن **بوابة الدخول اتقطعت من قبل كده وإحنا مش واخدين بالنا**:
`fn_use_invite` و`fn_endorse` والمحفّز ودالة الاختبار و**كل الـ١٨ نص** مكانوش
موجودين. البوابة مقفولة فمحدش اتأثر — بس أول ما تتفتح كانت هتقع.

كله اترفع تاني واتفحص. والدرسين ١٧ و١٨ اتكتبوا من اليوم ده.

**القاعدة اللي خرجت منه:** الحزمة = **هجرة واحدة** (وإلا تتقسم `NA`/`NB`)،
والمالك ينسخ من **جيتهب** بزرار «Copy raw file» مش من معاينة الشات. وقبل أي
Run، بص على آخر سطر في المحرر — لو منتهي في نص جملة، ده قص مش باج.

### ملفات التوثيق في المشروع

`CLAUDE.md` المرجع الأول لأي حد بيفتح المشروع · `CHECK_DB.sql` الفاحص الشامل ·
`REVIEW*.md` المراجعة بتفاصيلها · `RUNBOOK.md` و`DEPLOY_CHECKLIST.md` للتشغيل ·
`ADMIN_GUIDE.md` للوحة · `DESIGN_TOKENS.md` و`COPY.md` للشكل والنبرة ·
`WORK_MIGRATION_*.sql` الحزم اللي بتتلزق (لحد `26B`).

---

## ٨. الناقص

> ⚠ **تصحيح مهم:** الملف ده كان بيقول «الدفع متوقف». **ده مش صح دلوقتي** —
> طريقة الدفع الشغّالة هي **إنستاباي** وحسابها `01013518185` رقم حقيقي،
> فالحجز والدفع **شغّالين**. الحارس بيفحص الطريقة اللي العضو اختارها، فلو
> اختار فودافون كاش (لسه وهمي) بياخد ٥٠٣، ولو اختار إنستاباي بيعدّي عادي.

### بيوقف الإطلاق

1. **رقم الطوارئ لسه `+201000000000`.** الزرار **مخفي في أربع صفحات**
   (القواعد · الأمان · صفحة الحجز · لوحة الكابتن) — المكوّن بيخفي نفسه لو
   الرقم وهمي، لأن زرار بيرن على رقم غلط أسوأ من مفيش زرار.
   **الحل:** `/admin/settings` ← العمولة والدفع ← «رقم الطوارئ».
2. **٦ سبوطات مسوّدة ومخفية.** أكوا بارك · حديقة الأزهر · نادي جاز · جزيرة
   الدهب · ورشة فخار · متحف الخزف. كلها **سعرها ٠، ومفيش منطقة ولا مكان ولا
   صورة**، وواحدة منهم ميعادها عدّى. من غيرهم الموقع فيه **سبوطة واحدة**.
3. **رقم فودافون كاش** لو عايز الطريقة التانية تشتغل.
4. **تغيير `CRON_SECRET`.** اتعرض في محادثة، وهو كمان الـpepper الاحتياطي
   لهاشات OTP والأدمن.

### محتوى ناقص

5. **الأسعار.** ٨٧ قالب من ٩٧ سعرهم `0` — الدليل متعمّد إنه مفيهوش أرقام.
   لازم تسعّر القالب قبل ما تفتح منه سبوطة.
6. **الصور.** ٦ قوالب بس عليها صور من ٩٧.
7. **العلامة.** مفيش ولا سبوطة عليها `sign_ar` — فكرت «أول ربع ساعة» هيفتح
   من غير سطر «هتعرفهم إزاي».
8. **الشروط لسه مقفولة.** فيها خانتين فاضيين (مدة صلاحية الرصيد · وسيلة
   التواصل)، ومحتاجة محامي يراجعها. دي **مقفولة عن قصد** مش باج.

### مبني ومستني بيانات بس

طبقة الشغل (`/shoghl`) · الغامضة (`/s/mystery`) · الكباتن (`/captains`) ·
الخريطة. الأربعة شغّالين بالكامل ومفيش ليهم ولا صف في القاعدة، فصفحاتهم فاضية.

### مبني ومقفول عن قصد

**بوابة الدخول.** دعوة · اعتماد · تزكية · حظر — كلهم مبنيين ومتفحوصين
(`test_entry_gate()` سبع بنود) و**مقفولين** في `settings`. تفتحهم من
`/admin/settings` ← بوابة الدخول لما تحب تقفل النادي.

---

## ٩. للي جاي بعد كده

أي حد — إيجنت أو شخص — بيفتح المشروع يبدأ من هنا.

1. **اقرا `CLAUDE.md` كله + `REVIEW.md`.** الملف ده ملخص للمالك، و`CLAUDE.md`
   هو المرجع العامل.
2. `npm install` ثم `npm run verify` — لازم يعدّي قبل أي شغل.
3. **أي تغيير اتبعه في كل الطبقات** — قاعدة ← `api.ts` ← واجهة ← لوحة ← نصوص
   ← بذرة ← توثيق. ممنوع تصلّح حتة وتسيب حتة. وسيلة رخيصة تمسك اللي فاتك: ضيف
   الحقل للنوع في `src/types` الأول و`tsc` هيوريك كل مكان ناقص.
4. **رقم ← `settings`. نص ← `copy_strings`.** مفيش استثناء.
5. **تغيير قاعدة** = ملف هجرة + دالة `test_*` + حزمة `WORK_MIGRATION_N.sql`،
   **وجرّبه على البوستجرس المحلي الأول**. متسلّمش تصليح أمان من غير ما تشوفه
   بيمنع فعلًا.
6. **قبل الرفع:** `npm run verify` + لقطة للصفحة المتأثرة. كل تعديل كوميت
   لوحده بالعربي.
7. **الأمان:** متضعّفش سياسة عشان تخلص، ومتحطش سر في الكود، ومتكتبش على جداول
   التبادل مباشرة.
8. **أي صفحة جديدة لازم يبقى ليها مدخل.** `/game` فضلت شهور مبنية ومترجمة
   وليها مفتاح ميزة — ومفيش ولا لينك ليها. الميزة اللي محدش يقدر يوصلها =
   ميزة مش موجودة.

### القاعدة اللي تحت كل ده

أكتر باج اتمسك في المشروع ده كان باج **صامت** — ما بيطلعش خطأ وما بيوقفش
البناء. فأي تصليح هنا ما بيخلصش لما يشتغل، بيخلص لما يبقى معاه حاجة بتصرخ لو
اتكسر تاني — وتكون اتجربت بغلط متعمّد قبل ما تتصدق.
