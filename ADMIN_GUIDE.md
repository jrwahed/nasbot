# دليل لوحة نسبوط

الدليل ده لصاحب الموقع — مش للمبرمجين. كل اللي فيه بيتعمل من المتصفح،
من غير ما تفتح كود ولا تعمل نشر.

---

## القاعدة الحاكمة

**الكود فيه المنطق بس. المحتوى كله في قاعدة البيانات.**

يعني أي نص، أي سؤال، أي اختيار، أي سعر، أي قاعدة، أي قالب رسالة، وأي إعداد —
بتعدّله من اللوحة وبيظهر في الموقع خلال ثواني.

---

## اللي **ما ينفعش** يتعدّل من اللوحة

في تلات حاجات بتفضل في الكود عن قصد. لو محتاج تغيّر فيها، ده شغل مبرمج
ومحتاج نشر جديد:

### 1. شكل الصفحات وترتيبها
مكان الأزرار، أحجام الخطوط، الألوان الأساسية، وترتيب الأقسام في الصفحة.
دي كلها في الكود علشان الموقع يفضل شكله واحد ومتماسك.
**ينفع تغيّر:** الكلام اللي جوه الأزرار والعناوين — من قسم «النصوص».

### 2. منطق الدفع والأمان
إزاي الحجز بيتقفل، إمتى الفلوس بترجع، مين يشوف العنوان، ومين يدخل اللوحة.
دي في الكود وفي سياسات القاعدة (RLS) علشان محدش يقدر يكسرها بالغلط —
ولا حتى أنت.
**ينفع تغيّر:** الأسعار، نسب الخصم، أرقام التحويل، ومدة المراجعة — من قسم الإعدادات.

### 3. خوارزمية المطابقة نفسها
إزاي بنكوّن المجموعات ونوزّع الناس — الطريقة نفسها مكتوبة في دالة
`fn_build_matching` في القاعدة، ومحدش بيعدّلها من اللوحة.
**ينفع تغيّر:** الأوزان والقواعد اللي بتشتغل بيها — فرق السن المسموح، نسبة
البنات، أقل عدد «فاتحين كلام» في المجموعة، ووزن الناس اللي اتعرفوا قبل كده —
كل ده من قسم «المطابقة».

> باختصار: **إيه** اللي بيتقال و**بكام** — من اللوحة.
> **إزاي** الصفحة مرصوصة و**إزاي** الفلوس والأمان بيشتغلوا — في الكود.

---

## الدخول

اللوحة على `/admin`. الدخول بخطوتين — الاتنين مطلوبين:

1. **رقم الموبايل** — بيوصلك رمز على واتساب زي أي عضو.
2. **كود التطبيق** — 6 أرقام من تطبيق مصادقة (Google Authenticator أو أي واحد
   شبهه) بيتغيّر كل نص دقيقة.

**أول مرة بس:** بعد ما تدخل الرمز، اللوحة هتوريك سر وتطلب منك تضيفه في
التطبيق، وتكتب الكود مرة علشان نتأكد إنه شغّال. بعد كده كل مرة هتكتب الكود
وخلاص.

> السر بيتقال مرة واحدة. **صوّره أو اكتبه في مكان أمان** — لو ضاع منك مفيش
> زرار «نسيت»، لأن اللي يقدر يعيد التفعيل يقدر يشيل الحماية كلها. لازم حد
> تاني من الفريق يظبطها من القاعدة.

الجلسة بتقعد 4 ساعات، وبتتقفل لوحدها لو سبت اللوحة نص ساعة.

الأدوار: `owner` · `admin` · `ops` · `finance` · `support` —
كل دور بيشوف الأقسام اللي تخصّه بس.

### تزويد حد للفريق

من اللوحة: **`/admin/team`** (قسم «الفريق» — للمالك بس). اكتب رقم موبايله،
اختار الدور، «ضيفه». من نفس الصفحة: تغيير الدور، وقف/رجّع، تصفير تطبيق
المصادقة لو غيّر موبايله، وشيل من الفريق.

الشخص لازم يكون عامل حساب في الموقع الأول (سجّل من صفحة الانضمام برقمه).
بعد الإضافة بيدخل من `/admin/login` بإيميله وباسورده وبيفعّل تطبيق
المصادقة أول مرة.

حمايتين: ما تقدرش توقف نفسك أو تنزّل دورك، وآخر مالك نشط ما يتشالش.

لو اللوحة نفسها مقفولة (مثلًا أول مالك خالص)، البديل من Supabase ← SQL Editor:

```sql
insert into admin_users (profile_id, role_key, is_active)
select id, 'owner', true from profiles where phone = '+201XXXXXXXXX'
on conflict (profile_id) do update set role_key = excluded.role_key, is_active = true;
```

---

## خريطة اللوحة

| القسم | بتعمل فيه إيه | محتاج صلاحية |
|---|---|---|
| **الرئيسية** `/admin` | الأرقام، وأهم حاجة: «محتاج شغل دلوقتي» — اللي مستني إيدك النهارده | — |
| **النصوص** `/admin/content` | كل كلمة معروضة في الموقع | `content.edit` |
| **اللعبة** `/admin/game` | أسئلة «مين جاي؟» ونقاطها وأنواع الشخصيات | `game.edit` |
| **حقول التسجيل** `/admin/profile-fields` | نموذج التسجيل نفسه: الحقول والاختيارات والموافقات | `fields.edit` |
| **القوالب** `/admin/templates` | وصف كل نوع خروجة: الحكاية، السعر، المدة، اللي شامله | `sbotat.edit` |
| **السبوطات** `/admin/sbotat` | المواعيد الفعلية — جدول وتقويم، نسخ، تكرار، إلغاء | `sbotat.view` |
| **الحجوزات** `/admin/bookings` | مين حاجز، قايمة الانتظار، وتسجيل الحضور | `bookings.view` |
| **المطابقة** `/admin/matching` | أوزان تكوين المجموعات، والتشغيلات، ونقل الناس بين المجموعات | `matching.view` |
| **الناس** `/admin/people` | الأعضاء، تاريخهم، الملاحظات، والحظر | `people.view` |
| **الكباتن** `/admin/captains` | الكباتن وطلباتهم وتقاريرهم، والأماكن | `captains.edit` |
| **الفلوس** `/admin/payments` | مراجعة التحويلات، الاسترداد، المحفظة، الكوبونات | `payments.view` |
| **البلاغات** `/admin/reports` | بلاغات الأعضاء على بعض — طابور الأمان | `reports.view` |
| **الرسايل** `/admin/notifications` | قوالب الواتساب، طابور الإرسال، والإرسال الجماعي | `notifications.view` |
| **الخريطة** `/admin/map` | مواقع الأماكن وأسماء المناطق | `map.edit` |
| **الإعدادات** `/admin/settings` | كل الأرقام والقواعد، مفاتيح المزايا، والصيانة | `settings.view` |
| **الشغل** `/admin/shoghl` | طبقة الفريلانسرز: المجالات، أماكن الشغل، وإعداداتها | `sbotat.view` |
| **السجل** `/admin/audit` | مين عمل إيه وإمتى | `audit.view` |

اللي مالوش صلاحية قسم، القسم ده مش هيبان له أصلًا في القايمة.

---

## حاجات لازم تاخد بالك منها

**الفلوس.** مراجعة التحويلات هي شغلك اليومي: العضو بيحوّل على فودافون كاش أو
إنستا باي وبيرفع صورة الإيصال، وإنت بتبص عليها وتأكد أو ترفض. الرفض لازم معاه
سبب مكتوب — العضو بيشوفه.

**الإلغاء مش زرار عادي.** لما تلغي سبوطة أو حجز، القاعدة بترجّع الفلوس لوحدها
حسب القواعد اللي في الإعدادات. والفرق مهم: «إحنا اللي لغينا» بيرجّع كل المبلغ
ويزوّد رصيد اعتذار كمان، و«العضو لغى» بيتبع قواعد المدة. اقرا كلام التأكيد
قبل ما تدوس.

**البلاغات.** ده طابور أمان، مش صندوق وارد. الناس بتقابل بعضها فعلًا على الأرض،
فالبلاغ المفتوح لازم يتقفل بقرار مكتوب.

**الإرسال الجماعي.** بيوريك عدد المستقبلين قبل ما تبعت، وفيه حد يومي في
الإعدادات. راجع العدد والنص الأول — الرسالة مش بتترجع.

---

## قسم «النصوص» — `/admin/content`

كل كلمة معروضة في نسبوط موجودة هنا. 357 نص، مقسومين على 19 شاشة.

**إزاي تشتغل:**
1. فلتر بالشاشة أو دوّر بالكلمة.
2. غيّر النص.
3. دوس «احفظ».

التغيير بيبان في الموقع **خلال أقل من دقيقة** من غير نشر —
وغالبًا خلال ثانيتين.

**حاجات مهمة:**

- **اللي بين `{{ }}` سيبه زي ما هو.** ده بيتملي لوحده.
  مثلًا `أنا جاي — {{price}}` بتطلع «أنا جاي — 300».
  لو مسحته، الرقم مش هيظهر.
- **الكلمات الممنوعة** بتتمنع لوحدها. لو النص فيه واحدة منها،
  الحفظ بيتقفل وبيقولك هي إيه.
- **النسخ القديمة**: كل تعديل بيتسجّل. دوس «النسخ القديمة»
  تحت أي نص علشان تشوف اللي كان قبله و«رجّع دي».
- **الاستبدال الجماعي** بيشتغل على النصوص المعروضة قدامك بس —
  فلتر الأول، شوف العدد، وبعدين غيّر.
- **نزّل نسخة / ارفع نسخة**: ملف JSON فيه كل النصوص.
  مفيد لو عايز تراجعها في ملف أو ترجع لنسخة قديمة كلها مرة واحدة.

---

## قسم «اللعبة» — `/admin/game`

لعبة «مين جاي؟» كلها بتتظبط من هنا: الأسئلة، الاختيارات، النقاط، والأنواع.

### الأسئلة والنقاط

- **الترتيب**: اسحب السؤال من `⠿`، أو استعمل السهمين.
- **شغّال**: لو قفلته، السؤال بيختفي من اللعبة من غير ما يتمسح.
- **نوع السؤال**: اختيار واحد · أكتر من اختيار · كتابة حرة.
- **شبكة النقاط**: كل صف اختيار، وكل عمود نوع شخصية.

**النقاط بتتقرا كده:**

| القيمة | معناها |
|---|---|
| فاضية | الاختيار ده ما بيأثرش على النوع ده |
| `0` | ما بيزوّدش نقط — بس لو حصل تعادل بيرجّح النوع ده |
| `1` وفوق | نقط فعلية بتتجمع |

### إزاي بتطلع النتيجة

1. بنجمع نقط كل نوع من كل الإجابات.
2. اللي مجموعه أكبر بيكسب.
3. **لو اتعادلوا**: بنبص على ترجيحات الصفر — لو واحد منهم مرجّح، بياخدها.
4. **لو لسه متعادلين**: الأول في ترتيب الأنواع بياخدها.

الطريقة دي في الكود. اللي بتتحكم فيه إنت هو **النقط والترتيب**.

### الأنواع

الاسم بصيغة المذكر والمؤنث، الجملة اللي تحته، ولون الستيكر.
**الترتيب مهم** — هو اللي بيكسر التعادل لما مفيش ترجيح.

### «جرّب واتفرّج على الحساب»

دوس على الإجابات وشوف النقط بتتحرك خطوة بخطوة على اليمين،
ومين كسب وليه. لو كان في تعادل، بيقولك مين اتعادلوا وإيه اللي حسمها.

استعمله **قبل** ما تغيّر نقط في اللعبة الحقيقية.

### الأرقام

كام واحد بدأ وكام خلّص، بيقفوا عند أنهي سؤال، وكل نوع طلع كام مرة.

---

## قسم «الشغل» — `/admin/shoghl`

ده قسم طبقة الفريلانسرز: سبوطات الشغل اللي بتتعمل في كافيهات ومساحات عمل.
صفحة واحدة بتمن تبويبات، وكلها شغّالة دلوقتي. القسم بيبان لأي حد عنده
`sbotat.view`، بس كل تبويب ليه صلاحيته لوحده — ولو مالكش صلاحية تبويب
هيقولهالك بالاسم بدل ما يفضل فاضي من غير سبب.

### المجالات — `fields.edit`

قاموس المجالات اللي العضو بيختار منها (تصميم، برمجة، كتابة محتوى…).
كل صف فيه: الاسم اللي بيظهر، مفتاح لاتيني ثابت، اسم أيقونة، ولون هيكس.

- «مجال جديد»: اكتب الاسم والمفتاح بيتعمل لوحده من الاسم — ينفع تغيّره
  **قبل** الإضافة بس. بعد كده ثابت لأن الكود بيشاور عليه.
- كل خانة في الجدول بتتحفظ لوحدها أول ما تسيبها.
- الأسهم ▲▼ بتغيّر الترتيب — وده الترتيب اللي بيظهر بيه للعضو.
- «شغّال» لما تطفّيه المجال بيختفي من الاختيارات، بس بيفضل على اللي اختاروه.

### أماكن الشغل — `sbotat.edit`

الأماكن من نوع «كافيه شغل» أو «مساحة عمل» بس. الجدول بيوريك الاسم والمنطقة
والحالة والتقييم — لو التقييم نزل تحت 4 بيتعلّم أحمر «التقييم نزل»، وده
معناه راجع تقييمات آخر سبوطات هناك قبل ما تعلن سبوطة جديدة فيه.

دوس على اسم المكان يفتحلك مواصفاته:

- **المكان نفسه:** الاسم، العنوان (بيظهر للعضو بعد الحجز بس)، المنطقة،
  النوع، وشغّال/واقف.
- **مواصفات الشغل:** عدد المكاتب، سرعة النت وملاحظة عليه، البريز، الصوت،
  أوضة اجتماعات، باركنج، تكييف، الحد الأدنى للطلب، مواعيد الفتح والقفل،
  أحسن أيام، الصور (مسار في كل سطر)، وملاحظات داخلية.
- **سعر الكرسي بالجملة:** اللي بندفعه للمكان. **داخلي** — العضو ما بيشوفوش هو
  ولا الملاحظات الداخلية، الموقع بيقرا من عرض من غيرهم.

«ضيف مكان» بيعمل المكان **واقف** بشكل افتراضي — كمّل مواصفاته وشغّله لما
تتأكد. الأسعار بتتكتب بالجنيه زي باقي اللوحة.

### الإعدادات — `settings.edit`

أرقام الشغل بس (باقي الإعدادات في قسم الإعدادات العام):

| المجموعة | اللي فيها |
|---|---|
| الكروت | سعر ومدة كارت ٤ أيام وكارت ٨ أيام، ومدة رجوع الجلسة عند الإلغاء المبكر |
| الجلسة المفردة | سعر الجلسة الواحدة وسعر أول مرة |
| المجموعة واليوم | أقصى عدد من نفس المجال في المجموعة، مدة توليد اليوم الثابت، ساعة الغدا وساعة الشكوى |
| المؤشرات | مستهدف التحوّل شغل → ترفيه (نسبة) |

كل خانة بتتحفظ لما تسيبها وبتبان في الموقع على طول. اللي عنده `settings.view`
من غير `settings.edit` بيشوف الأرقام بس.

### الكروت — `payments.review`

الكارت بيتشرى من الموقع بتحويل يدوي زي أي حجز: العضو بيختار كارت ٤ أو ٨ أيام،
بيحوّل، وبيرفع صورة التحويل. الكارت بيفضل **مستني الاعتماد** لحد ما تبص عليه هنا.

- فوق: **مبيعات الكروت بالشهر** — المعتمدة بس. الكارت المعلّق مش مبيعة لحد ما يتأكد.
- الجدول بيتفلتر بالحالة (المستني هو الافتراضي): اسم العضو ورقمه، نوع الكارت،
  الاستهلاك (كام يوم اتصرف من كام)، الحالة، تاريخ الانتهاء، والسعر.
- **«اعتمد»** بيفعّل الكارت على طول: بيبدأ من دلوقتي وبينتهي بعد المدة اللي في
  الإعدادات (٦ أسابيع لكارت الـ٤، ١٠ لكارت الـ٨). **«ارفض»** بيلغي الكارت
  وبيوصل العضو السبب — فاكتب السبب في خانة «السبب» تحت قبل ما ترفض.
- الزرارين بيمشوا من نفس الدالة اللي بتعتمد أي تحويل تاني، علشان الكارت والدفعة
  يتحركوا مع بعض. **متعدلش حالة الكارت من القاعدة بإيدك** — هتفصله عن دفعته.
- لو مكتوب جنب الكارت «لسه مرفعش صورة»، يبقى طلب الكارت وما حوّلش لسه.
- **«ضيف كارت يدوي»** للتعويض أو الهدية: رقم العضو + نوع الكارت + السبب.
  بيتعمل **شغّال على طول** بصفر جنيه، والسبب بيتسجّل مع الكارت وبيفضل ظاهر
  في الجدول. مش بيتحسب في المبيعات.

خصم اليوم من الكارت بيحصل لوحده عند الحجز، والرجوع لوحده عند الإلغاء المبكر
(قبل المدة اللي في الإعدادات). مفيش خانة تعدّل الرصيد بالإيد — ده مقصود.

### الأيام الثابتة — `bookings.edit`

مين عنده يوم ثابت في الأسبوع، وإحنا بنحجزله فيه لوحدنا.

- الجدول: العضو، اليوم (وساعته لو محدد)، المكان (أو «أي مكان»)، القالب،
  هل الحجز تلقائي، الحالة، وآخر تاريخ اتولّد له.
- **«أوقف أسبوعين»** بيوقف التوليد أسبوعين وبيرجع لوحده بعدها — مش إلغاء.
  **«رجّعه»** بيشيل الإيقاف فورًا. **«ألغِ»** بينهيه خالص (وبعدها يقدر يعمل
  يوم جديد في نفس اليوم).
- **«اقفل التلقائي»** بيسيب اليوم شغّال بس من غير حجز — العضو بياخد تفكير وبس.

التوليد بيحصل لوحده كل يوم ٩ الصبح بتوقيت القاهرة، قبل الموعد بالمدة اللي في
الإعدادات (٧ أيام افتراضيًا). لو مفيش سبوطة شغل معلنة في اليوم ده بيتسجّل تنبيه
في تنبيهات الأماكن. لو الكارت خلص بيوصل العضو إشعار من غير حجز.

- **«ولّد دلوقتي»** (فوق على اليسار) بيشغّل نفس مهمة التوليد فورًا بدل ما
  تستنى ٩ الصبح. **ده مش تجربة**: بيحجز فعلًا للي عندهم رصيد كارت وبيبعتلهم
  إشعار — فالسؤال اللي بيطلعلك قبل ما تدوس مقصود. لو رجعلك «مفيش حاجة محتاجة
  توليد دلوقتي» يبقى كله متولّد خلاص، مش عطل.

> الزر ده بيمشي على `fn_admin_run_work_recurring()` — غلاف اتضاف في هجرة
> 0050 بيتأكد من `bookings.edit` وبعدين بينادي `job_work_recurring()`
> (اللي هي نفسها ممنوحة لحساب الخادم بس). لو الزر رجّعلك «permission denied»
> يبقى `WORK_MIGRATION_4.sql` لسه ما اتشغّلش.

### تقارير الأماكن — `payments.view` للقراءة · `payments.review` للدفع

حساب أسبوعي لكل مكان شغل: بندفع لهم كام عن الأسبوع، وعلى أساس إيه.

- **الأسبوع بيبدأ الاتنين** — زي ما القاعدة بتخزّنه بالظبط. الافتراضي هو آخر
  أسبوع **كامل**، ومن القايمة أو من «أسبوع قبله / أسبوع بعده» تتنقل بينهم.
- الجدول لكل مكان: عدد السبوطات، الحاضرين، الغياب، متوسط تقييم المكان
  (بيتعلّم أحمر تحت ٤)، المستحق، وهل اتدفع.
- **المستحق = الحاضرين × سعر الكرسي بالجملة** بتاع المكان (من تبويب «أماكن
  الشغل»). لو السعر ما اتحطش، المستحق هيطلع صفر — ده مش عطل في الحسبة.
- **«ابنِ تقرير الأسبوع»** بيحسب الأسبوع اللي إنت واقف عليه من أول وجديد.
  آمن تدوسه أكتر من مرة: الصفوف اللي **اتسجّل إنها اتدفعت ما بتتلمسش** خالص.
  الأسبوع اللي لسه ما خلصش مش بيتبني — استنى يقفل علشان الأرقام تبقى كاملة.
- **«اتدفع»** بيسجّل إننا دفعنا للمكان (التاريخ ومين سجّله). مفيش تراجع من
  اللوحة — فاقرا المبلغ في السؤال قبل ما تأكد.
- **«CSV»** بينزّل تقرير المكان ده علشان تبعتهوله. **الملف ده بيروح لبره،
  فمفيهوش ولا اسم عضو ولا سعر الكرسي بالجملة** — أرقام مجمّعة بس. بيتفتح
  في إكسل بعربي سليم.

التقرير بيتبني لوحده كل اتنين ٨ الصبح بتوقيت القاهرة للأسبوع اللي فات.

### المؤشرات — `settings.view`

الأرقام اللي بتقول إن طبقة الشغل بتعمل شغلها ولا لأ.

**فوق خالص وأهم حاجة في الصفحة: التحوّل شغل ← ترفيه خلال ٣٠ يوم.** من كل ١٠٠
واحد جه أول مرة على سبوطة شغل، كام واحد فيهم حجز سبوطة **ترفيهية مدفوعة** خلال
٣٠ يوم بعدها. بيتقارن بمستهدف `settings.work_conversion_target_pct` (من تبويب
«الإعدادات»): **أخضر** يعني عند المستهدف أو فوقه، **أحمر** يعني تحته.

ليه ده أهم رقم؟ لأن الشغل **مش خط إيراد** — ده باب للنادي. لو الرقم واطي يبقى
الناس بتيجي تشتغل وتمشي، والباب مش بيفتح، وساعتها كل الأرقام التانية (كروت
اتباعت، حجوزات) مش بتعني حاجة.

- الرقم الكبير محسوب على **الأسابيع اللي خلصت نافذتها بس**. الأسبوع اللي لسه
  ما عداش عليه ٣٧ يوم بيتعلّم «(لسه)» في الجدول و**مش داخل** في الرقم — علشان
  ما نحسبش نسبة على ناس لسه قدامهم وقت يتحوّلوا فيه.
- لو مفيش حد عدّى عليه ٣٠ يوم لسه، بيكتب «لسه» بدل ما يوريك صفر كإنه حقيقي.
- الجدول تحت: كل أسبوع بسبوطاته وحجوزاته وحضوره ونسبة غيابه، الكروت المباعة
  وإيرادها، الجلسات اللي اتخصمت، ونسبة التبادل في «شغالين معاك».
- **«حدّث الأرقام»** بيعيد حساب كل ده حالًا. من غيره بتشوف حسبة آخر ٣:١٥ فجرًا.
- لو طلعتلك رسالة إن الأرقام فاضية، يعني مفيش ولا سبوطة شغل ولا كارت اتسجّل
  لسه. دوس «حدّث الأرقام» الأول؛ لو فضلت فاضية يبقى فعلًا لسه مفيش بيانات.

### الشركات — `people.view`

الشركات اللي ملت نموذج «عايزين نجيب فريقنا» في صفحة `/shoghl`.

- الأحدث فوق. فوق الجدول عدّاد لكل حالة، وفلتر بالحالة.
- الأعمدة: الشركة والشخص، الرقم (بيتدوس عليه فيتصل)، عدد الناس، كام مرة في
  الشهر، **«اللي كتبوه»** (نص الشركة نفسها — بيفضل زي ما هو)، **«ملاحظتنا»**
  (خانة تكتب فيها إنت — بتتحفظ لما تسيبها)، وتاريخ الطلب.
- **الحالة** بتتغيّر من القايمة: جديد · كلمناهم · اتحوّل لعميل · مامشيش.
  أول ما تغيّرها بيتسجّل إنك إنت اللي بتتابع الطلب ده.
- القراءة والتعديل الاتنين على `people.view` — كده سياسة `leads` في هجرة 0043
  بالظبط، مش سهو. يعني أي حد يقدر يشوف الطلبات يقدر يغيّر حالتها.
- الإدراج بيحصل من النموذج بس (٣ طلبات في اليوم لكل رقم). مفيش «ضيف طلب»
  من اللوحة عن قصد.

> لو أي تبويب طلعلك رسالة إن الجدول أو العمود مش موجود،
> يبقى `WORK_MIGRATION.sql` لسه ما اتشغّلش على القاعدة. ولو الزراير اللي
> بتشغّل مهام (**«ولّد دلوقتي»** · **«ابنِ تقرير الأسبوع»** · **«حدّث
> الأرقام»**) رجّعت «permission denied»، يبقى `WORK_MIGRATION_4.sql` هو اللي
> ناقص.

---

## لو التغيير مبانش

1. استنى دقيقة — أقصى تأخير 30 ثانية.
2. اعمل تحديث للصفحة (Ctrl+Shift+R).
3. لو لسه، يبقى فيه مشكلة في الاتصال بالقاعدة —
   الموقع ساعتها بيشتغل بنسخة احتياطية مخزّنة في الكود، وده مقصود
   علشان الموقع ما يقعش. كلّم المبرمج.

---

## للمبرمج

- النصوص في `copy_strings`، والنسخة الاحتياطية المتولّدة في `src/data/copy-fallback.ts`.
- `npm run check:copy` بيفشل لو أي نص عربي رجع تاني في المكوّنات.
- اللعبة في `game_questions` · `game_options` · `game_option_scores` · `personality_types`،
  والاحتياطي في `src/data/game.ts`. منطق الحساب في `src/lib/game-config.ts`.
- الكاش: `unstable_cache` بوسم `copy`، مهلة 30 ثانية، وبيتبطّل على طول
  من `POST /api/admin/revalidate` (بتوكن أدمن) أو `POST /api/copy/revalidate` (بـ `CRON_SECRET`).
- `npx tsx scripts/check-admin.ts` — صلاحيات اللوحة، ومنع الزائر، ورفض الأدوار الأقل.
- `npx tsx scripts/check-admin-login.ts` — الدخول بخطوتين من الآخر للآخر + فتح كل الأقسام.
- `npx tsx scripts/check-game.ts` — اللعبة من القاعدة بتدي نفس نتايج الاحتياطي.
- `npx tsx scripts/check-matching.ts` — المطابقة: كل واحد بيتحط مرة واحدة،
  الأحجام في حدودها، وسبوطة البنات بتفضل بنات.
- الفحوصات دي محتاجة سيرفر شغّال على `SITE_URL` (افتراضيًا `localhost:3210`)
  ورقم اختبار في `TEST_PHONE_ALLOWLIST` — وأرقام الاختبار مقفولة في الإنتاج.

### حاجة لازم تتظبط قبل الإنتاج
`admin_users.totp_secret` متخزّن نص صريح. أي حد يوصل لمفتاح الخدمة أو لنسخة
من القاعدة ياخد سر التحقق بخطوتين لكل الفريق — يعني الحماية بترجع خطوة واحدة.
الحل: تشفيره بمفتاح بره القاعدة (Supabase Vault أو AES-GCM بمفتاح في متغير
بيئة منفصل عن مفتاح الخدمة).
