-- ============================================================================
-- الجزء ١ من ٢ — طبقة «الشغل»
--
-- شغّل الملف ده الأول لوحده، واستنى «Success»، وبعدين شغّل الجزء ٢.
--
-- ليه مقسوم؟ بوستجرس ما بيسمحش تستخدم قيمة enum جديدة في نفس المعاملة اللي
-- ضافتها، ومحرر SQL بيشغّل الملف كله كمعاملة واحدة. الجزء ده بيضيف القيمتين
-- وبيخلص، فالجزء ٢ يلاقيهم متسجّلين.
--
-- آمن يتكرر.
-- ============================================================================

alter type venue_kind_t add value if not exists 'cafe_work';
alter type venue_kind_t add value if not exists 'coworking';
