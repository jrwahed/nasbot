# CLAUDE.md — قاموس مشروع «نسبوط»

> أي إيجنت أو شخص بيفتح المشروع: **اقرأ الملف ده الأول.** هو خريطة المشروع
> وقواعده وطريقة الشغل فيه. مكتوب بالعربي علشان صاحب المشروع يقراه، والمصطلحات
> التقنية بالإنجليزي. لو غيّرت بنية المشروع، حدّث الملف ده.

---

## ١. إيه هو نسبوط

نادي خروجات مع ناس جُدد في القاهرة. المجموعة صغيرة، والمجموعة بتتكشف قبل
الخروجة بيوم. **مفيش عضوية.** فيه كمان طبقة **«الشغل»** للفريلانسرز
(سبوطات شغل نهارية + كارت مدفوع مقدم + يوم أسبوعي ثابت).

⚠ **المنتج اتحوّل (٢٠٢٦-٠٩-١٢):** نسبوط ما بقاش قايم على «الكابتن
والتنظيم». **العضو العادي بيفتح خروجته بنفسه** والناس اللي ما تعرفهوش
تنضم ليها. سبوطات نسبوط وسبوطات الأعضاء بيقعدوا في **نفس القايمة**،
وعمود `sbotat.origin` هو اللي بيفرّق. الكابتن بقى **اختياري** — «صاحب
الخروجة» (`sbotat.host_id`) هو اللي بيمسك المجموعة، وهو عضو زي أي حد
مش موظف مدفوع.

- **منشور على:** Vercel — `https://nasbot.vercel.app`
- **القاعدة:** Supabase (مؤسسة `jpqjqyuecrabvsgcidfs` ← مشروع **`nasbot-prod`** = `nutmgtulrqrfaysrigfi`، منطقة `eu-central-1`). ⚠ في نفس المؤسسة مشروع تاني فاضي (`vohmnfaidwgvuusezktc`) — **متلمسهوش**.
- **الفرع:** التطوير على `claude/beautiful-euler-es0a2h`، والنشر من `main`

---

## ٢. المكدّس والأوامر

- **Next.js 15.1.12** (App Router) · **React 19** · **TypeScript strict** · **Tailwind**
- **Supabase** (Postgres 17 + Auth + Storage + RLS + pg_cron + pg_net)
- **الإيميل:** SMTP (Gmail) عبر `nodemailer` — شغّال. الواتساب **مش** متفعّل.

```bash
npm run dev            # تطوير
npm run verify         # الحراس التلاتة + tsc --noEmit + next build  ← قبل أي رفع
node scripts/check-copy.mjs            # مفيش نص عربي في المكونات
node scripts/check-notify-vars.mjs     # متغيّرات قوالب الإشعارات = CORE
node scripts/check-content-fallback.mjs # مفيش صفحة محتوى بتطلع فاضية
node scripts/check-migration-bundles.mjs # حزم WORK_MIGRATION = ملفات الهجرة
node scripts/check-nav-flags.mjs       # مفيش مدخل لصفحة ورا مفتاح من غير نفس المفتاح
```
البناء على Vercel بيتجاهل `NEXT_DIST_DIR` (شوف `next.config.mjs`). Node مثبّت `22.x`.

---

## ٣. القواعد الحاكمة — ممنوعات مطلقة

1. **ممنوع نص عربي في أي مكوّن** تحت `src/app` أو `src/components` (ما عدا `src/app/admin/**`). كل نص بيمر بـ `const t = useT()` ومفتاحه في `copy_strings`. الحارس `scripts/check-copy.mjs` بيفشل البناء لو خالفت.
2. **كل رقم في `settings`، مش في الكود** — سعر، نسبة، ميعاد، حد. لو رقم في الكود، ده باج (شوف `REVIEW_ADMIN.md` قايمة «المفروض من اللوحة»).
3. **الأمان من RLS مش من إخفاء الأزرار.** أي فعل حساس لازم يترفض على الخادم. مفتاح الخدمة `SUPABASE_SERVICE_ROLE_KEY` ممنوع يوصل المتصفح.
4. **الفلوس بالقروش (piastres)** في القاعدة، وبالجنيه في الواجهة (`toPounds`/`toPiastres` في `map-db.ts`).
5. **الدفع تحويل يدوي** (فودافون كاش / إنستا باي) + اعتماد من اللوحة (`fn_approve_transfer`). مفيش بوابة.
6. **كلمات ممنوعة في النصوص:** تعارف · شريك · إعجاب · فعالية · تذكرة · باقة · منصة · مستخدم · اكتشف. النبرة عامية مصرية.
7. **الهوية:** مش تطبيق تعارف (مفيش صور قبل الكشف، الاختيار سري)، ومش موقع تذاكر (**المجموعة** هي المنتج — مش المكان ولا النشاط). ⚠ القاعدة دي كانت بتقول «الكابتن والمجموعة هما المنتج» — الكابتن اتشال منها في ٢٠٢٦-٠٩-١٢ لما العضو بقى يفتح خروجته بنفسه. **السرية ما اتكسرتش ولا هتتكسر:** حتى صاحب الخروجة بيشوف مجموعته بعد الكشف بس، بالظبط زي أي حد حاجز. الألوان: `#F4632A` برتقالي · `#14161A` غامق · `#FBF7EF` كريمي · `#2B4CFF` كوبالت · `#EFE3CF` رملي. مفيش ظلال ناعمة ولا تدرجات ولا أبيض ناصع.

---

## ٤. البنية

```
src/
  app/
    (عام)        /  /one  /map  /captains  /join  /login  /me  /game …
    rules faq about terms aman   صفحات المحتوى — محتواها كله من `content_blocks`
    tamenny/[token] «معايا حد يعرف» — الصفحة الوحيدة اللي بتدّي معلومة لحد مش مسجّل
    new          «افتح خروجة» — العضو بيظبّط خروجته (ورا مفتاح member_sbota)
    game         «مين جاي؟» — مدخلها `GameStrip` في الرئيسية + رابط الهيدر
    me/sbotati   «خروجاتي» — اللي أنا فاتحها: الأعداد · السطر · الإلغاء
    captain/[id] لوحة اللي ماسك المجموعة — صاحب الخروجة أو كابتن نسبوط
    s/[slug]     صفحة السبوطة + /pay + /done
    my/[id]      حجزي + /chat + /review
    shoghl/      طبقة الشغل: /  /[slug]  /[slug]/pay  /pass  /amaken
    me/shoghl    شغلي (الكارت + اليوم الثابت + شغالين معاك)
    admin/       اللوحة (20 قسم — شوف §7)
    api/         otp · pay(create/transfer/pass) · admin · account/ensure ·
                 copy/revalidate · cron/* · health/mail
  components/    مكوّنات الواجهة + components/work/* لطبقة الشغل + admin-ui.tsx
  data/          نصوص احتياطية + قوايم (⚠ بعضها المفروض في القاعدة — REVIEW)
  lib/           طبقة البيانات والمنطق (§5)
  lib/server/    server-only: admin-auth · mailer · otp · supabase-admin · whatsapp
supabase/migrations/   كل تغييرات القاعدة، بالترتيب الزمني في الاسم
scripts/         check-copy · فاحصات · بذور النصوص
```

### الملفات المفتاحية
| الملف | مسؤول عن |
|---|---|
| `src/lib/api.ts` | **طبقة البيانات الأساسية** — كل قراءة/كتابة للموقع العام. لو `hasSupabase=false` بترجع `api-mock.ts` |
| `src/lib/work.ts` · `collab.ts` | طبقة الشغل والتعاون |
| `src/lib/map-db.ts` | تحويل صفوف القاعدة ↔ أنواع الواجهة، والفلوس |
| `src/lib/copy.ts` | قراءة `copy_strings` بكاش موسوم (`COPY_TAG`) |
| `src/lib/server/admin-auth.ts` | `requirePermission` — الحد الأمني الحقيقي للوحة |
| `src/lib/supabase.ts` | عميل المتصفح (anon). `hasSupabase` بيتأكد إن المتغيرين موجودين |
| `src/middleware.ts` | حارس `/admin/*` على الحافة (طبقة أولى مش الحد الأمني) |
| `src/lib/server/pay-guard.ts` | بيمسك رقم فودافون كاش/إنستاباي الوهمي ويوقف الدفع قبل ما يتعمل حجز |
| `src/components/HostCard.tsx` | بطاقة صاحب الخروجة — **من غير صورة** عن قصد (الهوية §٣.٧) |
| `src/lib/content.ts` | صفحات المحتوى بكاش موسوم — نفس نمط `copy.ts` بالحرف |
| `src/components/FooterLinksProvider.tsx` | بيمرّر المحتوى للعميل: روابط الذيل · `useContentRef('guarantee')` · `useNumberedRules()` |

---

## ٥. القاعدة — أهم الجداول والدوال

**الجداول:** `profiles` · `sbotat`(+`sbotat_public`) · `sbota_templates` · `venues` · `bookings` · `payments` · `wallet_ledger` · `coupons` · `pair_affinity` · `matching_runs` · `chat_rooms`/`chat_members` · `notifications`/`notification_templates` · `admin_users`/`admin_roles`/`role_permissions` · `settings`(صف واحد) · `copy_strings` · `safety_links` · `audit_log`. **طبقة الشغل:** `professions` · `work_venues` · `work_passes` · `pass_redemptions` · `recurring_bookings` · `work_affinity` · `venue_reports` · `leads` · `work_metrics`.

**دوال مهمة:** `fn_can_book` · `fn_capacity_guard` (قفل السعة) · `fn_booking_paid` · `fn_cancel_booking` · `fn_approve_transfer` · `fn_redeem_pass`/`fn_revert_pass`/`fn_activate_pass` · `fn_build_matching`/`fn_build_work_matching` · `fn_reveal` · `fn_pair_want`/`fn_work_want` (الطريق الصح لكتابة التبادل) · `fn_is_admin`/`fn_has_permission` · `fn_can_see_place`/`fn_sbota_place` (الحارس والبيانات، كل واحد لوحده) · `fn_safety_link`/`fn_safety_my`/`fn_safety_view`/`fn_safety_mark`/`fn_safety_revoke` · `fn_sbota_arrival`/`fn_set_sbota_sign`.

**صفحات المحتوى (0082):** `content_pages` · `content_blocks`. القواعد والأسئلة ومين إحنا والشروط — الفقرات بتتضاف وتتمسح وتترتّب من `/admin/content` ← «صفحات الموقع». الكتابة بـ`content.edit`. والفقرة اللي الكود بيسأل عنها بالاسم ليها `ref` ثابت (`guarantee`).

**خروجات الأعضاء (0078–0081 · 0086):** `sbotat.origin`/`host_id`/`host_name_ar`/`host_note_ar` · `fn_create_sbota` (الطريق الوحيد) · `fn_update_own_sbota` · `fn_cancel_own_sbota` · `fn_my_hosted_sbotat` · `fn_is_my_sbota_host`. الحدود السبعة في `settings` (`site_fee` · `member_sbota_*`) ومفتاح الميزة `member_sbota`.

**العضو بيكتب خروجته (0086):** `sbotat.title_ar`/`details_ar`/`venue_name_ar`/`address_ar`/`cost_note_ar` — مش بيختار من قايمة. **الحجز فيها ببلاش** (`fn_book_free`، وبترفض أي سبوطة سعرها مش صفر)، والتكلفة **معلومة** مش مبلغ بنحصّله. العنوان بيبان مع الكشف بس (`fn_sbota_address`). وفيه محفّز `fn_guard_sbota_text` بيرفض الكلمات الممنوعة في القاعدة.

⚠ **ليه قالب عام (`khroga-men-3odw`) بدل `template_id` nullable؟** العمود `not null` وعليه join في `sbotat_public` وبيتقرا في `fn_can_book`. القالب العام خلّى كل ده ما يتلمسش، وكلام العضو بيغطّي عليه في الفيو بـ`coalesce`.

**⚠ الأمان الحرج — تلات قواعد اتكسروا قبل كده:**

1. **كتابة التبادل لازم عبر `fn_pair_want`/`fn_work_want`.** الكتابة المباشرة على جداول affinity كانت ثغرة تزوير تبادل (S1/S2 — اتسدّت في `0052` بسحب سياسات الكتابة خالص).
2. **سياسات الكتابة الحساسة لازم `fn_has_permission('<الصلاحية>')` مش `fn_is_admin()`.** `fn_is_admin` بقت «أي صف نشط في admin_users»، يعني حتى `support` كان بيغيّر الأسعار (`0056`). القراية للإدارة سايبينها على `fn_is_admin` عن قصد.
3. **ممنوع تستخدم `current_user` كحارس جوه `security definer`.** بوستجرس بيخلّيه = **صاحب الدالة** (postgres) مش اللي بينادي، فالشرط عمره ما بيبقى true والحارس بيبقى ديكور. ده حصل فعلًا في تلات دوال. استخدم **`fn_caller_is_browser()`** (`0063`) — بتقرا الدور من `request.jwt.claims`: anon/authenticated = true، وservice_role والكرون = false.

   ونفس المصيدة بالظبط في العكس: **`auth.uid() is not null` مش حارس.** الزائر المجهول `auth.uid()` بتاعه null، فشرط زي `if auth.uid() is not null and not fn_has_permission(...)` **بيعدّي المجهول** بدل ما يمنعه (ده اللي فتح `fn_reveal` للنت كله — `0062`).

4. **لو العضو بيعمل حاجة جديدة، اعملها بدالة مش بسياسة `insert`.** `fn_create_sbota` (0078) مثال: لو سبنا باب كتابة مباشر على `sbotat`، العضو كان هيكتب `price = 0` أو `origin = 'nasbot'` وينتحل صفة الموقع. الدالة أصلًا **مفيهاش parameter للسعر** — بيتحسب جوه من القالب و`settings`. نفس نمط `fn_pair_want` بالظبط.

**وكمان:** أي دالة جديدة بتبقى `execute` لـ **PUBLIC** افتراضيًا في بوستجرس. كل دالة حساسة لازم يبقى وراها `revoke execute … from public, anon;` صريح — المنح لـ `authenticated` لوحده **مش** كفاية.

**المهام المجدولة (pg_cron جوه Supabase، مش Vercel Cron):** الكشف · التذكير · قفل الشات · المقاييس · التنضيف · مهام الشغل الأربعة · إشعارات الشغل. الجدولة في `RUNBOOK.md` و`WORK_CRON.sql`.

⚠ **١٢ مهمة منهم بيكتبوا في `notifications` بس — والمهمة رقم ١٣
(`nasbot-work-notify`) هي الوحيدة اللي بتبعت فعلًا.** يعني أي رسالة
بتستنى دورها. كانت مجدولة **كل ساعة** عند الدقيقة ٥، والمقاس من الإنتاج:
إيميل «مكانك محجوز» اتولد 11:36 واتبعت 12:05 — **٢٩ دقيقة**. و«مجموعتك
ظهرت» (من `job_reveal_due` كل ١٠ دقايق) كانت ممكن تستنى قرب ساعة، وهي
اللحظة اللي طقس الكشف كله متعلّق بيها. بقت **كل ٥ دقايق** (٢٠٢٦-٠٩-٢١).

والتذكيرات كانت شكلها تمام (٥ دقايق تأخير) لأنها بتتولد على رأس الساعة
بالصدفة — **الرقم كان بيكدب**. لو بتقيس تأخير، قيسه على رسالة بتتولد في
وقت عشوائي (تأكيد الحجز)، مش على وحدة مجدولة.

---

## ٦. طريقة تطبيق تغييرات القاعدة (مهم)

الإيجنت **ما عندوش وصول مباشر للقاعدة**. فأي تغيير قاعدة:
1. يتكتب ملف هجرة في `supabase/migrations/` (اسمه `<timestamp>_<NNNN>_<وصف>.sql`).
2. يتجمّع في ملف `WORK_MIGRATION_N.sql` في الجذر بترويسة عربي.
3. **صاحب المشروع** بيلزقه في Supabase ← SQL Editor ← Run.

⚠ محرر SQL بيشغّل الملف كـ **معاملة واحدة**، وPostgres ما بيسمحش تستخدم قيمة `enum` جديدة في نفس المعاملة اللي أضافتها → لو في `alter type ... add value`، اعزله في ملف لوحده يتشغّل الأول. (ده اللي خلّى `WORK_MIGRATION` اتقسم لـ 1 و2.)

كل هجرة: `if not exists`/`create or replace`/`on conflict` — آمنة تتكرر. ومعاها دالة اختبار `test_*` بترجّع صفوف «نجح/فشل».

⚠ **«آمنة تتكرر» لازم تتجرّب، مش تتفترض.** الزق الملف **مرتين ورا بعض** على قاعدة
الاختبار — لو التانية وقعت، الملف مش آمن. أشهر غلطة: `create policy X` من غير
`drop policy if exists X` قبلها بالظبط (مش الاسم القديم — **نفس الاسم الجديد**).
ده وقع فعلًا في `0056` ووقف اللزق على المالك.

### ⚠ الاختبار مش رفاهية — الجزء ده اتكتب بعد ما اتعلمناها بالعافية

الدفعة `0052..0060` اتكتبت **من غير** دوال اختبار. لما اتكتبت `test_review_fixes()`
واتشغّلت، لقت **اتنين من التصليحات نفسهم مكسورين** (`fn_reveal` مفتوحة للزائر
المجهول، وتلات حراس definer ما بيتنفّذوش خالص). يعني التصليح نفسه محتاج يتفحص.

**إزاي تفحص من غير وصول للقاعدة** — فيه بوستجرس محلي في البيئة، والشيم جاهز:

```bash
# ابدأ سيرفر مؤقت
mkdir -p /tmp/pg && chown -R postgres:postgres /tmp/pg
su postgres -c "/usr/lib/postgresql/16/bin/initdb -D /tmp/pg/data -U postgres --auth=trust"
su postgres -c "/usr/lib/postgresql/16/bin/pg_ctl -D /tmp/pg/data -o '-p 5433' -w start"

# شغّل الشيم (بيعمل أدوار anon/authenticated/service_role + auth.uid() + cron)
# ثم كل الهجرات بالترتيب، ثم الملف الجديد كمعاملة واحدة زي SQL Editor:
psql -h 127.0.0.1 -p 5433 -U postgres -d nasbot --single-transaction -f WORK_MIGRATION_N.sql
```

الشيم في `scripts/pg-shim.sql`، وبيركّب `pgcrypto` و`uuid-ossp` في اسكيما
**`extensions`** زي سوبابيس بالظبط (شوف الدرس التمنتاشر — الفرق ده خلّى
باج يعدّي محليًا ويقع على الإنتاج). **أربع** هجرات قديمة بتفشل على المحلي
(`0025_storage_buckets` · `0026_scheduled_functions` ·
`repair_seeded_auth_users` · `0073_media_write_policies`) لأنها محتاجة
`storage` و`pg_cron` الحقيقيين — **ده متوقّع**، مش باج.

**والأهم: اختبر السلوك مش الشكل.** إن المحفّز «موجود» ما يعنيش إنه بيمنع.
لكل دالة حساسة جرّب الأربع حالات:

| مين بينادي | إزاي تحاكيه | المتوقّع |
|---|---|---|
| زائر مجهول | `set role anon;` + claims `{"role":"anon"}` | `permission denied` |
| عضو من غير صلاحية | `set role authenticated;` + `{"sub":"<uuid>","role":"authenticated"}` | استثناء من الحارس |
| مالك بالصلاحية | نفس ده بـ `sub` بتاع صف في `admin_users` | **بينجح** (متكسرش اللوحة) |
| مفتاح الخدمة / الكرون | `{"role":"service_role"}` أو من غير claims | بيعدّي |

---

## ٧. اللوحة `/admin`

الأقسام: الرئيسية · النصوص · اللعبة · حقول التسجيل · القوالب · السبوطات · الحجوزات · المطابقة · الناس · الكباتن · الفلوس · البلاغات · الرسائل · الخريطة · الإعدادات · **الشغل** · **الفريق** · السجل. الدخول: إيميل + باسورد + تطبيق مصادقة (TOTP). الأدوار: `owner`·`admin`·`ops`·`finance`·`support`.

في `/admin/sbotat` فيه فلتر «مين فاتحها» ووسم «من عضو» وزرار **«اعتمد»** للمسوّدات (بيبان بس لما `member_sbota_auto_open = false`). وفي `/admin/settings` تبويب الأرقام فيه مجموعة «خروجات الأعضاء».

**نمط الكتابة الصح في اللوحة:** بعد أي `update/insert/delete` اعمل `.select('id')` وعدّي النتيجة على `rejected()` من `src/lib/admin.ts` — المصفوفة الفاضية معناها **القاعدة رفضت**، مش نجاح. من غير الحارس ده الصفحة بتقول «اتحفظ ✓» وهي كذبة. مطبّق دلوقتي في: الحجوزات · النصوص · اللعبة · المطابقة · حقول التسجيل · الإعدادات · الشغل · الفريق.

---

## ٨. الحالة الحالية (٢٠٢٦-٠٩-١٢)

- ✅ منشور وشغّال · دخول إيميل+باسورد · لوحة كاملة · طبقة الشغل (المراحل ١-٦) · إيميل بجدولة.
- 🟢 **نقلة المنتج: العضو بيفتح خروجته بنفسه — اتلزقت** (`WORK_MIGRATION_8.sql`: `0078` الأعمدة والدوال والحدود · `0079` اختبارها · `0080` أماكن الفورم · `0081` ٥٢ نص). *(اللزق بكلام المالك ٢٠٢٦-٠٩-١٢ — لو حصل شك، شوف «التأكد إيه اللي اتلزق» تحت.)*
- 🟢 **صفحات الذيل بقت شغّالة — اتلزقت** (`WORK_MIGRATION_9.sql` / `0082`). كانت ٥ روابط و**أربعة منهم بيروحوا `/rules`**؛ دلوقتي كل واحدة لها مسار ومحتواها في `content_blocks`. و«بقى كابتن» بقى «افتح خروجة» → `/new`.
- 🟢 **«دليل السبوطات» محلّ بيانات العرض — اتلزق (٢٠٢٦-٠٩-١٢).** `WORK_MIGRATION_14A/14B` (`0088`–`0093`): بيانات العرض كلها اتمسحت (٩ سبوطات · ٩ حجوزات · ٤ كباتن · ٥ أماكن · ١٢ عضو وهمي · ١٠ قوالب)، و**٨٥ قالب** من الدليل حلّوا محلها في ٨ تصنيفات (`adventure` ١٧ · `curious` ١٨ · `nature` ١٤ · `music` ١٠ · `water_sun` ٩ · `handmade` ٧ · `nile` ٥ · `culture` ٥).
  - عمودين جداد: `keywords_ar` · `photo_alt_ar` — بيتعدّلوا من `/admin/templates`، ونص الصورة بيوصل لـ`alt` الحقيقي عبر `sbotat_public`.
  - **مفيش صور ومفيش أسعار**: الدليل متعمّد إنه مفيهوش أرقام. `default_price = 0` في الـ٨٥ كلها — لازم المالك يسعّرها قبل ما يفتح سبوطة، ويرفع الصور من اللوحة.
  - **طبقة الشغل ما اتلمستش** (٣ قوالب + ٤ أماكن)، والقالب العام `khroga-men-3odw` فضل (`fn_create_sbota` بتشاور عليه).
  - ⚠ **لزق بالترتيب:** `14A` لوحده الأول (فيه `alter type … add value`) وبعدين `14B`.
- 🟢 **كل الدفعات اتلزقت على الإنتاج (٢٠٢٦-٠٩-١٢) — من `WORK_MIGRATION_5` لحد `_13`.** الفحص الشامل رجّع **٢٤ دالة اختبار · ١٦٩ صف · صفر فشل**، والفاحص نفسه اتجرّب بفخ متعمّد قبل ما نصدّقه.
  - `_10` «عجل» رجعت نشطة · `_11` محتوى الصفحات والرسايل (`0084`) · `_12` نصوص مدخل اللعبة (`0085`) · `_13` العضو بيكتب خروجته (`0086`+`0087`) — الحجز فيها **ببلاش**، والعنوان بيبان مع الكشف، وكل خروجة **مسوّدة لحد ما اللوحة تعتمدها**.
  - **الشروط لسه مقفولة** لحد ما تملا مدة صلاحية الرصيد ووسيلة التواصل، ومحامي يراجعها.
- 🟢 **«غير كده» و«حاجة تانية» بقوا يتكتبوا — اتلزق** (`WORK_MIGRATION_15` / `0094`). الاتنين كانوا طريق مسدود: العضو يختار و«خلاص»، والمعلومة تضيع. دلوقتي بتظهر خانة نص:
  - `/new` المنطقة → `sbotat.area_label_ar` (العمود كان موجود و`fn_create_sbota` مكانتش بتملاه أبدًا). و**القاعدة بترفض** «غير كده» من غير اسم، مش الفورم بس.
  - `/join` «بتشتغل إيه؟» → عمود جديد `profiles.work_status_other`، وبيتمسح لوحده لو غيّر اختياره.
  - ⚠ `fn_create_sbota` بقت **١١ parameter**. إضافة parameter بـ`default` بتعمل دالة **تانية** مش بتعدّل القديمة — والنداء بيبقى ambiguous. فلازم `drop function` بالتوقيع القديم بالظبط الأول.
- ✅ **المحتوى اتكتب** (على `CONTENT_BRIEF.md`) وواقع في `0084`: ١٧ سؤال · ٦ فقرات «مين إحنا» · ١٢ فقرة شروط · ٢١ قالب رسايل · ٨ رسايل قفل. و«كابتن في كل سبوطة» اتشال من الستيكر والشريط والاقتباس المخترع.
- 🟢 **العنوان بيوصل للدافع تاني · ورسالة واتساب جاهزة — اتلزق (٢٠٢٦-٠٩-١٣).**
  `WORK_MIGRATION_19.sql` / `0098` — تلاتة من نفس الجذر (شوف درس تلتاشر):
  - `fn_sbota_address` بقت تقرا `sbotat.venue_name_ar`/`address_ar` لما
    `venue_id` فاضي. الحارس زي ما هو بالحرف: **حجز مدفوع**، والزائر المجهول
    واللي لسه مستني الدفع ما بياخدوش حاجة (`test_address_back()` بيثبت
    التلاتة سلوكيًا، واتجرّب بفخين متعمّدين).
  - `booking_confirmed` بقى فيه رابط الحجز `{{4}}` → `/my/<رقم الحجز>`.
  - قالب جديد `whatsapp_booking` — اللوحة بتفتح بيه واتساب الحاجز والرسالة
    مكتوبة (اسمه · السبوطة · الميعاد · المكان · رابط حجزه). بيتعدّل من
    `/admin/notifications`، وترتيب متغيّراته في `src/lib/wa-message.ts`،
    و`check-notify-vars.mjs` بيقارن الاتنين ويفشل البناء لو اختلفوا.
  - الزرار في «الحجوزات» جنب كل حاجز، وفي «الفلوس ← التحويلات المستنية»
    جنب «أكّد التحويل» بالظبط. **مش بيبعت** — بيفتح المحادثة والمالك بيدوس.
- 🟢 **الأمان بقى ليه صفحة، والاطمئنان بقى ليه رابط — اتلزق على الإنتاج (٢٠٢٦-٠٩-٢١).**
  `WORK_MIGRATION_25A.sql` (`0104`) ثم `25B` (`0105`):
  - **`/aman` «إزاي بنأمّنك»** — صفحة محتوى زي `/rules` بالظبط (في `content_pages`،
    بتتعدّل من `/admin/content`). كل فقرة فيها بتوصف حارس **شغّال في القاعدة
    من زمان ومحدش قايله**. رابطها بيبان في الذيل لوحده، وفيه رابط تاني تحت
    صندوق الضمان في `/s/[slug]` و`/s/[slug]/pay` — اللحظة اللي السؤال بيتسأل فيها.
  - **`/tamenny/[token]` «معايا حد يعرف»** — العضو بيعمل رابط من صفحة حجزه
    ويبعته لأمه. اللي معاه الرابط بيشوف: الاسم الأول · الخروجة · الميعاد ·
    المكان · ووصل ولا لسه. **وبس.** التوكن ١٦ بايت عشوائي، بينتهي لوحده بعد
    الخروجة بساعات من `settings.safety_link_hours`، والعضو يقدر يقفله.
    و**اللي معاه الرابط قارئ فقط** — «وصلت بالسلامة» بيتدوس من العضو بس.
  - السن بقى بيتقرا من `settings` — كان مكتوب في جسم `fn_can_book` (18 و21)،
    والمالك بيغيّره من اللوحة ومحدش بيسمعه. و`unwired` اتشال من الخانتين.
  - رقم الطوارئ الوهمي كان لسه حي في **صفحتين**: `/my/[bookingId]` مكتوب
    `tel:+201000000000` بالحرف، و`/captain/[sbotaId]` بياخده من
    `src/data/lists.ts`. التصليح القديم (A15) لمس `/rules` بس. بقى مكوّن واحد
    `EmergencyCall` بيقرا من `settings` وبيخفي نفسه لو الرقم وهمي.
  - `test_safety_page()` (٦ صفوف) و`test_safety_link()` (١٤ صف) — كلهم نجح،
    واتجرّبوا بـ**تسع فخاخ متعمّدة** (باب كتابة على الجدول · الرابط مفتوح
    للمجهول · العنوان من غير حارس · «وصلت» من غير ملكية · التوكن بيتغيّر كل
    مرة · `fn_safety_my` من غير حارس · السن مكتوب في الكود × ٢ · الصفحة فاضية).

- 🟢 **الوصول وأول ربع ساعة — اتلزقوا على الإنتاج (٢٠٢٦-٠٩-٢١).**
  `WORK_MIGRATION_26A.sql` (`0106`) ثم `26B` (`0107`):
  - **«جايين منين»** — `fn_who_booked` بقى فيها `same_area` («منهم ٣ من
    ناحيتك» قبل الدفع)، و`fn_group_members` بقى فيها `area_code`/`area_label`/
    `same_area` (بعد الكشف). «غير كده» بترجّع **null** مش رقم، وnull و0 مش
    نفس الحاجة. المقارنة في القاعدة مش في الواجهة.
  - **«أول ربع ساعة»** — عمود `sbotat.sign_ar` (العلامة) + `fn_sbota_arrival`
    (العلامة + أقدم حجز في **نفس المجموعة** هو اللي بيستقبل) + تلات أسئلة
    في `copy_strings`. الكرت بيفتح مع الكشف بنفس الحارس بالظبط.
  - `fn_guard_sbota_text` مشي ورا العمود الجديد — من غير كده الكلمات
    الممنوعة كانت هتعدّي من خانة العلامة.
  - و`fn_my_hosted_sbotat` كانت بتعرض **اسم القالب** بدل العنوان اللي العضو
    كتبه (كل خروجات الأعضاء على قالب عام واحد) — بقت `coalesce` زي
    `sbotat_public`.
  - `test_ride_areas()` (٩) و`test_arrival()` (١١) — كلهم نجح، واتجرّبوا
    بـ**تمن فخاخ**: العدّ اللي بيعدّني أنا · «غير كده» بتتعدّ · حارس الكشف
    مشيل · الحارس نسي العمود الجديد · العلامة من غير ملكية · الكرت من غير
    كشف · الكرت من غير حجز · «خروجاتي» راجعة لاسم القالب.

- ⚠ **اللزق نفسه كان درس (٢٠٢٦-٠٩-٢١).** المالك لزق الأربع ملفات من
  الشات، و**كل واحد اتقطع في مكان مختلف**، فالقاعدة قعدت في حالة مخلوطة:
  صفحة `aman` موجودة بفقراتها بس `fn_can_book` ما اتحدّثتش · `fn_sbota_place`
  موجودة بس **جدول `safety_links` كله مش موجود** · `fn_who_booked` اتحدّثت
  و`0107` ولا حاجة. والمحرر بيشتغل كمعاملة واحدة، فالمفروض ده ما يحصلش —
  بس القص جه على حدود جُمَل، فاللي وصل اتنفّذ واتحفظ.
  **اتصلّح بإعادة لزق الأربعة كلهم بالترتيب** — وده اشتغل بالظبط لأن كل
  هجرة آمنة تتكرر (`if not exists` على صفحة `aman` خلّت الفقرات ما تتكررش،
  و`ref='tamenny'` منعت تكرار الفقرة التاسعة). الفحص الشامل بعدها: **صفر فشل**.
  الدرسين السبعتاشر والتمنتاشر اتكتبوا من اليوم ده.

- 🟢 **العلامة بتوصل وهو نازل — اتلزق (٢٠٢٦-٠٩-٢١).**
  `WORK_MIGRATION_31.sql` / `0112`:
  - `sbotat.sign_ar` كانت بتظهر في كرت «أول ربع ساعة» بعد الكشف بس، والعضو
    ساعتها قافل الموقع. بقت في **تذكير الـ3 ساعات** كمان (`{{3}}`).
  - ⚠ العلامة **اختيارية**، فمحتاجة قاعدة في الرندر: **السطر اللي كل
    متغيّراته فضيت بيتشال من الرسالة**. من غيرها كان هيوصل «تعرفهم من: »
    ناقص — نفس باج «وصلنا تحويلك لـ .» القديم. والقاعدة محدّدة عن قصد:
    سطر نص متغيّراته موجود **بيفضل** (`{{2}} — {{3}}.` مع `{{3}}` فاضية
    بتطلّع «بادل.»). وفيه `OPTIONAL_VARS` في `notify.ts` علشان المتغيّر
    الاختياري ما يتسجّلش «نقص» في اللوج.
  - `test_signs()` بيفشل في حالة **واحدة**: سبوطة اتكشفت ولسه شغّالة
    ومفيهاش علامة. والسبوطات الجاية من غير علامة بتتقال **معلومة** مش فشل
    (الدرس العاشر). وسبوطة البذرة المؤقتة مستثناة **بالـslug**.
  - وفي `/admin/sbotat` وسم برتقالي **«مفيش علامة»** جنب أي سبوطة شغّالة
    من غيرها — التنبيه في المكان اللي بيتصلّح منه.
  - ⚠ «امتد `check:content`» مكانتش تنفع: فاحصات البناء **ما بتوصلش
    للقاعدة**، وحالة السبوطات بيانات حيّة. فالفحص اتحط في القاعدة
    (`test_signs`) وفي اللوحة.

- 🟢 **طقس الكشف — اتلزق (٢٠٢٦-٠٩-٢١).** `WORK_MIGRATION_30.sql` / `0111`:
  - **إيميل تأكيد الحجز كان لسه بيقول «الساعة 8 بالليل» بالحرف** — `0109`
    وصّل الساعة باللوحة وصلّح الموقع، والقالب فضل كاتب الرقم في جسمه.
    الدرس التلتاشر تاني: مصدر اتغيّر، وحد تاني بيقرا منه ما مشيش وراه.
    بقى `{{5}}` من `reveal_at` بتاع **السبوطة نفسها** — مش ساعة عامة.
  - **رسالة الكشف بقى ليها افتتاحية ثابتة** («يا فلان، مجموعتك ظهرت.»)
    وبقت تودّي على `/my/<الحجز>` بدل `/me` وبدل الشات. الشات مكان الكلام،
    والمجموعة والعلامة في صفحة الحجز.
  - وصفحة الحجز بقت تحط كرت **«أول ربع ساعة» فوق قايمة الأسامي** — أول
    سؤال في دماغ العضو لحظة الكشف مش «مين معايا»، ده «هعرفهم إزاي».
  - `getArrival` كانت بترجّع `null` من غير قاعدة، فالكرت **عمره ما ظهر في
    التطوير**. بقى ليه بيانات عرض زي باقي المكوّنات.
  - ⚠ تعديلات القوالب في الهجرة لازم تتكتب **نص واحد بين علامتي تنصيص
    بأسطر حقيقية** — مش `||chr(10)||`. `check-notify-vars.mjs` بيقرا
    الشكل ده بالظبط؛ بالتلزيق بيحكم على النص القديم ويقول «تمام» على غلط.
  - `test_reveal_ritual()` ٥ صفوف، واتجرّب بفخ (رجّعنا القالبين القدام →
    أربع صفوف وقعوا).

- 🔴🟢 **ثغرة: الأسامي كانت بتطلع لحجز لسه مش مدفوع — اتقفلت (٢٠٢٦-٠٩-٢١).**
  `WORK_MIGRATION_29.sql` / `0110`. `fn_group_members` كانت بتتأكد إن
  الحجز بتاعي وإن الكشف عدّى — **وما بتسألش عن حالة حجزي أنا**. يعني حد
  يحجز، يرفع أي صورة على إنها تحويل (الحجز بيفضل `pending_payment`)،
  يستنى الكشف، وينادي الدالة → الأسامي والأنواع والمناطق كلها بترجعله.
  والدالة `security definer`، يعني RLS مش بتنقذنا.
  - الواجهة كانت بتخبّي القايمة (الدرس التناشر) — بس ده **إخفاء زرار**،
    ونداء مباشر للـRPC كان بيعدّي. القاعدة §٣.٣ بالحرف.
  - التصليح على قاعدة الدرس الستاشر: الدالة بقت تنادي **نفس** الحارس
    (`fn_can_see_place`) اللي بينادوه `fn_sbota_address` و`fn_sbota_arrival`
    و`fn_safety_view` — مش نسخة منه.
  - `test_group_guard()` (٥ صفوف) بيثبت الأربع حالات سلوكيًا، واتجرّب
    **بفخ**: رجّعنا دالة `0106` القديمة فالصف الأحمر وقع لوحده
    («شاف 2 اسم وهو لسه مدفعش»). لازم `fn_test_seed_up()` قبله.
  - وصفحة `/my/[id]` بقت تعرف **خمس حالات** بدل حالتين: اتلغى · في
    الانتظار · مستني التأكيد · اتأكد وقبل الكشف · بعد الكشف. قبل كده
    التلاتة الأولانيين كانوا بيشوفوا «هتعرف مجموعتك…» وعدّاد تنازلي —
    يعني الموقع بيعد تنازلي لحجز مش موجود، وبيطمّن حد مكانه مش مضمون.
    والشات وبطاقة الكابتن وقسم المكان و«انقلني لبنات بس» كلهم بقوا
    بيختفوا في الحالات اللي مالهمش فيها معنى.
  - و`bookingFromDb` بقت `coalesce(title_ar, name_ar)` زي `sbotat_public`
    بالظبط — من غيرها خروجة العضو كانت هتظهر في «حجزي» و«حجوزاتي» باسم
    القالب العام. وأعمدة الحجز بقت ثابت واحد (`BOOKING_COLS`) بدل نصين
    متطابقين مكتوبين بالإيد (الدرس الخمستاشر).

- 🟢 **ساعة الكشف وساعات الشات بقت من اللوحة — اتلزق (٢٠٢٦-٠٩-٢١).**
  `WORK_MIGRATION_28.sql` / `0109`. تلات أرقام في `/admin/settings` ←
  «الساعات والمواعيد» كانوا **ديكور** بوسم `unwired`: ساعة الكشف · الشات
  بيفتح قبل السبوطة بـ · الشات بيقفل بعدها بـ. `fn_sbota_timings` كانت
  بتحسب بـ`interval '20 hours'` و`'48 hours'` **مكتوبين في جسمها** من
  `0003` — نفس باج السن اللي اتصلّح في `0104` بالظبط.
  - 🔴 **الشات ما بيفتحش قبل الكشف مهما كان الرقم**:
    `chat_opens_at = greatest(reveal_at, starts_at - chat_open_hours)`.
    الرقم بيأخّر بس — لأن ده شات المجموعة، وفتحه بدري = كشف الأسامي بدري.
    بالقيمة الافتراضية (48) النتيجة `reveal_at` بالظبط زي الأول.
  - وسطر «هتعرف مجموعتك … الساعة 8 بالليل» كان **مكتوب في الكود**
    (`map-db.ts`) وفيه رقم ثابت، وصفحة `/my/[id]` كانت بتقص آخره بـ
    `.replace(t('group.label.2'), '')` علشان تعمل منه عنوان. بقى
    `<RevealLine />`: مفتاحين نص (`group.reveal.line`/`.short`) + أربع
    كلمات للوقت (`shared.hour.*`) ورقم واحد من `settings`.
  - `test_reveal_hour()` ٦ صفوف على الإنتاج، كلهم «نجح»، واتجرّبوا بفخين
    (شيل `greatest` → الصف الأحمر بيقع · رجّع الدالة القديمة → ٥ صفوف تقع).
  - اللي **لسه** مكتوب في الكود بالقصد: قفل الحجز (٣٦ ساعة) والكشف «قبلها
    بيوم» (٢٤ ساعة) — **مالهمش خانة في اللوحة أصلًا**، فمحدش بيتكدب عليه.
    لو المالك عايزهم من اللوحة، دي أعمدة جديدة في `settings` وقرار لوحده.

- 🟢 **الأقسام الفاضية بقى يتقفلوا من اللوحة — اتلزق (٢٠٢٦-٠٩-٢١).**
  `WORK_MIGRATION_27.sql` / `0108` — مفتاح ميزة `captains` (كان الوحيد من
  غير مفتاح؛ `work_sbota` و`mystery` مفاتيحهم من `0036`). الهجرة **ما
  بتقفلش حاجة** — بتدّي المالك الزرار بس، والقفل من `/admin/settings` ←
  مفاتيح المزايا.
  - القفل بيخفي **الصفحة والمدخل** مع بعض: القايمة والذيل بقوا مصفوفة
    واحدة كل عنصر فيها شايل مفتاحه (`NAV` في `Header.tsx` · `EXTRA` في
    `Footer.tsx`)، وسبع مداخل تانية اتوصّلوا — شريط «الشغل» · قسم
    «الكباتن» · دبوس اللابتوب وزرار «افتح الخريطة» في الخريطة الصغيرة ·
    كرت «الغامضة» في الخريطة الكبيرة · رابط الكباتن في لوحة صاحب الخروجة ·
    وصفحة «شغلي» (بقى لها `layout.tsx` ورا `work_sbota`، كانت بره القفل).
  - الحارس `scripts/check-nav-flags.mjs` (جوه `npm run verify`) بيفشل
    البناء لو أي ملف فيه رابط لصفحة ورا مفتاح وهو مش بيقرا نفس المفتاح.
    اتجرّب بأربع فخاخ وقت كتابته + فخين بعد ما اتوسّع — كلهم اتمسكوا.
  - `test_section_flags()` على الإنتاج: ٣ صفوف، كلهم «نجح».

- 🔴 **مراجعة شاملة عاملة `REVIEW.md`** (+ 4 ملفات تفصيلية) لقت 150 نتيجة، 34 حمرا. اقرأها قبل ما تكمّل أي حاجة.
- 🟢 **الدفعة الأولى اتلزقت على القاعدة** (`WORK_MIGRATION_5.sql`) — أحمر الأمان والفلوس والقاعدة كله واقع.
- 🟢 **الدفعة التانية اتلزقت**: `WORK_MIGRATION_6.sql` (`0065` قوايم التسجيل · `0066` كتل الخريطة · `0067` اختبارها · `0070` تخمين رمز الدخول S7 · `0071` حجم الصفحة ومجاميع الفلوس · `0072` الصيانة بتوقف الحجز · `0073` صلاحيات رفع الصور · `0074` افتراضات التسعير).
- 🟢 **الدفعة التالتة اتلزقت**: `WORK_MIGRATION_7.sql` (`0075` بحث نشاطات المهارة + رجوع «عجل» + حارس الـenum · `0076` `test_last_review_items()` · `0077` نصوص شاشة «مقفول»). **الترتيب: ٥ ← ٦ ← ٧** لأن `0075` بيعمل `create or replace` لـ`test_public_lists()` بتاعة `0067`.
- ⚠ **قبل توزيع اللينك:** (١) القاعدة خلصت — الزق `CHECK_DB.sql` للتأكد، المفروض «مفيش ولا فشل». (٢) **رقم فودافون كاش/إنستاباي في `settings` لسه وهمي، والدفع متوقف عن قصد لحد ما تحطه.** `src/lib/server/pay-guard.ts` بيمسك القيم الافتراضية والفاضي وأي رقم كله أصفار، ومسارات `/api/pay/create` و`/api/pay/pass` بترفض بـ503 **قبل** ما يتعمل حجز، و`/admin/settings` بيعرض تحذير أحمر على الخانة. يعني مفيش عضو هيبعت فلوسه لحد تاني — بس مفيش دفع خالص كمان. (٣) `CRON_SECRET` اتعرض في محادثة — يتغيّر (وهو كمان الـpepper الاحتياطي لهاشات OTP والأدمن).
- ✅ **نصوص القواعد والضمان اتنقلت للقاعدة** — كانت في `src/data/lists.ts` (نتيجة U+A مفتوحة). وبما إن نفس النص بيظهر في صفحة الحجز والدفع وسبوطة الشغل والغامضة كمان، الستة كلهم بقوا يقروا من نفس المصدر عبر `useContentRef`/`useNumberedRules` — علشان تعديل من اللوحة ما يسيبش نص قديم واقف في نص الموقع.
- ✅ **باقي المراجعة اتقفل (كله في الكود، شغّال من غير لزق):**
  - `A2` مفاتيح المزايا بقت بتتقرا فعلًا: `src/lib/flags.ts` (نفس نمط `copy.ts`، وسم `flags` **و** `copy` علشان `revalidateSite()` تبطّلها كمان) → `FlagsProvider` في `layout.tsx` → `<FeatureGate flag="…">` على `/game` · `/game/result` · `/map` · `/s/mystery` · `/shoghl/*` (من الـlayout) · الشاتين · `/s/[slug]/pay`، و`useFlag()` لكرت الإحالة في `/me` وزرار الحجز في `/s/[slug]`. الاحتياطي في `src/data/flags-fallback.ts` **مفتوح دايمًا**.
  - `maintenance.allow_roles` بقى ليها معنى: `src/middleware.ts` بيتأكد من جلسة سوبابيس + صف `admin_users` نشط + الدور جوه `allow_roles` (والقراية بتوكن العضو، يعني RLS هي اللي بتقرر). قبل كده أي حد يكتب كوكي `nb_admin` كان بيعدّي. `/admin/*` سايبينه مفتوح وقت الصيانة عن قصد — محمي أصلًا بـTOTP و`requirePermission`، وقفله كان هيخلق خطر قفل على المالك.
  - `/admin/map` فيه قسم «كتل الخريطة العامة» بيعدّل `map_areas` (الاسم · الملاحظة · «بعيدة» · الظهور) بحارس `rejected()`. الهندسة (x/y/w/h/r/lx/ly) **مش** بتتعدّل من اللوحة عن قصد.
  - `activityToDb` بقى بحث حقيقي من `skill_activities` (`setActivityMap` في `map-db.ts` + `loadActivityMap` في `api.ts`)، والمجهول بيرجّع `null` وبيتخطّى بدل ما يتحفظ «سباحة».
  - ترقيم من القاعدة (`.range()` + `{ count: 'exact' }`) في `/admin/matching` · `captains` · `templates` · `shoghl`، والعدّادات فوق الجداول بقت `head: true` counts.

### التأكد إيه اللي اتلزق فعلًا

**الفحص الكامل: الزق `CHECK_DB.sql` في SQL Editor.** بيشغّل كل دوال
الاختبار الموجودة (بيلاقيها لوحده) ويطلّع اللي فشل بس. لو رجع «مفيش ولا
فشل» يبقى القاعدة تمام.

⚠ فيه حاجتين اتعلمناهم وإحنا بنكتبه، ومكتوبين جواه:
1. **لازم ترجّع الدور والهوية بين كل دالة والتانية.** الاختبارات السلوكية
   بتلبس `anon` و`authenticated`، ومن غير الترجيع الدالة اللي بعدها بتشتغل
   بهوية غلط وتقول «فشل» وهي سليمة. (حصل فعلًا مع `test_rls`
   و`test_work_rls` — الاتنين بينجحوا لوحدهم.)
2. **«فشل» بس هي اللي تتحسب، مش «مش نجح».** فيه صفوف معلوماتية زي «الموقع
   شغّال» في `test_maintenance_blocks` — لا نجاح ولا فشل.

وأي فاحص جديد اختبره بفشل متعمّد قبل ما تصدّقه: فاحص بيقول «تمام» على طول
أسوأ من مفيش فاحص.

⚠ **درس تالت — هجرتين بيكتبوا على نفس الصف = قنبلة موقوتة.** أول تشغيل
للفاحص على الإنتاج لقى `cycling` مقفولة: `0065` بتقفلها (حل مؤقت قديم)
و`0075` بترجّعها، واللي يتلزق **آخر** يكسب. فلزق الدفعة ٦ بعد ٧ — أو
تكرارها بعدها — بيرجّع باج اتصلّح، **بالصمت**. اتصلّحت في `0083`، و`0065`
نفسها اتحصّنت (بقت ما تقفلش لو حارس `0075` موجود) فالترتيب بطل يفرق.

**القاعدة:** لو هجرة جديدة بتلغي أثر هجرة قديمة، متكتفيش بإنك تكتب فوقها —
**ارجع للقديمة وخلّيها مشروطة**. ساعتها بس يبقى «آمن يتكرر» صحيح فعلًا.

⚠ **درس سادس — التصليح اللي ما وصلش للملف اللي بيتلزق = تصليح مش موجود.**
الدرس التالت فوق كان «اتصلّح»: `0065` اتحصّنت و`0067` بقت مشروطة. بس المالك
**ما بيلزقش ملفات الهجرة** — بيلزق `WORK_MIGRATION_6.sql`، والحزمة دي
ما اتجدّدتش. فالسطر القديم غير المحصّن فضل فيها، والباج فضل حي على الإنتاج
وإحنا كاتبين في الملف ده إنه اتقفل. اكتشفناه بالصدفة وإحنا بنجرّب حاجة تانية.

**القاعدة:** أي تعديل في `supabase/migrations/*.sql` **لازم** يتجدّد في حزمته.
الحارس `scripts/check-migration-bundles.mjs` (جوه `npm run verify`) بيقارن كل
قسم في كل حزمة بملف الهجرة بتاعه حرف بحرف ويفشل البناء لو اختلفوا — ومش بيسمح
بحزمة جديدة من غير ترويسة `-- # <اسم الملف>.sql` لكل قسم.

⚠ **درس سابع — المسح بالبادئة بيمسح اللي ما عملتهوش.** `fn_test_seed_down`
(بذرة الاختبار المؤقتة) كانت بتمسح `profiles where id::text like '33333333-%'`
على افتراض إن البادئة دي «محجوزة للاختبار». **هي مش محجوزة** — دي بادئة بذرة
الأعضاء الأصلية (`0013`)، وفيها صف كان عليه **حساب أدمن نشط** (`...0099`).
فالدالة مسحت حساب لوحة، رغم إن `0089` حاطة شرط صريح إنها ما تمسحش أي صف عليه
`admin_users`. ده حصل على الإنتاج فعلًا (٢٠٢٦-٠٩-١٢).

**القاعدة:** امسح **بالأرقام اللي أنشأتها بالظبط** مش بالبادئة، وحط حارس صريح
على اللي ممنوع يتمسح مهما حصل (`and not exists (select 1 from admin_users …)`).
الحارس: `test_seed_down_safe()`.

⚠ **درس تامن — `prosrc` فيها التعليقات كمان.** أول نسخة من `test_seed_down_safe`
كانت بتدوّر على `'%like ''33333333%'` في كود الدالة — وخبطت في **التعليق** اللي
جوه الدالة نفسها اللي بيشرح الباج، فقالت «فشل» على دالة سليمة. أي فحص على
`prosrc` لازم يبقى محدد كفاية إنه يمسك الكود مش الكلام (`id::text like` مش
`like`). وجرّبه بالغلط المتعمّد **وبالنسخة الصح** — الاتنين، مش واحد.

⚠ **درس عاشر — الفاحص اللي بيقول «مفيش غير اللي حطيته» بيبقى أحمر أول ما
المالك يشتغل.** حصلت **تلات مرات** في يوم واحد: عدّينا السبوطات فبقى أحمر أول
ما اتفتحت مسوّدات حقيقية · عدّينا القوالب «٨٥ بالظبط» فبقى أحمر أول ما رجعت
قوالب نسبوط · دوّرنا على القالب باسمه «بادل مبتدئين» فبقى أحمر أول ما المالك
غيّر الاسم لـ«بادل» · وعدّينا الحجوزات كـ«بيانات عرض» فبقى أحمر أول ما عمل
حجز تجربة.

**القاعدتين:**
1. الفاحص يتأكد إن **اللي حطيناه لسه موجود وكامل** — مش إنه الوحيد. صف زيادة
   مش عطل، ده المالك بيشتغل.
2. أي فحص على حاجة المالك بيغيّرها من اللوحة (الاسم · السعر · العدد) لازم
   يبقى على **المفتاح الثابت** (`slug` · `id`) مش على اللي بيتعرض.

⚠ **درس حداشر — العمود اللي اتعمل لسياق، يتنقل لسياق تاني ويتحوّل لتسريب.**
`sbotat.venue_name_ar` اتعمل في `0086` لخروجة العضو، والفيو العام بيطلّعه —
وده مقصود هناك (الحجز ببلاش والعنوان بس هو اللي بيستنى الكشف). وبعدين اتشالت
قايمة الأماكن من `/admin/sbotat` وبقى اسم مكان **سبوطة نسبوط** يتكتب في نفس
العمود. فاسم المكان بقى مكشوف لأي زائر، وهو قبل كده كان مخفي لحد ما العضو
يدفع (كان جاي من `venues` عبر `fn_sbota_address`). اتصلّح في `0097` بـ
`case when origin = 'member'`. **لما تعيد استعمال عمود، اسأل: الفيو العام
بيطلّعه؟ وهل ده صح في السياق الجديد؟**

⚠ **درس اتناشر — الحارس في القاعدة مش بيغني عن حارس في الواجهة.**
`getGroup` كانت بتحسب «اتكشف؟» من الساعة بس (`Date.now() >= revealAt`) من
غير ما تبص على حالة الحجز. فحد يحجز، يرفع أي صورة على إنها تحويل (الحجز
بيفضل `pending_payment`)، ويستنى ميعاد الكشف — وصفحة `/my/<id>` تتصرّف كأنه
داخل. القاعدة كانت بتمنع الأسامي والعنوان فعلًا، بس الواجهة كانت بتوري اسم
المكان وتفتح أقسام مالهوش حق فيها. الشرط بقى `booking.paid && …`.

⚠ **درس تلتاشر — العمود اتنقل والدالة ما مشيتش وراه.** درس حداشر قال إن
`venue_name_ar` اتنقل لسياق جديد وبقى تسريب. الناحية التانية من نفس النقلة
فضلت مفتوحة شهر: `venue_id` بقى **null** في كل سبوطات نسبوط (المالك بيكتب
المكان بإيده)، و`fn_sbota_address` فضلت بتعمل `join` على `venues` بالـ
`venue_id`. يعني حد يدفع فعلًا، والدالة تلف على جدول الأماكن، ما تلاقيش صف،
وترجّع **ولا صف** — والدافع مش شايف العنوان خالص. **ومفيش حاجة بتفشل**:
لا خطأ ولا لوج، بس شاشة فاضية. ونفس الجذر ضرب تلات أماكن تانية في نفس
الوقت: تذكير الـ24 ساعة كان بيقول «المكان: .»، وإيميل التأكيد كان بيقول اسم
القالب مش عنوان السبوطة اللي المالك كتبه، وصفحة الفلوس كمان.
**القاعدة:** لما تشيل مصدر بيانات من الواجهة، دوّر على **كل** اللي بيقرا
منه — `grep` على اسم العمود القديم في القاعدة والكود — مش على الصفحة اللي
انت فيها بس. الحارس: `test_address_back()`.

⚠ **درس ستاشر — الحارس والبيانات في دالة واحدة = نسخة تانية من الحارس.**
`fn_sbota_address` كانت بتعمل حاجتين: تتأكد إن اللي بيسأل من حقه، وتجيب
العنوان. ماشي — لحد ما احتجنا **نفس العنوان** من مكان تاني (رابط الاطمئنان،
واللي بيقرا بالتوكن مش بالجلسة). ساعتها مفيش اختيار غير إننا نكتب المنطق
تاني، ودي بالظبط اللي بتولّد الدرس التلتاشر: نسختين من نفس الحارس، واحدة
منهم بتتنسى أول ما حاجة تتغيّر.

اتقسمت لتلاتة في `0105`: **`fn_sbota_place`** (البيانات، من غير أي حارس،
ومقفولة على الكل) · **`fn_can_see_place`** (الحارس لوحده) · و`fn_sbota_address`
بقت غلاف رفيع بينادي التنتين. `fn_safety_view` بتنادي **نفس** الحارس، فمستحيل
الاتنين يفترقوا. و`test_address_back()` القديمة لسه بتعدّي بالخمسة — ده الدليل
إن الحارس ما اتغيّرش.

**القاعدة:** أول ما تلاقي نفسك بتنسخ حارس علشان تخدم سياق تاني — اوقف،
واطلع الحارس في دالة لوحده.

⚠ **درس تمنتاشر — الشيم المحلي اللي مش شبه الإنتاج بيكدب عليك بثقة.**
`fn_safety_link` بتستعمل `gen_random_bytes` (من `pgcrypto`) وكانت
`set search_path = public`. الاختبار المحلي عدّى **١٤ من ١٤**، وأول تشغيل
على الإنتاج وقع:
`function gen_random_bytes(integer) does not exist`.

السبب: **سوبابيس بيركّب الامتدادات في اسكيما `extensions` مش `public`**.
والشيم كان سايب `0001` يعمل `create extension if not exists pgcrypto`،
فبتتركّب في `public` محليًا — والدالة بتلاقيها عادي. على الإنتاج الامتداد
متركّب أصلًا في `extensions`، فالسطر ده no-op والدالة ما بتلاقيش حاجة.

**اتصلّح في المكانين:**
1. `scripts/pg-shim.sql` بقى بيركّب `pgcrypto` و`uuid-ossp` في `extensions`
   **قبل** الهجرات — فالشيم بقى بيعيد إنتاج الغلطة دي محليًا. اتجرّب بفخ:
   رجّعنا `search_path = public` على الشيم الجديد والاختبار طلّع نفس
   الاستثناء بالحرف.
2. أي دالة بتستعمل `pgcrypto` لازم يبقى `set search_path = public, extensions`
   — الاتنين، علشان تشتغل في البيئتين.

**القاعدة:** الفرق بين المحلي والإنتاج **مش في البيانات بس** — في الأدوار
والاسكيمات والامتدادات كمان. أي فرق منهم = فاحص بيقول «تمام» وهو مش شايف.
ولما تكتشف فرق، صلّح **الشيم** مش الدالة بس.

⚠ **درس تسعتاشر — القفل اللي بيخفي القايمة بس، بيسيب المدخل في نص الصفحة.**
`/shoghl` و`/s/mystery` و`/captains` مبنيين وصفر بيانات، فالمفروض المالك
يقفلهم من اللوحة لحد ما يبقى فيهم حاجة. وصّلنا الأقفال بالقايمة والذيل،
وكتبنا حارس (`scripts/check-nav-flags.mjs`) بيتأكد إن كل رابط في الملفين
ورا نفس مفتاح صفحته — والحارس قال «سليم».

وبعدين **قفلنا المفاتيح بعينينا وصوّرنا الصفحة**. الرابط اختفى من القايمة
والذيل فعلًا، و**الرئيسية فضلت فيها تلات مداخل** لنفس الأقسام المقفولة:
شريط «الشغل» بزرار «خد يومك» · دبوس اللابتوب في الخريطة الصغيرة · وقسم
«الكباتن» كامل. وبعد ما وسّعنا الحارس طلع كمان أربعة: كرت «الغامضة» في
الخريطة الكبيرة · زرار «افتح الخريطة» · رابط الكباتن في لوحة صاحب الخروجة ·
وصفحة «شغلي» كلها (كانت **بره** `/shoghl/` فالـlayout مش بيغطيها).

**القاعدتين:**
1. الحارس اللي بيفحص ملفين اسمه بيقول «القايمة والذيل» — والقاعدة الحقيقية
   **«أي مدخل في الموقع كله»**. أي ملف فيه رابط لصفحة ورا مفتاح لازم يقرا
   نفس المفتاح (`useFlag('…')` أو `<FeatureGate flag="…">`)، والاستثناء
   الوحيد صفحة جوه القسم المقفول نفسه.
2. **اقفل المفتاح وصوّر.** القفل اللي ما اتجرّبش مقفول، زيّه زي الفاحص اللي
   ما اتجرّبش بفخ. «الحارس بيعدّي» مش «القفل بيشتغل».

⚠ **درس سبعتاشر — الحزمة الكبيرة بتوصل مقصوصة، والخطأ بيشاور على حتة تانية.**
حصلت **مرتين**. المالك لزق `WORK_MIGRATION_25.sql` (١٠٢١ سطر · ٥٣ كيلو) فوصل
منها **١٠٠ سطر بس** (٧ كيلو)، والمحرر قال:
`unterminated dollar-quoted string ... LINE 67: do $$`.

والرسالة دي **مضللة**: هي بتشاور على `do $$` في سطر ٦٧ كأن فيه غلطة في
الصياغة هناك، والحقيقة إن الملف اتقطع قبل ما الـ`$$` تتقفل. أول مرة قعدنا
ندوّر على غلطة صياغة مش موجودة.

**إزاي تعرف الفرق بسرعة:** شوف آخر سطر في المحرر. لو الملف منتهي في نص
جملة — ده قص، مش باج. وقارن عدد السطور: `wc -l` على الملف ضد آخر رقم سطر
في المحرر.

**القاعدتين:**
1. **ملف واحد = هجرة واحدة.** الحزم اللي فيها أكتر من هجرة بتكبر وبتتقص.
   لو لازم تتقسم، سمّيها `NA`/`NB` وخلّي كل واحدة تشتغل لوحدها بالترتيب
   (زي `14A`/`14B`).
2. **متقولش للمالك «انسخ من الشات».** ابعته **رابط الملف على جيتهب** وقوله
   يستعمل زرار «Copy raw file» — ده بينسخ الملف كامل، والمعاينة في الشات
   بتعرض أول شوية سطور بس.

⚠ **درس أربعتاشر — الرابط اللي في الإيميل محدش بيدوس عليه غيره هو.**
زرار «افتح نسبوط» في إيميل تأكيد الحجز كان رايح `/sbota/<slug>` — **مسار
مش موجود في الموقع من أصله** (المسارات `/s/<slug>` و`/my/<رقم>`). وصفحة
«تمام مكانك محجوز» كانت بتوصّل لـ`/my/b1` — رقم حجز من **بيانات العرض**
اتمسحت من زمان. الاتنين عدّوا من `tsc` ومن `next build` ومن المراجعة، لأن
مفيش حاجة في الكود بتتأكد إن الرابط ليه صفحة. أول حاجز حقيقي بيدفع كان
هيقع على 404 مرتين.
**القاعدة:** أي رابط بيتبعت لعضو (إيميل · واتساب · صفحة تأكيد) لازم
يتجرّب بالفتح، ورقمه يجي من البيانات الحقيقية مش من ثابت في `src/data`.

⚠ **درس خمستاشر — فاصلة واحدة مكتوبة بالإيد قفلت صفحة الفلوس كلها.**
`paySelect` في `/admin/payments` كان بيلزق قطع نص الـselect بـ`.join('')`
وكل قطعة كاتبة فاصلتها في آخرها. واحدة نسيت، فالنص طلع
`…reviewed_at,created_atbookings!payments_booking_id_fkey(` — وبوستجرست
رفض الطلب كله. يعني **التبويبات التلاتة في صفحة الفلوس كانت بتقول
«مقدرناش نجيب التحويلات» من أول يوم**، والمالك اكتشفها لما راح يعتمد أول
تحويل حقيقي.

ومفيش حاجة كانت بتمسكها: النص بيتبني **وقت التشغيل**، فلا `tsc` ولا
`next build` ولا أي فاحص عندنا بيشوفه؛ والصفحة مش مفتوحة للزوار علشان
حد يلاقي الغلط بالصدفة.

**القاعدتين:**
1. **متكتبش فاصلة بإيدك في نص `select`** — خليها كلها من `.join(',')`.
   الشكل ده الغلطة فيه مستحيلة، أحسن من فاحص بيدوّر عليها.
2. أي صفحة لوحة بتتبني استعلامها ديناميك، **افتحها بعينك** قبل ما تقول
   خلصت. «الكود اتكتب» مش «الصفحة بتشتغل».

⚠ **درس تاسع — الفاحص الشامل كان بيدوّر على كلمة واحدة والفشل بيتقال بكلمتين.**
`CHECK_DB.sql` كان بيفلتر `result like 'فشل%'` بس. وأربع دوال — `test_rls` ·
`test_work_rls` · `test_pair_want` · `test_work_admin_rpcs` — بتكتب **«رسب»**،
ودول بالظبط اختبارات الـRLS، أهم حاجة عندنا. يعني ٦٦ صف فشل ممكن كانوا يعدّوا
من غير ما حد ياخد باله. اتصلّح — ومتشيلش أي كلمة من التنتين.

⚠ **درس خامس — الاحتياطي لازم يملا الناقص، مش الغايب بس.** `content.ts`
كان بيستعمل الاحتياطي لو **الصفحة نفسها** مش في القاعدة. وهجرة `0082`
بتعمل الصفحات الأربعة **من غير فقرات** — فالصفحة موجودة وفاضية والاحتياطي
عمره ما اشتغل. النتيجة: `/about` على الإنتاج بيقول «بنكتب الصفحة دي
دلوقتي» والمحتوى موجود في الكود قدامنا. `copy-fallback` بيملا **المفاتيح
الناقصة** مش الجدول الناقص — والمحتوى لازم يعمل نفس الحاجة. صفحة من غير
ولا فقرة **مش قرار**؛ القرار إن المالك يقفلها (`is_active`).
الحارس: `scripts/check-content-fallback.mjs`.

⚠ **درس رابع — نص القالب والكود اللي بيملاه لازم يتفحصوا مع بعض.** قوالب
`notification_templates` فيها `{{1}}` و`{{2}}`، والقيم بتيجي من `CORE` في
`src/lib/server/notify.ts`. لو اتخالفوا **مفيش حاجة بتفشل** — الإيميل
بيتبعت والمتغيّر الناقص بيوصل فراغ. كان فيه اتنين واقعين من زمان:
`transfer_received` («وصلنا تحويلك لـ .») و`mutual_match` (الشات بيتفتح
والرابط ضايع). دلوقتي `scripts/check-notify-vars.mjs` جوه `npm run verify`
بيقارن الاتنين ويفشل البناء. **أي قالب جديد لازم يعدّي منه.**

وللفحص السريع بس (الشكل مش السلوك) — السطر ده بيقول كل دفعة واقعة ولا لأ:

```sql
select 'WM5 (fn_caller_is_browser)' as دفعة,
       to_regprocedure('fn_caller_is_browser()') is not null as اتلزقت
union all select 'WM6 (fn_maintenance_on)',   to_regprocedure('fn_maintenance_on()')   is not null
union all select 'WM7 (test_last_review_items)', to_regprocedure('test_last_review_items()') is not null
union all select 'WM8 (fn_create_sbota)',     to_regprocedure('fn_create_sbota(uuid,uuid,timestamptz,int,boolean,text)') is not null
union all select 'WM9 (content_pages)',       to_regclass('content_pages')             is not null;
```

⚠ **الترتيب مش تفصيلة:** `fn_create_sbota` (دفعة ٨) بتنادي `fn_maintenance_on()`
اللي جاية من دفعة ٦. بوستجرس ما بيتحققش من النداءات دي وقت إنشاء الدالة، فلو
٦ مش متلزقة الدالة **بتتعمل عادي وبتقع وقت الاستعمال**. فلو السطر بتاع WM6
رجع `false`، الزقها حتى لو ٨ و٩ اتلزقوا.

### ملفات التوثيق
`CONTENT_BRIEF.md` (المحتوى الناقص — بريف جاهز يتبعت لكاتب) · `CHECK_DB.sql` (فاحص القاعدة الشامل) · `README.md` · `DB_PLAN.md` · `ADMIN_PLAN.md` · `WORK_PLAN.md` · `DESIGN_TOKENS.md` · `COPY.md` · `ADMIN_GUIDE.md` · `RUNBOOK.md` · `DEPLOY_CHECKLIST.md` · **`REVIEW*.md`** (المراجعة) · ملفات `WORK_MIGRATION_*.sql` (لحد `_31`) و`WORK_CRON.sql` (تتلزق في SQL Editor).

---

## ٩. لو انت إيجنت جديد بتكمّل — ابدأ من هنا

1. اقرأ الملف ده كله + `REVIEW.md`.
2. `npm install` ثم `npm run verify` — لازم يعدّي قبل أي شغل.
3. أي تغيير: اتبعه في **كل الطبقات** (قاعدة → `api.ts` → واجهة → لوحة → نصوص → بذرة → توثيق). ممنوع تصلّح حتة وتسيب حتة. وسيلة رخيصة تمسك اللي فاتك: ضيف الحقل للنوع في `src/types` الأول و`tsc` هيوريك كل مكان ناقص، وقارن مفاتيح النصوص آليًا بين الكود والاحتياطي والبذرة والهجرة بدل ما تعدّها بعينك.
4. رقم → `settings`. نص → `copy_strings`. مفيش استثناء.
5. تغيير قاعدة → ملف هجرة + دالة `test_*` + `WORK_MIGRATION_N.sql` للمالك يلزقه، **وجرّبه على البوستجرس المحلي الأول** (§6). متسلّمش تصليح أمان من غير ما تشوفه بيمنع فعلًا.
6. قبل الرفع: `npm run verify` + لقطة للصفحة المتأثرة. كل تعديل كوميت لوحده بالعربي.
7. الأمان: متضعّفش سياسة عشان تخلّص، ومتحطش سر في الكود، ومتكتبش على جداول التبادل مباشرة.
8. **أي صفحة جديدة لازم يبقى ليها مدخل.** `/game` فضلت شهور مبنية وشغّالة ومترجمة وليها مفتاح ميزة — ومفيش ولا لينك ليها. الميزة اللي محدش يقدر يوصلها = ميزة مش موجودة. ولو المدخل في الهيدر بس، خد بالك إن الـnav `lg:flex` يعني كمبيوتر بس.
