# PLAN.md — نسبوط

خطة البناء الكاملة. المرجع الأعلى: `design/نسبوط.dc.html`، وبعده `DESIGN_TOKENS.md` و`COPY.md`.

---

## 1. التقنيات

| البند | الاختيار |
|---|---|
| الإطار | Next.js 14+ · App Router |
| اللغة | TypeScript (strict) |
| الأنماط | Tailwind CSS — بدون أي مكتبة واجهات |
| الخطوط | `next/font/google`: Rubik (700 · 900) · IBM Plex Sans Arabic (400 · 600) |
| الحركة | CSS في الأساس · `framer-motion` عند الحاجة بس (`/one` · اللعبة · النوافذ السفلية) |
| تصدير الصورة | `html-to-image` (بطاقة النتيجة 9:16) |
| الاختبارات | Playwright (اختبارات + لقطات) |
| الاتجاه | `<html lang="ar" dir="rtl">` · خصائص منطقية `ps/pe` · `ms/me` · `start/end` |

---

## 2. بنية المجلدات

```
نسبوط/
├── design/                          ← المرجع (ما يتلمسش)
│   ├── نسبوط.dc.html
│   ├── support.js
│   └── uploads/Main@1x.png
├── DESIGN_TOKENS.md
├── COPY.md
├── PLAN.md
├── README.md                        ← آخر خطوة
├── next.config.mjs
├── tailwind.config.ts
├── tsconfig.json
├── playwright.config.ts
├── scripts/
│   └── shots.ts                     ← لقطات 390 / 768 / 1440 لكل مسار
├── shots/                           ← الناتج
├── tests/
│   ├── main-path.spec.ts            ← الرئيسية ← التقييم
│   └── game.spec.ts                 ← اللعبة من السؤال 1 للنتيجة
└── src/
    ├── app/
    │   ├── layout.tsx               ← html/body · الخطوط · الوضع · LiveQ · Header/Footer
    │   ├── globals.css              ← متغيرات CSS · nb-marquee · nb-pulse · reduced-motion
    │   ├── page.tsx                 ← 1 · 2 · 3  الرئيسية
    │   ├── not-found.tsx            ← 21
    │   ├── one/page.tsx             ← 4
    │   ├── game/
    │   │   ├── page.tsx             ← 5
    │   │   └── result/page.tsx      ← 6
    │   ├── s/
    │   │   ├── [slug]/
    │   │   │   ├── page.tsx         ← 7 · 17 (سبوطة الشغل نفس المسار بنوع work)
    │   │   │   ├── pay/page.tsx     ← 9
    │   │   │   └── done/page.tsx    ← 10
    │   │   └── mystery/page.tsx     ← 16
    │   ├── join/page.tsx            ← 8
    │   ├── my/[bookingId]/
    │   │   ├── page.tsx             ← 11
    │   │   ├── chat/page.tsx        ← 12
    │   │   └── review/page.tsx      ← 13
    │   ├── me/page.tsx              ← 14
    │   ├── map/page.tsx             ← 15
    │   ├── rules/page.tsx           ← 18
    │   ├── captains/page.tsx        ← 19
    │   └── captain/[sbotaId]/page.tsx ← 20
    ├── components/                  ← القسم 4 تحت
    ├── data/                        ← القسم 5 تحت
    ├── lib/
    │   ├── api.ts                   ← الطبقة الوحيدة اللي هتتغير عند توصيل قاعدة بيانات
    │   ├── session.ts               ← cookie موقّع بسيط
    │   ├── theme.ts                 ← ليلي/نهاري + الساعة
    │   ├── type.ts                  ← قواعد نوع اللعبة
    │   ├── track.ts                 ← track(event, props)
    │   ├── ics.ts                   ← ملف التقويم
    │   └── chat-store.ts            ← localStorage + واجهة قابلة للتبديل
    └── types/index.ts
```

---

## 3. تخطيط المسارات على 21 شاشة

| # | المسار | الملاحظة |
|---|---|---|
| 1 | `/` | الرئيسية — الوضع من `data-theme` |
| 2 | `/` نهاري | **نفس الصفحة** — البطاقات والترتيب بيتغيروا بالوضع، مش مسار تاني |
| 3 | `/` على 1440 | **نفس الصفحة** — تخطيط الكمبيوتر، مش مسار تاني |
| 4 | `/one` | |
| 5 | `/game` | |
| 6 | `/game/result` | |
| 7 | `/s/[slug]` | `ehna-el-rabe3` · `fetar-3al-nil` · `ba3d-ma-el-shams-teghib` · `tarabeza-setta` · `el-mal3ab-lina` |
| 8 | `/join` | يقبل `?next=` |
| 9 | `/s/[slug]/pay` | |
| 10 | `/s/[slug]/done` | |
| 11 | `/my/[bookingId]` | |
| 12 | `/my/[bookingId]/chat` | |
| 13 | `/my/[bookingId]/review` | |
| 14 | `/me` | |
| 15 | `/map` | |
| 16 | `/s/mystery` | مسار ثابت **قبل** `[slug]` في الترتيب |
| 17 | `/s/work-[slug]` | `work-cafe-tagamo3` — نفس `[slug]` بنوع `work` ووضع نهاري إجباري |
| 18 | `/rules` | |
| 19 | `/captains` | |
| 20 | `/captain/[sbotaId]` | محمي بدور `captain` |
| 21 | `/not-found` | `not-found.tsx` + صفحة `/not-found` صريحة علشان اللقطات |

---

## 4. المكونات (`src/components/`)

### الهوية
| المكون | الواجهة |
|---|---|
| `Logo` | `size?: number = 34` · `variant: 'primary' \| 'onDark' \| 'onOrange' \| 'mono'` |
| `QIcon` | `size?: number = 44` · `color?` · `bg?` · `rotate?: number` |
| `LiveQ` | `state?: 'idle' \| 'looking' \| 'jump' \| 'sleep'` — SVG بعينين ونضارة في النهاري |
| `Sticker` | `color: 'orange' \| 'ink' \| 'cobalt' \| 'amber' \| 'cream'` · `rotate?: number` · `size?` |

### الأزرار والحقول
| المكون | ملاحظات |
|---|---|
| `PrimaryButton` | برتقالي · Rubik 900 · `min-h 48+` · `loading` · `disabled` — **واحد بس في كل شاشة** |
| `SecondaryButton` | شفاف بحد 2px |
| `StickyCTA` | `sticky bottom-0` + `env(safe-area-inset-bottom)` — على الكمبيوتر بيرجع عادي |
| `Field` | حد `2px solid ink` · `radius 14` · `min-h 52` · رسالة خطأ بالعامية |
| `FilterChips` | تمرير أفقي · `scrollbar-width:none` · بيفلتر فعلًا |

### البطاقات والأقسام
| المكون | ملاحظات |
|---|---|
| `SbotaCard` | الحالات: `normal` · `full` · `girls` · `work` · `mystery` |
| `PhotoPlaceholder` | `label` · `variant: 'sand' \| 'sandDeep'` · `shape: 'rect' \| 'circle'` |
| `CaptainCard` | ليلي `#1E2128` · نهاري `#EFE3CF` |
| `WhoBooked` | بدون أسماء · نسخة `work` بالمجال بدل السن |
| `GuaranteeBox` | رملي + درع `#3E5C43` |
| `TypeCard` | بطاقة النتيجة 9:16 القابلة للتصدير |
| `ClueGrid` | 7 مربعات · قفل SVG · «بكرة» |
| `RatingFaces` | 5 وشوش SVG مرسومة |

### التخطيط والحركة
| المكون | ملاحظات |
|---|---|
| `Header` | الشعار + روابط الكمبيوتر (`الجدول · الخريطة · القواعد · الكباتن · دخول/ملفي`) + `ThemeToggle` |
| `Footer` | من الملف حرفيًا |
| `Marquee` | 28s · محتوى مكرر مرتين · `dir=ltr` بره و`rtl` جوه |
| `MiniMap` | 260px (الرئيسية) · 140px (الكشف) |
| `CairoMap` | خريطة SVG كاملة بكتل المناطق |
| `Countdown` | حي · بيقلب لصيغة «هتعرف مجموعتك…» قبل 24 ساعة |
| `BottomSheet` | نافذة سفلية (القواعد · بطاقة الخريطة) |
| `ThemeToggle` | زر صغير في الرأس |
| `ChatRoom` | غرفة + قفل بالوقت + ضغط مطول + أدوات الكابتن |

---

## 5. البيانات الوهمية (`src/data/`)

| الملف | المحتوى |
|---|---|
| `sbotat.ts` | الخمسة من الملف + بطاقات النهار + سبوطة الشغل + الغامضة |
| `captains.ts` | سارة · يوسف · دينا · عمر (نصوص الملف) |
| `people.ts` | السبعة بألوان ستيكراتهم من الملف |
| `types.ts` | الأنواع الستة + جملها + صيغة المؤنث/المذكر |
| `clues.ts` | 7 أدلة للغامضة |
| `interests.ts` | العشرين |
| `filters.ts` | العشرة |
| `areas.ts` | مناطق الخريطة + إحداثيات SVG |
| `chat.ts` | رسالة الكابتن المثبتة + رسائل تجربة |
| `bookings.ts` | حجوزات المستخدم |

---

## 6. طبقة الـ API (`src/lib/api.ts`)

كل الدوال `async` وبترجع البيانات الوهمية بتأخير بسيط (120–300ms). **الملف ده هو النقطة الوحيدة اللي هتتغير لما نوصّل قاعدة بيانات.**

```
getSbotat(filter?)      getSbota(slug)         getRandomSbota(timeOfDay)
book(slug, userId)      sendOtp(phone)         verifyOtp(phone, code)   // 1234
createAccount(profile)  getGroup(bookingId)    getChat(bookingId)
sendMessage(roomId, t)  submitReview(payload)  getMe()
getCaptainBoard(id)     markArrived(id, uid)   pay(slug, method, ref?)
getClues()              getBookings()          redeemReferral(code)     // -15%
```

---

## 7. الحالة والقواعد

| القاعدة | التنفيذ |
|---|---|
| مفيش تسجيل قبل «أنا جاي» | الرئيسية وصفحة السبوطة مفتوحة للكل · الضغط بيوجّه لـ `/join?next=/s/[slug]/pay` |
| الرمز الوهمي | `1234` |
| الجلسة | cookie موقّع بسيط · بدون كلمة سر |
| الصور الشخصية | مخفية قبل الكشف · تظهر للكابتن بس · وفي «رايحين معاك» بعد التقييم المتبادل |
| الشات | غرفة لكل حجز · بيتفتح مع الكشف · بيتقفل بعد السبوطة بيومين ← قراءة فقط + `LiveQ.sleep` |
| الشات الخاص | من `/me` بس · بين اتنين اختاروا بعض في التقييم |
| الغامضة | الزر معطّل لو الحجوزات المكتملة < 2 |
| كوبون الإحالة | خصم 15% |
| كوبون بعد التقييم | 10% |

---

## 8. الاستجابة

| المقاس | الرئيسية | باقي الصفحات |
|---|---|---|
| 360–430 | عمود واحد · CTA لاصق · فلاتر بتمرير | عمود واحد · CTA لاصق |
| 768–1024 | بطاقات في عمودين · الخريطة جنب «نديها واحدة؟» | `max-w` مريح |
| 1280–1600 | **خريطة يمين 60% ثابتة** + بطاقات شمال في عمودين · «نديها واحدة؟» ثابت فوق الخريطة | `max-width: 720px` في النص · الأزرار في مكانها الطبيعي |

قواعد ثابتة: `max-width: 100%` للصور والبطاقات · الشرايط جوه `overflow-x: auto` · **ممنوع تمرير أفقي للصفحة على 360**.

---

## 9. التحليلات

`track(event, props)` بتطبع في الـ console دلوقتي. الأحداث:
`view_schedule` · `open_card` · `click_ana_gai` · `start_game` · `finish_game` · `reach_payment` · `paid` · `share_type_card` · `use_referral`

---

## 10. ترتيب التنفيذ

| المرحلة | المحتوى |
|---|---|
| **0** | المشروع + Tailwind + الخطوط + `globals.css` + التوكنز + `scripts/shots.ts` |
| **1** | المكونات المشتركة كلها (القسم 4) |
| **2** | الرئيسية — ليلي (شاشة الملف) ← **لقطة ومقارنة بالملف** |
| **3** | الرئيسية — نهاري + 768 + 1440 ← لقطات |
| **4** | `/s/[slug]` (شاشة الملف) ← **لقطة ومقارنة** |
| **5** | `/join` (شاشة الملف) ← **لقطة ومقارنة** |
| **6** | `/s/[slug]/pay` + `/done` |
| **7** | `/my/[bookingId]` (شاشة الملف) ← **لقطة ومقارنة** |
| **8** | `/my/[bookingId]/chat` |
| **9** | `/my/[bookingId]/review` |
| **10** | `/me` |
| **11** | `/one` |
| **12** | `/game` + `/game/result` |
| **13** | `/map` |
| **14** | `/s/mystery` |
| **15** | `/s/work-[slug]` |
| **16** | `/rules` |
| **17** | `/captains` |
| **18** | `/captain/[sbotaId]` |
| **19** | `/not-found` |
| **20** | اختبارات Playwright · `next build` · `eslint` · فحص التمرير والتباين |
| **21** | `README.md` بالعربي |

**بعد كل مرحلة صفحة:** لقطة على 390 · 768 · 1440، أبص عليها بنفسي، وأقارن بالملف لو الشاشة موجودة فيه، وأصلّح الفروق **قبل** ما أنتقل.

---

## 11. تعارضات مرصودة والقرار فيها

| # | التعارض | القرار |
|---|---|---|
| 1 | إزاحة الشعار: البرومبت `حجم/16` · الملف `4/-5` عند 34px | **الملف** — صيغة معايرة في `DESIGN_TOKENS.md §3` |
| 2 | سُمك المفرغة: البرومبت `حجم/50+1` · الملف `2px` عند 34 | **الملف** — `max(1, size × 0.06)` |
| 3 | ارتفاع الفلتر `40px` أقل من قاعدة الـ 44 | **الملف** للشكل + مساحة لمس شفافة تكمّل 44 من غير تغيير بصري |
| 4 | `nb-pulse` بيستخدم `box-shadow` والبرومبت بيمنع الظلال | **الملف** — دي نبضة مش ظل ناعم · بتتلغي تحت `prefers-reduced-motion` |
| 5 | «الأيام الفاضية» في الملف 7 أيام · وفي اللعبة 4 اختيارات | **الاتنين** — النموذج له قايمته [ملف] واللعبة لها قايمتها [برومبت] |
| 6 | أسماء الأنواع: مؤنث في الكشف [ملف] · مذكر في النتيجة [برومبت] | **الاتنين** — النوع بيتصرّف حسب نوع الشخص |
| 7 | البرومبت بيذكر «شاشة 7» لصفحة السبوطة و«القسم 4.7» — الملف مسمّيها `7` | متطابقين، مفيش تعارض |

---

## 12. قائمة القبول (هتتراجع في الآخر)

- [ ] الشاشات 1 · 7 · 8 · 11 مطابقة تقريبًا بالبكسل على 390
- [ ] 21 شاشة شغالة على 390 · 768 · 1440 بلقطات في `shots/`
- [ ] الوضعان تلقائي + بالزر
- [ ] «نديها واحدة؟» بيطلع واحدة وبيبدّلها
- [ ] اللعبة 8 أسئلة ← نوع ← بطاقة بتتحفظ صورة
- [ ] الفلاتر بتفلتر · المسار من الرئيسية للتقييم ما بيتكسرش
- [ ] مفيش تسجيل قبل «أنا جاي»
- [ ] الصور الشخصية مخفية حسب القواعد
- [ ] الشات بيتفتح ويتقفل بالوقت · مفيش خاص إلا بالاختيار المتبادل
- [ ] عامية بس · مفيش كلمة ممنوعة · مفيش صورة مخزون · مفيش أبيض · مفيش تدرجات ولا ظلال ناعمة
- [ ] `LiveQ` بحالاتها الأربع + النضارة
- [ ] `next build` نظيف · الاختبارات بتعدي · مفيش تمرير أفقي · التباين مضبوط
