# REVIEW_ADMIN.md — مراجعة لوحة «نسبوط»

مراجعة **قراءة بس** — مفيش ولا سطر اتغيّر في المشروع غير الملف ده.
الأساس: `ADMIN_PLAN.md` · `ADMIN_GUIDE.md` · `WORK_PLAN.md` · `DB_PLAN.md` · `RUNBOOK.md` ·
`DEPLOY_CHECKLIST.md` · كل صفحة تحت `src/app/admin/**` · `AdminShell.tsx` · `admin-ui.tsx` ·
`src/lib/admin.ts` · وكل الهجرات في `supabase/migrations/` و`WORK_MIGRATION*.sql`.

**مفيش قاعدة حية.** كل النتايج مستنبطة من الكود والـ SQL. عملت `npx next build` وعدّى
بنجاح (كل صفحات اللوحة بتتبني static · أكبر واحدة `/admin/shoghl` بـ 19.9 kB)، بس تشغيل
اللوحة في المتصفح من غير مفاتيح بيوقف عند «الصفحة دي للإدارة.» لأن `loadAdminMe()` بترجّع
`NO_ADMIN` من غير سوبابيس — يعني مفيش فايدة من النقر، فالمراجعة كلها من الكود.

> **الخلاصة في سطرين:** اللوحة **مبنية كويس ومكتوبة كويس** — بس تلات أقسام كاملة
> (حقول التسجيل · الخريطة · مفاتيح المزايا والصيانة) بتكتب في جداول **محدش بيقراها**،
> وأهم زرار في المنتج («اعتمد التوزيع واكشفه») بينادي دالة **مش ممنوحة** للمستخدم فبيقع دايمًا،
> ونص محرّرات اللوحة بتقول «اتحفظ ✓» حتى لو القاعدة رفضت.

---

## الجدول

| # | الخطورة | المجال | المشكلة | فين بالظبط | الأثر | الإصلاح المقترح | من اللوحة ولا كود؟ | الوقت |
|---|---|---|---|---|---|---|---|---|
| A1 | 🔴 | المطابقة | زرار «اعتمد التوزيع واكشفه» بينادي `fn_reveal` — والدالة دي **عمرها ما اتمنحت** لدور `authenticated`. الهجرتين `0011` و`0028` بيعملوا `revoke execute on all functions … from authenticated` وبيمنحوا قايمة بيضا مفيهاش `fn_reveal`. | `src/app/admin/matching/page.tsx:805` · `supabase/migrations/20260908103005_0026_scheduled_functions.sql:5` · `…_0028_close_function_holes.sql:8-30` | الكشف اليدوي **مستحيل** من اللوحة. المدير بيشوف «مقدرناش نعتمد: permission denied for function fn_reveal». الكشف بيحصل بس لما `job_reveal_due` توصل لميعادها. | `grant execute on function fn_reveal(uuid) to authenticated;` **مع** إضافة `if not fn_has_permission('matching.approve') then raise exception` جوه الدالة (زي `fn_build_matching`). | كود (هجرة) | ٣٠ د |
| A2 | 🔴 | الإعدادات · المزايا | جدول `feature_flags` **ما بيتقراش في أي مكان** في الموقع العام — القراءة الوحيدة في `/admin/settings`. | `src/app/admin/settings/page.tsx:451` هي القراءة الوحيدة · صفر نتايج لـ `grep -r feature_flags src/ \| grep -v admin` | قفل `booking` أو `game` أو `chat` أو `mystery` أو `map` أو `referral` أو `work_sbota` من اللوحة **ما بيعملش أي حاجة**. المالك هيفتكر إنه قفل الحجز وهو مفتوح. | دالة `getFlags()` في `src/lib/copy.ts` بنفس نمط `getCopy` (وسم `flags`)، وفحص في كل مسار: الحجز · اللعبة · الشات · الخريطة · الغامضة · الإحالة · الشغل. | كود | ٤ س |
| A3 | 🔴 | الإعدادات · الصيانة | زرار «اقفل الموقع للصيانة» بيكتب في `maintenance.is_on` — والميدل وير بيشتغل على `/admin/:path*` **بس**، ومحدش بيقرا الجدول. | `src/app/admin/settings/page.tsx:523-534` · `src/middleware.ts:97-103` | الرسالة بتقول «نسبوط بيتقفل على كل الأعضاء **فورًا**» — والموقع بيفضل شغال ١٠٠٪. ده أخطر وعد كاذب في اللوحة. | وسّع الـ matcher لكل الموقع، اقرا `maintenance` من كاش بوسم `flags`، ولو `is_on` اعرض `message_ar` واسمح للأدوار اللي في `allow_roles` بس. | كود | ٣ س |
| A4 | 🔴 | حقول التسجيل | `/admin/profile-fields` بيعدّل `profile_fields` · `field_options` · `skill_activities` · `consents` — و**صفحة التسجيل ما بتقراش ولا واحد فيهم**. بتقرا القوايم الثابتة من `src/data/lists.ts`. | `src/app/join/page.tsx:11-21` (استيراد `interests`/`areas`/`skillLevels`/`sports`/`budgets`/`days`/`girlsOnlyOptions`/`MAX_INTERESTS`) · صفر نتايج لـ `grep -r profile_fields src/ \| grep -v admin` | القسم كله «محرّر بيكتب في الفراغ». تعديل اختيار أو نشر نسخة موافقة جديدة **مالهوش أي أثر** على أي عضو. | خلي `/join` يقرا الحقول والاختيارات من القاعدة (بوسم كاش `fields`) و`src/data/lists.ts` يبقى الاحتياطي بس — بالظبط زي ما اللعبة اتعملت. | كود | ٦ س |
| A5 | 🔴 | الخريطة | `/admin/map` بيعدّل `venues.map_lat/map_lng/area_label_ar` — والخريطة العامة بترسم من مصفوفات ثابتة في الكود. | `src/app/admin/map/page.tsx:143,161` · `src/data/areas.ts:24 (mapAreas)` · `:94 (mapPins)` · `:105 (mysteryPin)` · `:111 (visitedAreaIds)` · `src/app/map/page.tsx:7` | تحريك مكان على الخريطة من اللوحة ما بيغيّرش حاجة للزوار. | حوّل `mapAreas`/`mapPins` لجداول (`map_areas` · مشتق من `venues`)، وخلي `CairoMap` يقراهم. | كود | ٥ س |
| A6 | 🔴 | الإعدادات | كل حفظ في `/admin/settings` **من غير `.select('id')`** — RLS بترفض بصفر صفوف من غير خطأ، والصفحة بتقول «اتحفظ ✓ وبان في الموقع» وبتحدّث الحالة المحلية كمان. | `src/app/admin/settings/page.tsx:471-482` (كل الأرقام والنصوص والـ JSON) · `:502` (`feature_flags`) · `:513` (`maintenance`) · `:553` (حذف كلمة ممنوعة) | لو دور مش مسموح له لمس الإعدادات فتح الصفحة، هيغيّر أرقام الفلوس ويشوف «✓» — والرقم زي ما هو. تضليل كامل. | ضيف `.select('id')` بعد كل `update`/`delete` وافحص `data.length === 0` — نفس نمط `src/app/admin/shoghl/page.tsx:338 rejected()`. | كود | ١ س |
| A7 | 🔴 | النصوص | نفس مشكلة A6 في محرر النصوص كله: الحفظ الفردي، والاستبدال الجماعي، والاستيراد من ملف. الاستبدال الجماعي بيعدّ `!error` على إنه نجاح فبيقول «اتغيّر 40 من 40» حتى لو القاعدة رفضت الأربعين. | `src/app/admin/content/page.tsx:103-107` · `:158-163` · `:211-212` | المالك يفتكر إنه غيّر نصوص الموقع كلها وهي زي ما هي. | نفس علاج A6 + عدّ النجاح من طول `data` مش من غياب `error`. | كود | ٤٥ د |
| A8 | 🔴 | اللعبة | نفس المشكلة في **كل** محرّر اللعبة: تعديل سؤال · تعديل اختيار · إعادة الترتيب · حذف اختيار · شبكة النقاط (`upsert`/`delete`) · تعديل نوع شخصية. مفيش ولا واحد فيهم بيفحص الرفض، والترتيب وشبكة النقاط مفيهمش حتى فحص `error`. | `src/app/admin/game/page.tsx:202` · `:208` · `:226` · `:272-273` · `:283` · `:289` · `:591` | تعديل نقطة في شبكة النقاط ممكن يضيع من غير ما حد يعرف — واللعبة دي أهم حاجة طلبها المالك. | نفس علاج A6 على السبعة. | كود | ١ س |
| A9 | 🔴 | حقول التسجيل | نفس المشكلة في ١٣ موضع في `/admin/profile-fields` — من ضمنهم **«انشر نسخة موافقة جديدة»** اللي المفروض يطلب من كل الأعضاء يوافقوا تاني. | `src/app/admin/profile-fields/page.tsx:226,238,284,295,534,545,605,754,765,826,978,994,1009` | قرار قانوني (إعادة الموافقة) ممكن يقول «اتنشرت ✓» وهو ما اتنشرش. | نفس علاج A6. | كود | ١ س |
| A10 | 🔴 | الأدوار · الأمان | سياسة `settings` في القاعدة هي `fn_is_admin()` مش `fn_has_permission('settings.edit')`. و`fn_is_admin()` بترجّع `true` لأي صف نشط في `admin_users` **مهما كان دوره**. | `supabase/migrations/20260908095553_0009_rls.sql:90` · `…_0035_admin_roles_permissions.sql:156-160` | دور `support` أو `ops` يقدر — بنداء API مباشر — يغيّر `commission_pct` و`vodafone_number` و`refund_full_days`. الصلاحيات الدقيقة على الإعدادات **واجهة بس**. | `drop policy settings_admin` وبدّلها بـ `for select using (true)` + `for update using (fn_has_permission('settings.edit'))`. | كود (هجرة) | ١ س |
| A11 | 🔴 | الأدوار · الأمان | نفس الحكاية على تسعة جداول تانية: `bookings` · `sbotat` · `venues` · `captains` · `coupons` · `reports` · `matching_runs` · `behavior_flags` · `chat_members` — كلها `for all using (fn_is_admin())`. | `supabase/migrations/20260908095553_0009_rls.sql:100,101,102,112,131,147,152,154,174,200` | `finance` يقدر يلغي سبوطة، و`support` يقدر يعمل كوبونات، و`ops` يقدر يقفل بلاغ. والرسايل في اللوحة اللي بتقول «القاعدة رفضت — محتاج صلاحية X» **مش هتظهر أبدًا** على الجداول دي. | استبدل كل `fn_is_admin()` بالصلاحية الصح: `sbotat.edit` · `bookings.edit` · `coupons.edit` · `reports.action` · `matching.run` … إلخ. | كود (هجرة) | ٣ س |
| A12 | 🟠 | الفلوس | `fn_approve_transfer` بتفحص `fn_is_admin()` بس، مش `payments.review`. | `supabase/migrations/20260909100300_0042_work_functions.sql` (جسم الدالة، سطر `if not fn_is_admin()`) | أي حد في الفريق — حتى `support` — يقدر يأكد تحويل فلوس أو يرفضه ويلغي حجز. (بالمقارنة `fn_issue_refund` و`fn_wallet_adjust` بيفحصوا الصلاحية الدقيقة صح.) | بدّل الفحص لـ `fn_has_permission('payments.review')`. | كود (هجرة) | ١٥ د |
| A13 | 🔴 | الإشعارات | المصرف الوحيد للطابور بيسحب `template_key like 'work%'` **بس**. كل الإشعارات التانية (`booking_confirmed` · `group_reveal` · `reminder_24h` · `reminder_3h` · `waitlist_promoted` · `review_request` · `win_back` …) بتفضل `queued` **للأبد** لأن واتساب مقفول. | `src/lib/server/notify.ts:257` (`.like('template_key','work%')`) · `:299` (`if (!key.startsWith('work_')) continue`) | العضو بيدفع وما بيوصلوش تأكيد، وبيتكشف عليه مجموعة وما بيعرفش، وبيوصل ميعاد السبوطة من غير تذكير. ده أكبر عطل تشغيلي في المنتج حاليًا. | وسّع `runWorkNotify` لكل القوالب (أو مصرف تاني)، وضيف `subject_ar` و`default_path` كأعمدة في `notification_templates` بدل الخرايط الثابتة في الكود. | كود | ٤ س |
| A14 | 🟠 | الكاش | `revalidateSite()` بينادي `/api/admin/revalidate` اللي بينادي `revalidateCopy()` — وده بيبطّل وسم `copy` **بس**. الوسوم اللي وعد بيها `ADMIN_PLAN §4` (`game` · `templates` · `settings` · `flags` · `fields`) مش موجودة أصلًا. | `src/lib/admin.ts:52-69` · `src/app/api/admin/revalidate/route.ts:33` · `src/lib/copy.ts:17,42,49` | الرسالة «اتحفظ ✓ **وبان في الموقع**» بتظهر بعد حفظ إعدادات أو قالب أو مفتاح ميزة — وهي غير صحيحة (الإعدادات مش متكاشة أصلًا، والمزايا مش متقروءة أصلًا). | إما تضيف الوسوم الخمسة بجد، أو تغيّر نص الرسالة على المسارات اللي مالهاش كاش. | كود | ٢ س |
| A15 | 🔴 | الإعدادات · الفلوس | `settings.emergency_phone` بيتعدّل من اللوحة (وموصوف «بيظهر للأعضاء في الشات ويوم السبوطة») — والموقع بيستعمل رقم ثابت في الكود. | `src/app/admin/settings/page.tsx:186-189` · `src/data/lists.ts:151` (`'+201000000000'`) · `src/app/rules/page.tsx:59` · `src/app/captain/[sbotaId]/page.tsx:93` | لو حصل حادث في سبوطة، العضو هيتصل برقم وهمي. والمالك مقتنع إنه ظبّطه من اللوحة. | خلي `getSettings()` تجيب `emergency_phone` وتمرّره للصفحتين، وامسح الثابت. | كود | ٤٥ د |
| A16 | 🔴 | الإعدادات | **١٦ عمود في `settings` معروضين وقابلين للتعديل في `/admin/settings` ومحدش بيقراهم** (وكمان `work_lunch_at` و`work_complaint_at` في تبويب إعدادات الشغل — يبقى ١٨) — لا الكود ولا الـ SQL. القايمة كاملة في القسم (ب) تحت. | `src/app/admin/settings/page.tsx` (المجموعات كلها) · النتيجة من مسح كل عمود في `src/` و`supabase/migrations/` | تبويب «الأرقام» بيدّي إحساس إن المالك بيدير المنتج، وهو بيدير حقول فاضية. أخطرهم `commission_pct` و`gateway_fee_pct` (فلوس) و`min_age` (قانوني) و`chat_open_hours`/`chat_close_hours`. | يا توصّل كل عمود بقارئه، يا تشيله من الشاشة. حاجة واحدة مش مقبولة: تسيبه معروض وهو ميت. | الاتنين | ٨ س |
| A17 | 🔴 | الأداء | **عشر صفحات بتحمّل الجدول كله في المتصفح وبتفلتر بـ JavaScript.** التفاصيل في الجدول الصغير تحت الجدول ده. | مذكور لكل صفحة تحت | على ١٠ آلاف صف الصفحات دي بتقع (أو بتاخد دقايق وبتوقّع تبويب المتصفح). دلوقتي شغالة لأن البيانات قليلة بس. | فلترة وترتيب وصفحات على الخادم (`.range()` + `.ilike()` + `count: 'exact'`)، وكل بحث نصي يروح لـ PostgREST مش لـ `Array.filter`. | كود | ٦ س |
| A18 | 🟠 | اللعبة | تبويب «الأرقام» بيقرا `game_sessions` — و**مفيش سطر واحد في الموقع بيكتب فيه**. | `src/app/admin/game/page.tsx:877` · صفر نتايج لـ `grep -r game_sessions src/ \| grep -v admin` | التبويب هيقول «لسه محدش لعب» للأبد، مهما لعب كام واحد. ومفيش أي فكرة عن نسبة الإكمال ولا فين الناس بتسيب اللعبة. | اكتب صف في `game_sessions` عند بدء اللعبة (`insert`) وحدّثه عند كل سؤال وعند النتيجة — السياسات `gs_insert`/`gs_update` مكتوبة وجاهزة من `0036`. | كود | ٢ س |
| A19 | 🟠 | السجل | `audit_log` فيه عمود `before` — و**مفيش دالة واحدة بتملاه**. كل الإدخالات بتكتب `after` بس. وأغلب أفعال اللوحة مالهاش سجل خالص (القايمة في القسم ج). | `supabase/migrations/20260908095210_0006_reviews_photos_ops.sql:127` · كل `insert into audit_log` في الهجرات | «مين غيّر العمولة؟» و«مين حظر الحساب ده؟» و«مين نشر نسخة الموافقة؟» — مالهمش إجابة. وسجل بـ `after` من غير `before` مش سجل تدقيق. | تريجر عام `fn_audit_row()` على الجداول الحساسة بيكتب `to_jsonb(old)` و`to_jsonb(new)`، بدل ما نعتمد على إن كل دالة تفتكر. | كود (هجرة) | ٤ س |
| A20 | 🟠 | الأمان | `requirePermission()` و`writeAudit()` مكتوبين بالكامل في `admin-auth.ts` — و**محدش بينديهم**. مفيش ولا `'use server'` واحد في المشروع، وكل أفعال اللوحة بتعدّي من المتصفح بمفتاح `anon`. | `src/lib/server/admin-auth.ts:498` · `:544` · صفر نتايج لـ `grep -rn "requirePermission\|'use server'" src/app` | `ADMIN_PLAN §5` بيقول «كل Server Action بتبدأ بـ `requirePermission`». الواقع: مفيش Server Actions أصلًا، والحماية كلها RLS — واللي فيها الثغرات A10 و A11. | يا تنقل الأفعال الحساسة (فلوس · حظر · أدوار · إعدادات) لمسارات خادم بتستعمل الدالتين، يا تصلّح السياسات وتشيل الوعد من الخطة. | كود | ٦ س |
| A21 | 🟠 | الأدوار | `/admin/shoghl` بيطلب `needs="sbotat.view"` — ودور `finance` **مالوش** الصلاحية دي. | `src/app/admin/shoghl/page.tsx:502` · `src/components/AdminShell.tsx:35` · `…_0035_admin_roles_permissions.sql:88-92` | المالية **مش شايفة القسم أصلًا** — رغم إن تبويب «الكروت» (اعتماد تحويلات) وتبويب «تقارير الأماكن» (دفع المستحقات) شغلهم بالظبط، والسياسات في القاعدة بتسمح لهم. | زوّد `sbotat.view` لدور `finance`، أو غيّر `needs` لـ `sbotat.view or payments.view`. | اللوحة (بعد هجرة) | ٣٠ د |
| A22 | 🟠 | الأدوار | صلاحيتين موجودتين في الكتالوج وما بيستعملهمش حد: `content.view` و`game.view`. الأقسام بتطلب `content.edit` و`game.edit`. | `src/components/AdminShell.tsx:20-21` · `src/app/admin/content/page.tsx:39` · `src/app/admin/game/page.tsx:72` · `…_0035:78` | دور `ops` عنده `content.view` و`game.view` — ومش شايف القسمين خالص، لا حتى للقراية. | خلي `needs` هي صلاحية العرض، والتعديل يتحجب جوه الصفحة بـ `me.permissions.has('content.edit')` — نفس نمط `/admin/settings`. | كود | ١ س |
| A23 | 🟠 | المطابقة · الأدوار | تبويب «الأوزان» بيسمح بالتعديل لو `matching.run` **أو** `settings.edit` — وبيكتب في جدول `settings`. ودور `ops` عنده `matching.run` من غير `settings.edit`. | `src/app/admin/matching/page.tsx:243` · `:320-332` | `ops` بيكتب في جدول الإعدادات من باب خلفي — و(بسبب A10) القاعدة **بتقبل**. وفوق كده الحفظ من غير `.select()` فمفيش فحص أصلًا. | خلي التعديل على `settings.edit` بس، وضيف `.select('id')`. | كود | ٢٠ د |
| A24 | 🟠 | الفلوس | فورم الاسترداد فيه خانة «الحالة» (اتعمل فعلًا / لسه بننفّذه) — والقيمة **ما بتتبعتش** لـ `fn_issue_refund` أصلًا (الدالة بتاخد ٤ معاملات مفيهمش الحالة). | `src/app/admin/payments/page.tsx:787` (`useState('succeeded')`) · `:944-950` (النداء) · `…_132858_refund_and_wallet_rpcs.sql:8` | المالية بتختار «لسه بننفّذه» والقاعدة بتسجّله بحالة تانية. حقل وهمي في شاشة فلوس. | يا تشيل الخانة، يا تضيف `p_status` للدالة. | كود | ٢٠ د |
| A25 | 🟠 | التغطية | `ADMIN_PLAN §3` بيقول `/admin/payments` = «المعاملات · الإيصالات · الاسترداد · المحفظة · الكوبونات · **المستحقات**». تبويب المستحقات **مش موجود**. | `src/app/admin/payments/page.tsx:43-49` (خمس تبويبات بس) | مستحقات أماكن الشغل اتعملت في `/admin/shoghl → تقارير الأماكن`، بس مستحقات أماكن الترفيه العادية (`commission_pct` · `org_fee`) مالهاش شاشة خالص. | يا تبويب سادس في الفلوس، يا تعديل الخطة إنها بقت في قسم الشغل. | الاتنين | ٤ س |
| A26 | 🟠 | المطابقة | نقل شخص بين المجموعات (السحب والإفلات) بيكتب في `bookings` و`chat_members` و`matching_runs` — كلهم من غير `.select()`. واعتماد `approved_by` مفيهوش أي فحص خطأ خالص. | `src/app/admin/matching/page.tsx:738-741` · `:776-779` · `:820` | النقل ممكن يفشل والشاشة تقول «اتنقل ✓»، والشخص يفضل في مجموعته القديمة وفي غرفتها. | نفس علاج A6. | كود | ٣٠ د |
| A27 | 🟠 | الحجوزات | تسجيل الحضور (check-in) بيكتب `checked_in_at` من غير `.select()`. وترقية من قايمة الانتظار بتعمل `insert` (ده بيرمي خطأ فتمام) بس المسح من `waitlist` من غير فحص رفض. | `src/app/admin/bookings/page.tsx:762-774` · `:600` · `:614` | الكابتن/المشرف يعلّم «وصل» ويرجع تاني يلاقيه مش متعلّم. | نفس علاج A6. | كود | ٢٠ د |
| A28 | 🟠 | كل اللوحة | من **٦٥ عملية `update`/`delete`** في صفحات اللوحة، **٢١ بس** بتفحص الرفض — يعني **٤٤ عملية** ممكن تقول «تمام» وهي ما عملتش حاجة. أوضح الناقصين: حذف كلمة ممنوعة · حذف عضو من الفريق · حذف كوبون · تعديل كوبون · حذف مكان · تعديل كابتن · قفل تنبيه مكان · حذف ملاحظة سلوك · تغيير حالة سبوطة عند الإلغاء. | `settings:553` · `team:194` · `payments:1295,1368` · `captains:339,537,760,790,819` · `people:557` · `sbotat:1256` | الرسالة بتقول «اتشال ✓» والصف موجود، وأحيانًا الواجهة بتشيله محليًا كمان فالمدير مش هيكتشف غير بعد Refresh. | ضيف `.select(...)` + `rejected(data)` — الدالة المساعدة مكتوبة أصلًا في `src/app/admin/shoghl/page.tsx:338`، انقلها لـ `src/lib/admin.ts` واستعملها في كل مكان. | كود | ٢ س |
| A29 | 🟠 | التشغيل | جدولة `/api/cron/work-notify` مش في الهجرات — في ملف يدوي فيه `<CRON_SECRET>` مكتوب كنص بديل، ودومين ثابت `https://nasbot.vercel.app`. | `WORK_CRON.sql:26,45,66` | لو الملف ده ما اتشغّلش يدويًا بعد النشر (وهو **مش مذكور في `DEPLOY_CHECKLIST`**)، إشعارات الشغل كلها بتقف. ولو الدومين اتغيّر لـ `nasbot.app` المهمة هترجّع 404 و`net.http_post` **مش بيعتبرها فشل** فمحدش هيعرف. | حوّله لهجرة بتقرا الدومين من إعداد، وضيف الخطوة في `DEPLOY_CHECKLIST` كبند إجباري. | كود + وثائق | ١ س |
| A30 | 🟠 | التشغيل · المراقبة | **مفيش أي مراقبة ولا تنبيه.** كل اللي موجود استعلامات SQL يدوية في `RUNBOOK §13`. مفيش Sentry (مذكور إنه «مش متوصّل») ومفيش صفحة صحة للمهام في اللوحة. | `RUNBOOK.md:405-427` · `DEPLOY_CHECKLIST.md:110` | مهمة مجدولة تقع النهاردة، والمالك يعرف بعد أسبوع لما حد يشتكي. `net.http_post` بالذات بترجّع نجاح فورًا حتى لو المسار رجّع 401. | كارت في `/admin` بيقرا `cron.job_run_details` (عبر دالة `security definer` مقفولة على `settings.view`) ويبقى أحمر لو أي مهمة فشلت آخر ٢٤ ساعة. | الاتنين | ٤ س |
| A31 | 🟡 | التشغيل | `job_reminders` بترجّع عدد تذكيرات الـ 24 ساعة بس — `get diagnostics` بتتنادى قبل إدخال الـ 3 ساعات. | `supabase/migrations/20260908103005_0026_scheduled_functions.sql` (جسم `job_reminders`) | العدد في `cron.job_run_details` ناقص، فلو حد بيراقب هيفتكر إن التذكيرات أقل. | اجمع العدّادين قبل `return`. | كود (هجرة) | ١٠ د |
| A32 | 🟡 | التغطية | قايمة الروابط في الصفحة الرئيسية مفيهاش `/admin/shoghl` ولا `/admin/team` — رغم إنهم في شريط `AdminShell`. | `src/app/admin/page.tsx:777-798` (`LINKS`) | تشتت بسيط: المالك بيلاقي القسم في مكان ومش لاقيه في مكان. | زوّد السطرين. | كود | ٥ د |
| A33 | 🟡 | الأدوار | صلاحية `db.export` موجودة في الكتالوج ومحدش بيستعملها — وتصدير CSV في `/admin/audit` مفتوح لأي حد عنده `audit.view` (يعني `finance` كمان). | `…_0035_admin_roles_permissions.sql:79` · `src/app/admin/audit/page.tsx` (زرار التصدير) | سجل تصرفات الفريق كله ينزل CSV بصلاحية عرض. | اربط التصدير بـ `db.export`، أو اشطب الصلاحية. | كود | ٢٠ د |
| A34 | 🟡 | الفلوس | إضافة كارت شغل يدوي (هدية/تعويض) بتكتب `work_passes` مباشرة بصفر جنيه — **من غير أي سطر في `audit_log`**، وعدد الجلسات (`8 : 4`) ومدة الصلاحية الاحتياطية (`10 : 6` أسابيع) ثابتين في الكود. | `src/app/admin/shoghl/page.tsx:1816-1826` · `:1802-1806` | إصدار قيمة مجانية من غير أثر. ولو المالك غيّر «كارت ٤ أيام» لـ ٥ أيام من اللوحة، الكارت اليدوي هيفضل ٤. | RPC `fn_admin_grant_pass` بتفحص `payments.review` وتكتب في السجل، وأعمدة `work_pass4_sessions`/`work_pass8_sessions` في `settings`. | الاتنين | ٢ س |
| A35 | 🟡 | الفلوس | تسجيل «اتدفع» لمستحق مكان بيكتب `venue_reports.paid_at` (بفحص رفض صحيح ✓) — بس **من غير `audit_log`**. | `src/lib/work.ts:908-920` | إقرار بدفع فلوس لطرف تالت من غير أثر. | نفس علاج A19. | كود | ٣٠ د |
| A36 | 🟡 | الرسايل | «الإرسال الجماعي» بيسجّل صف `draft` في `broadcasts` وبس — **مفيش إرسال**. الصفحة صادقة ومكتوب فيها كده بوضوح، بس الزرار مكتوب عليه «سجّل الحملة لـ N عضو» جوه قسم اسمه «الرسائل». | `src/app/admin/notifications/page.tsx:626-646` · `:657-661` (التحذير) | ميزة نص شغالة. مقبولة مؤقتًا لأنها مكتوبة، بس مربوطة بـ A13 — لما واتساب/الإيميل يشتغل لازم تكمل. | دالة `fn_send_broadcast` بتحوّل الشريحة لصفوف `notifications`. | كود | ٣ س |
| A37 | 🟡 | الوثائق | `ADMIN_GUIDE.md` بيوصف اللوحة بشكل **أحسن من الواقع** في ٦ مواضع (خريطة الأقسام ناقصة `/admin/team`، وأقسام موصوفة إنها بتأثر على الموقع وهي لأ). التفاصيل في القسم (د). | `ADMIN_GUIDE.md:88-107` · `:114-131` | المالك بيثق في الدليل ويتصرف على أساسه. | تحديث بعد إصلاح A2·A3·A4·A5. | وثائق | ١ س |
| A38 | 🟡 | الوثائق | `RUNBOOK §9` بيوصف «دوال الحافة» `match` و`notify` بروابط `/functions/v1/…` — **مفيش مجلد `supabase/functions` أصلًا**. و`§10` بيقول من غير واتساب النظام «بيعلّم الإشعار `sent`» — ده **مش صحيح**، الصفوف بتفضل `queued`. | `RUNBOOK.md:306-316` · `:318-322` · `ls supabase/` | لو حصلت مشكلة إشعارات، اللي بيصلّح هيدوّر على دوال مش موجودة ويفتكر إن الطابور بيتفضّى وهو واقف. | امسح قسم دوال الحافة، واكتب الحقيقة: الطابور بيتفضّى لـ `work_*` بس من `/api/cron/work-notify`. | وثائق | ٣٠ د |
| A39 | 🟡 | الوثائق | `DEPLOY_CHECKLIST §7` بيقول «المفروض تشوف **8** مهام» — الحقيقة **12** مهمة `nasbot-*` (٨ + ٤ مهام شغل) وكمان `nasbot-work-notify` لو اتعملت. و`§11` بيقول «مسارات `/api/cron/[job]` لسه متعملتش» و«لسه محتاج سطر `manifest`» — **الاتنين اتعملوا**. | `DEPLOY_CHECKLIST.md:186-215` · `:328-336` · `supabase/migrations/…_0045_work_jobs.sql:213-222` · `src/app/layout.tsx:30` | المالك هيعدّ ٨ ويلاقي ١٢ ويفتكر إن فيه حاجة غلط، أو يعدّي على خطوة مطلوبة فعلًا. | تحديث الجدول والقايمة. | وثائق | ٢٠ د |
| A40 | 🟡 | الوثائق · النشر | `ADMIN_PLAN §6` بيقول المنطقة `fra1` و`npm run verify` = typecheck + lint + build + **test**. الواقع: `vercel.json` مفيهوش `regions`، و`verify` = `check:copy && tsc && build` (مفيش lint ولا test). | `vercel.json` · `package.json:16` · `ADMIN_PLAN.md:236,248` | النشر ممكن يقع في منطقة بعيدة عن مصر، والفحص الآلي مش بيمسك أخطاء ESLint ولا اختبارات Playwright. | ضيف `"regions": ["fra1"]` وكمّل سكريبت `verify`. | كود | ٢٠ د |
| A41 | 🔵 | الوثائق | `ADMIN_GUIDE` بيقول «**357** نص، مقسومين على **19** شاشة». العدّ الفعلي في `copy-fallback.ts` = **543** مفتاح على **22** شاشة، و`scripts/copy-seed.json` فيه **590**. | `ADMIN_GUIDE.md:135` · `src/data/copy-fallback.ts` | رقم قديم، مش مؤذي بس بيقلّل الثقة. | حدّثه (أو شيل الرقم خالص). | وثائق | ٥ د |
| A42 | 🟡 | الأمان | `admin_users.totp_secret` متخزّن **نص صريح**. الدليل نفسه معترف بيها في آخر سطر — بس هي لسه مفتوحة. | `ADMIN_GUIDE.md:397-401` · `…_0035_admin_roles_permissions.sql:105` | أي نسخة احتياطية من القاعدة أو مفتاح خدمة مسرّب = التحقق بخطوتين لكل الفريق راح. | تشفير بـ Supabase Vault أو AES-GCM بمفتاح في متغير بيئة منفصل عن مفتاح الخدمة. | كود | ٣ س |
| A43 | 🔵 | الواجهة | `useFlash` بتخفي الرسالة بعد ٣ ثواني (٤ في الفلوس والشغل) وبتعرض رسالة واحدة بس. رسايل الخطأ الطويلة من Postgres بتروح قبل ما تتقرا. | `src/components/admin-ui.tsx:50-64` | المدير بيدوس ويشوف وميض، وميعرفش حصل إيه — خصوصًا في العمليات الجماعية. | خلي رسايل الخطأ تفضل لحد ما المستخدم يقفلها، والنجاح بس هو اللي يختفي لوحده. | كود | ٣٠ د |
| A44 | 🔵 | التغطية | `ADMIN_PLAN §3` وعد بـ «**معاينة حية**» في `/admin/content` — مش موجودة. (الرجوع لنسخة قديمة من `copy_history` موجود وشغال ✓). | `ADMIN_PLAN.md:174` · `src/app/admin/content/page.tsx` | المالك بيعدّل نص من غير ما يشوف شكله في مكانه. | إطار `iframe` جنب الحقل على الشاشة اللي النص فيها. | كود | ٣ س |
| A45 | 🔵 | التشغيل | الأرقام الأسبوعية في `/admin` بتقرا `fn_weekly_metrics` — ولو الاستعلام وقع الصفحة بتخفي القسم كله بصمت (`if (failed) return null`). | `src/app/admin/page.tsx:620-624` | المالك مش هيعرف إن الأرقام ناقصة — هيفتكر إنها مش متاحة لدوره. | اعرض سطر «الأرقام مش متاحة دلوقتي» بدل الإخفاء. | كود | ١٥ د |

### تفصيل A17 — الصفحات اللي بتحمّل كل الصفوف

| الصفحة | اللي بيتحمّل | فين |
|---|---|---|
| `/admin/sbotat` | `sbotat.select('*')` **من غير أي حد** + `bookings` 20,000 صف | `sbotat/page.tsx:264,273` |
| `/admin/templates` | `sbota_templates.select('*')` من غير حد، والبحث في JS | `templates/page.tsx:118-121` |
| `/admin/people` | 2,000 ملف + 5,000 ملاحظة سلوك، والبحث والفلاتر كلها JS | `people/page.tsx:243,248,269-280` |
| `/admin/audit` | 2,000 سطر سجل + 2,000 ملف، وكل الفلاتر JS | `audit/page.tsx:98,99` |
| `/admin/notifications` → الإرسال الجماعي | **5,000 ملف + 20,000 حجز** في المتصفح | `notifications/page.tsx:555,556` |
| `/admin/reports` | 500 بلاغ + 2,000 ملف + 1,000 ملاحظة، والفلترة JS | `reports/page.tsx:145-156` |
| `/admin/captains` | ٤ استعلامات بـ 500/500/500/1000 | `captains/page.tsx:211-235` |
| `/admin/game` | كل الأسئلة والاختيارات والأنواع `select('*')` + 5,000 جلسة | `game/page.tsx:95-97,879` |
| `/admin/profile-fields` | أربع جداول `select('*')` من غير حد | `profile-fields/page.tsx:113-116` |
| `/admin/content` | كل `copy_strings` من غير حد، والبحث والاستبدال في JS | `content/page.tsx:59-66` |
| `/admin/shoghl` → الكروت | آخر 400 كارت وفلترة الحالة في JS | `shoghl/page.tsx:1705` |
| `/admin/shoghl` → الشركات | 300 طلب في JS | `work.ts:1088,1097` |
| `/admin/payments` → كل المعاملات | آخر 300، والحالة والبحث في JS | `payments/page.tsx:647,660-673` |
| `/admin/matching` | 200 تشغيلة + 500 نتيجة | `matching/page.tsx:419,423` |
| `/admin/bookings` | 500 حجز (الفلاتر server-side ✓ بس البحث بالاسم JS، ومفيش صفحات) | `bookings/page.tsx:277-280` |
| `/admin/map` | كل الأماكن + 5,000 سبوطة للعد | `map/page.tsx:98-103` |

---

## (أ) التغطية — الخطة مقابل الواقع

### الصفحات (`ADMIN_PLAN §3`)

| القسم في الخطة | الصفحة موجودة؟ | موصولة بجداول حقيقية؟ | ملاحظة |
|---|---|---|---|
| `/admin` الرئيسية | ✅ | ✅ عدّادات `count: 'exact'` من ٦ جداول | «المهام» شغالة تمام. الأرقام الأسبوعية عبر `fn_weekly_metrics` وبتختفي بصمت لو فشلت (A45). قايمة الروابط ناقصة قسمين (A32). |
| `/admin/login` رمز + TOTP | ✅ | ✅ `admin_users` · `admin_sessions` · `otp_codes` | الرمز بيروح **إيميل** مش واتساب (واتساب محاكاة). `totp_secret` نص صريح (A42). |
| `/admin/content` النصوص | ✅ | ✅ `copy_strings` + `copy_history` (الرجوع شغال) | **مفيش معاينة حية** (A44) · الحفظ من غير فحص رفض (A7) · تحميل كامل (A17). |
| `/admin/game` اللعبة | ✅ | ✅ `game_questions` · `game_options` · `game_option_scores` · `personality_types` — **والموقع بيقرا منهم فعلًا** (`api.ts:1238`) | أحسن قسم في اللوحة. تبويب «الأرقام» ميت لأن `game_sessions` ما بيتكتبش (A18). الحفظ من غير فحص رفض (A8). |
| `/admin/profile-fields` حقول التسجيل | ✅ | ⚠️ **الجداول موجودة والصفحة بتكتب فيها — والموقع ما بيقراهاش خالص** (A4) | محرّر كامل بأربع تبويبات بيكتب في الفراغ. |
| `/admin/templates` القوالب | ✅ | ✅ `sbota_templates` | مفيش تحرير لـ `work_config` (جدول يوم الشغل) — فالجدول ده متعدّلش إلا بـ SQL. |
| `/admin/sbotat` المواعيد | ✅ | ✅ `sbotat` — جدول + تقويم + نسخ + متكرر + إلغاء | كامل. تحميل كل الصفوف (A17). |
| `/admin/bookings` الحجوزات | ✅ | ✅ `bookings` · `waitlist` — ٣ تبويبات | تسجيل الحضور من غير فحص رفض (A27). |
| `/admin/matching` المطابقة | ✅ | ✅ `matching_runs` + سحب وإفلات حقيقي | **زرار الاعتماد مكسور تمامًا (A1)** — وده قلب القسم. |
| `/admin/people` الناس | ✅ | ✅ `profiles` · `behavior_flags` + صفحة الشخص | الحظر بيفحص الرفض صح ✓. |
| `/admin/captains` الكباتن | ✅ | ✅ `captains` · `captain_applications` · `venues` · `provider_alerts` | ٤ تبويبات. أغلب الكتابة من غير فحص رفض (A28). |
| `/admin/payments` الفلوس | ⚠️ | ✅ `payments` · `refunds` · `wallet_ledger` · `coupons` | **تبويب «المستحقات» ناقص** (A25) · حقل «الحالة» ميت (A24). |
| `/admin/reports` البلاغات | ✅ | ✅ `reports` · `behavior_flags` — وبيفحص الرفض صح ✓ | تمام. |
| `/admin/notifications` الرسايل | ⚠️ | ✅ `notification_templates` · `notifications` · `broadcasts` | «الإرسال الجماعي» **بيسجّل مسودة وبس** (A36) · الطابور واقف لكل اللي مش `work_*` (A13). |
| `/admin/map` الخريطة | ⚠️ | ⚠️ بيكتب في `venues` — **والخريطة العامة من `src/data/areas.ts`** (A5) | |
| `/admin/settings` الإعدادات | ⚠️ | ⚠️ `settings` ✅ · `banned_words` ✅ · **`feature_flags` ميت (A2)** · **`maintenance` ميت (A3)** · ١٦ عمود ميت (A16) | |
| `/admin/audit` السجل | ✅ | ✅ `audit_log` | `before` فاضي دايمًا وأغلب الأفعال مش متسجّلة (A19). |
| `/admin/shoghl` الشغل | ➕ | ✅ ٨ تبويبات — **مش في `ADMIN_PLAN`** (جه من `WORK_PLAN`) | `finance` مش شايفه أصلًا (A21). |
| `/admin/team` الفريق | ➕ | ✅ `admin_users` — **مش في `ADMIN_PLAN` ولا في `ADMIN_GUIDE`** | بيفحص الرفض صح ✓ ما عدا الحذف. |

**مفيش ولا صفحة placeholder فاضية.** كل الـ 19 صفحة فيها منطق حقيقي.

### التبويبات جوه الصفحات متعددة التبويبات

| الصفحة | التبويب | الحالة |
|---|---|---|
| `/admin/payments` | التحويلات المستنية | ✅ شغال — `fn_approve_transfer` · إيصال موقّع · سبب رفض إجباري |
| | كل المعاملات | ✅ شغال (فلترة JS) |
| | الاسترداد | ✅ شغال — `fn_issue_refund` · حقل «الحالة» ميت (A24) |
| | المحفظة | ✅ شغال — `fn_wallet_adjust` بفحص صلاحية صح |
| | الكوبونات | ✅ شغال — بس من غير فحص رفض |
| | ~~المستحقات~~ | ❌ **مش موجود** |
| `/admin/shoghl` | المجالات | ✅ شغال بفحص رفض صح · `Empty` لو الجدول فاضي (رسالة مفيدة بتقول إن الهجرة ما اتشغّلتش) |
| | أماكن الشغل | ✅ شغال بفحص رفض صح |
| | الإعدادات | ⚠️ شغال بس `work_lunch_at` و`work_complaint_at` **ميتين** (البند 22 تحت) |
| | الكروت | ✅ شغال — الاعتماد عبر `fn_approve_transfer` · الكارت اليدوي من غير سجل (A34) |
| | الأيام الثابتة | ✅ شغال بفحص رفض صح |
| | تقارير الأماكن | ✅ شغال — `fn_admin_build_venue_report` ممنوحة صح · من غير سجل (A35) |
| | المؤشرات | ✅ شغال — `fn_work_metrics` ممنوحة صح |
| | الشركات | ✅ شغال |
| `/admin/settings` | الأرقام | ⚠️ بيحفظ من غير فحص رفض (A6) · ١٦ عمود ميت (A16) |
| | مفاتيح المزايا | ❌ **بيحفظ في جدول محدش بيقراه** (A2) |
| | الصيانة | ❌ **بيحفظ في جدول محدش بيقراه** (A3) |
| | الكلمات الممنوعة | ✅ شغال — بس دي حماية داخل اللوحة بس (مش على الموقع)، وده مقصود ومكتوب |
| `/admin/bookings` | الحجوزات / الانتظار / الحضور | ✅ التلاتة شغالين |
| `/admin/matching` | الأوزان | ⚠️ بيكتب `settings` من غير فحص رفض + مشكلة أدوار (A23) |
| | التشغيلات | ✅ شغال — `fn_build_matching` / `fn_build_work_matching` ممنوحين صح |
| | المجموعات | ❌ **زرار الاعتماد مكسور** (A1) |
| `/admin/game` | الأسئلة والنقاط · الأنواع · المحاكي | ✅ التلاتة شغالين |
| | الأرقام | ❌ **فاضي للأبد** (A18) |
| `/admin/captains` | الكباتن · الطلبات · التقارير · الأماكن | ✅ الأربعة شغالين |

> **رسايل `Empty` اللي معناها «فاضي» مش «مكسور»:** كل الـ `<Empty>` في اللوحة رسايل حالة فاضية
> مكتوبة كويس (مثال: «خلّصت كل حاجة. 👏»)، مش placeholders. الوحيدة اللي معناها عطل هي
> تبويب «الأرقام» في اللعبة — بيقول «لسه محدش لعب» وهو مش هيتغير أبدًا.

---

## (ب) «ده كان المفروض من اللوحة» — القايمة الكاملة

الترتيب من الأهم للأقل. العمود الأخير = اسم العمود المقترح في `settings` (أو الجدول البديل).
**«ميت»** يعني العمود موجود في `settings` وبيتعدّل من اللوحة و**محدش بيقراه**.

### أولًا: قوايم التسجيل — القسم كله معطّل (A4)

| # | الحاجة | فين بالظبط | القيمة دلوقتي | الوجهة المقترحة |
|---|---|---|---|---|
| 1 | أقصى عدد اهتمامات | `src/data/lists.ts:47` | `MAX_INTERESTS = 5` | `settings.max_interests` — **موجود وميت** |
| 2 | قائمة الاهتمامات العشرين | `src/data/lists.ts:21-42` | ٢٠ نص ثابت | `field_options` (field_key = `interests`) |
| 3 | الاهتمامات المختارة افتراضيًا | `src/data/lists.ts:45` | ٥ ثابتة | `field_options.is_default` (عمود جديد) |
| 4 | قائمة المناطق في التسجيل | `src/data/lists.ts:50-57` | ٦ ثابتة | `field_options` (`areas`) |
| 5 | درجات المستوى | `src/data/lists.ts:60` | `أول مرة · مبتدئ · متوسط · كويس` | `field_options` (`skill_levels`) |
| 6 | الرياضات اللي بنسأل عنها | `src/data/lists.ts:63` | `بادل · جري · سباحة` | `skill_activities` — **الجدول موجود ومعمول في اللوحة وميت** |
| 7 | شرايح الميزانية | `src/data/lists.ts:66` + `src/lib/map-db.ts:107` + قيد `check` في القاعدة | `250 / 500 / 1000` | `field_options` (`budgets`) + تخفيف القيد |
| 8 | أيام الأسبوع الفاضية | `src/data/lists.ts:69` | ٧ ثابتة | `field_options` (`free_slots`) |
| 9 | تفضيل «بنات بس» | `src/data/lists.ts:72` | `دايمًا · أحيانًا · مش مهم` | `field_options` (`girls_only_pref`) |
| 10 | أقل سن للتسجيل | `src/app/join/page.tsx:50` | `18` ثابت + أقدم سنة `1950` | `settings.min_age` — **موجود وميت** · `settings.max_age` |
| 11 | أقل سن للمبيت | — (مفيش أي فحص في الكود) | لا شيء | `settings.min_age_overnight` — **موجود وميت** |

### ثانيًا: أرقام الفلوس والسياسات — أعمدة `settings` الميتة (A16)

| # | العمود | معروض في اللوحة | القارئ الحقيقي | المفروض يبقى إيه |
|---|---|---|---|---|
| 12 | `commission_pct` | «عمولتنا %» — `settings/page.tsx:129` | **مفيش** | يدخل في حساب مستحق المكان في `fn_venue_report` وفي شاشة المستحقات (A25) |
| 13 | `gateway_fee_pct` | «رسوم بوابة الدفع %» — `:137` | **مفيش** | يتشال دلوقتي (مفيش بوابة) أو يدخل في صافي المستحق |
| 14 | `review_coupon_pct` | «كوبون التقييم %» — `:203` | **مفيش** | يولّد كوبون تلقائي بعد التقييم في `job_after_sbota` |
| 15 | `first_time_work_discount_pct` | «خصم أول سبوطة شغل %» — `:216` | **مفيش** — الشغل بيستعمل `work_first_time_price` بدلها | يتشال، أو يحل محل السعر الثابت |
| 16 | `refund_credit_hours` | «رصيد محفظة بدل كاش قبل X ساعة» — `:113` | **مفيش** — `fn_cancel_booking` بتستعمل `is_first` (أول مرة يلغي) بدلها | يتوصّل في `fn_cancel_booking` |
| 17 | `weather_rules` (jsonb) | «قواعد الجو» — `:404` | **مفيش** | جدول قواعد بيتقرا وقت الإلغاء بسبب الجو |
| 18 | `metric_thresholds` (jsonb) | «حدود الأرقام» — `:409` | **مفيش** — الرئيسية بتلوّن بمنطق ثابت (`rank()` في `page.tsx:713`) | تلوين أرقام `/admin` منه |
| 19 | `emergency_phone` | «رقم الطوارئ» — `:190` | **مفيش** — الموقع بيقرا `src/data/lists.ts:151` | راجع A15 |

### ثالثًا: المواعيد والتوقيتات — كلها ثابتة في SQL

| # | الحاجة | فين بالظبط | القيمة | الوجهة |
|---|---|---|---|---|
| 20 | قفل الحجز قبل السبوطة | `…_0003_supply.sql` جوه `fn_sbota_timings` | `interval '36 hours'` | `settings.booking_close_hours` (عمود جديد) |
| 21 | ساعة الكشف | نفس الدالة | `interval '20 hours'` مكتوبة بالإيد | `settings.reveal_hour_cairo` — **موجود وميت** |
| 22 | الكشف قبل السبوطة بـ | نفس الدالة | `interval '24 hours'` | `settings.reveal_before_hours` |
| 23 | فتح الشات | نفس الدالة | `chat_opens_at := reveal_at` (ثابت) | `settings.chat_open_hours` — **موجود وميت** |
| 24 | قفل الشات | نفس الدالة | `ends_at + interval '48 hours'` | `settings.chat_close_hours` — **موجود وميت** |
| 25 | التذكيرات | `…_0026_scheduled_functions.sql` جوه `job_reminders` | `24 hours` و`3 hours` مكتوبين مرتين | `settings.reminder_hours` (jsonb مصفوفة) |
| 26 | «الناس سألت عليك» | نفس الملف جوه `job_win_back` | غياب `45 days` · ما نكررش قبل `60 days` | `settings.winback_idle_days` · `settings.winback_cooldown_days` |
| 27 | مسح البيانات الشخصية | نفس الملف جوه `job_purge` | `30 days` بعد الحذف · رموز OTP بعد `1 day` | `settings.purge_after_days` · `settings.otp_keep_days` |
| 28 | «فاضل يوم واحد في الكارت» | `…_0045_work_jobs.sql` جوه `job_work_pass_reminders` | `sessions_total - sessions_used = 1` | `settings.pass_low_threshold` |
| 29 | «الكارت قرب ينتهي» | نفس الدالة | `interval '7 days'` | `settings.pass_expiring_days` |
| 30 | مهلة الدفع للحجز | `src/app/api/pay/create/route.ts:147` | `60 * 60_000` = ساعة | `settings.booking_hold_minutes` |
| 31 | مهلة الدفع للكارت | `src/lib/work.ts:486` | `15 * 60_000` = ١٥ دقيقة | `settings.pass_hold_minutes` |
| 32 | مهلة الحجز من قايمة الانتظار | `src/app/admin/bookings/page.tsx:593` | `24 * 60 * 60 * 1000` | `settings.waitlist_hold_hours` |
| 33 | «أوقف اليوم الثابت أسبوعين» | `src/lib/work.ts:637` | `+ 14` يوم | `settings.recurring_pause_days` |
| 34 | نافذة سبوطات الشغل المعروضة | `src/data/lists.ts:206` | `WORK_WINDOW_DAYS = 14` | `settings.work_window_days` |
| 35 | سبوطات الشغل «اللي لسه بدأت» | `src/lib/api.ts:1512` | `- 6 * 3600_000` | `settings.work_grace_hours` |
| 36 | وضع النهار / الليل | `src/lib/theme.ts:11` و`:27` | `h >= 5 && h < 17` | `settings.day_mode_start_hour` (=6) و`day_mode_end_hour` (=18) — **الاتنين موجودين وميتين، وقيمهم مختلفة عن الكود** |
| 37 | جدول يوم الشغل (البداية والنهاية) | `src/data/lists.ts:175-180` + `src/lib/map-db.ts:447` | `10:00 / 13:00 / 14:30 / 15:00` — بتتقرا من `sbota_templates.work_config` **مش** من `settings` | `settings.work_lunch_at` و`work_complaint_at` **موجودين في اللوحة وميتين** · محتاج كمان `work_day_start` و`work_day_end`، أو تحرير `work_config` في `/admin/templates` |

### رابعًا: أوزان المطابقة الداخلية (`ADMIN_PLAN §1` وعد إنها تتعدّل من اللوحة)

| # | الحاجة | فين بالظبط | القيمة | الوجهة |
|---|---|---|---|---|
| 38 | عقوبة تعدّي فرق السن | `…_135317_fn_build_matching_arabic_why_polish.sql:300` | `- 60 - 3 * (الفرق الزايد)` | `settings.match_weights` (jsonb) |
| 39 | مكافأة «بيفتح الكلام» | نفس الملف `:308` | `+ 20 * الناقص` | نفس الـ jsonb |
| 40 | عقوبة مجموعة كلها ساكتة | نفس الملف `:311` | `- 2` | نفس الـ jsonb |
| 41 | عقوبة تجميع كتار الغياب | نفس الملف `:323` | `- 15 * العدد` | نفس الـ jsonb |
| 42 | مكافأة الاقتراب من الحجم المستهدف | نفس الملف `:327` | `+ (المستهدف − الحالي)` | نفس الـ jsonb |
| 43 | نفس الخمسة في مطابقة الشغل | `…_0048_work_matching.sql` | نفس الأرقام بقيم مختلفة | نفس الـ jsonb بمفتاح `work` |
| — | ✅ **اللي شغال:** `match_max_age_gap` · `match_min_starters` · `match_girls_ratio_min/max` · `match_mutual_weight` · `match_no_show_limit` · `algorithm_version` | | كلهم بيتقروا من `settings` فعلًا | ✓ |

### خامسًا: الأسعار والأرقام في الكود

| # | الحاجة | فين بالظبط | القيمة | الوجهة |
|---|---|---|---|---|
| 44 | خصم الإحالة المعروض للعضو | `src/lib/api.ts:86` (`REFERRAL_DISCOUNT = 0.15`) مستعمل في `:594` | `0.15` ثابت | `settings.referral_discount_pct` — الخادم بيستعمله صح في `pay/create:107`، بس العرض للعضو بيستعمل الثابت فممكن الرقمين يختلفوا |
| 45 | عدد أيام كارت الشغل | `src/components/work/PassCard.tsx:30` · `src/app/admin/shoghl/page.tsx:1819` | `four = 4` · `eight = 8` | `settings.work_pass4_sessions` · `work_pass8_sessions` |
| 46 | مدة صلاحية الكارت اليدوي (احتياطي) | `src/app/admin/shoghl/page.tsx:1804-1806` | `?? 10` و`?? 6` أسابيع | يقرا من `settings` من غير احتياطي ثابت (الأعمدة موجودة) |
| 47 | الأسعار الاحتياطية للشغل | `src/data/lists.ts:162-172` | `400 / 720 / 120 / 60` + رقم فودافون `010 0000 0000` + `nasbot@instapay` | مقبول كاحتياطي **بس** لازم يتطابق مع قيم الهجرة — ورقم فودافون الوهمي ده بالظبط اللي `DEPLOY_CHECKLIST §2` محذّر منه |
| 48 | حساب إنستاباي الثابت | `src/data/lists.ts:154` | `'nasbot@instapay'` | `settings.instapay_handle` (موجود وبيتقرا في مسار الدفع ✓ — الثابت ده تكرار خطر) |
| 49 | خيارات «كام مرة في الشهر» لنموذج الشركات | `src/data/lists.ts:198-203` | `1 / 2 / 4 / 8` | جدول `lead_options` أو `field_options` |
| 50 | حد الاستهلاك في المكان | `src/lib/api.ts:1449` | `60` (في البيانات الوهمية بس) | من `work_venues.min_consumption` ✓ (الحقيقي شغال) |
| 51 | مدة صلاحية رابط الصورة | `src/lib/api.ts:405` (`3600`) · `src/app/admin/payments/page.tsx:111` (`300`) | ثابتين ومختلفين | `settings.signed_url_seconds` |
| 52 | مدة كوكي الجلسة | `src/lib/session.ts:50` | `60*60*24*90` = ٩٠ يوم | `settings.session_days` |
| 53 | مدة الحدث في ملف التقويم | `src/lib/ics.ts:15` | `?? 2` ساعة | من `sbota_templates.duration_min` |

### سادسًا: حدود ومهل تشغيلية

| # | الحاجة | فين بالظبط | القيمة | الوجهة |
|---|---|---|---|---|
| 54 | عمر جلسة اللوحة | `src/lib/server/admin-auth.ts:44` | `4 * 60 * 60 * 1000` | `settings.admin_session_hours` |
| 55 | خمول اللوحة | `src/lib/server/admin-auth.ts:47` | `30 * 60 * 1000` | `settings.admin_idle_minutes` |
| 56 | حماية معدل الدخول | `src/lib/server/admin-auth.ts:53,55,57` | نافذة ١٥ د · ٥ لكل رقم · ٢٠ لكل IP | `settings.login_rate_*` |
| 57 | صلاحية رمز الدخول | `src/lib/server/otp.ts:22` | `10 * 60_000` | `settings.otp_ttl_minutes` |
| 58 | دفعة الإشعارات ومحاولاتها | `src/lib/server/notify.ts:27,29,31` | `BATCH = 20` · `MAX_ATTEMPTS = 3` · مهلة `22s` | `settings.notify_batch` · `notify_max_attempts` |
| 59 | مهلة نداءات الشغل | `src/lib/api.ts:1360` · `src/lib/work.ts:25` · `src/lib/collab.ts:24` | `8000` ثلاث مرات | ثابت في الكود مقبول — بس المفروض يتعرّف في مكان واحد |
| 60 | حدود «شغالين معاك» | `src/lib/collab.ts:398,400` | `COLLAB_MAX_SBOTAT = 3` · `COLLAB_MAX_CANDIDATES = 18` | `settings.collab_*` |
| 61 | مدة ظهور رسالة اللوحة | `src/components/admin-ui.tsx:52` | `3000` ms | كود (بس راجع A43) |

### سابعًا: نصوص ومحتوى مفروض يبقى في `copy_strings`

| # | الحاجة | فين بالظبط | ليه مهم |
|---|---|---|---|
| 62 | **الشريط المتحرك في الرئيسية** | `src/data/lists.ts:134` | فيه أسماء سبوطات وأماكن و«فاضل 3» و«كامل» — **بيانات وهمية معروضة لكل زائر**. المفروض تتولّد من `sbotat` الحقيقية. |
| 63 | نص الضمان | `src/data/lists.ts:124` | فيه «قبل **3 أيام**» مكتوبة بالإيد — لو المالك غيّر `refund_full_days` من اللوحة، الوعد المنشور هيكدب على السياسة الفعلية. معروض في ٥ صفحات. |
| 64 | القواعد الخمس | `src/data/lists.ts:95-121` | معروضة في `/rules` و`/s/[slug]` و`/shoghl/[slug]` — جدول `rules` أو مفاتيح `copy_strings` |
| 65 | ستيكرات القواعد في الرئيسية | `src/data/lists.ts:75-80` | نص + لون + زاوية ميل |
| 66 | ستيكرات قواعد كشف المجموعة | `src/data/lists.ts:83-92` | نفس الحكاية |
| 67 | «اللي حصل الجمعة اللي فاتت» | `src/data/lists.ts:138-145` | نصوص صور نايبة (`[صورة المجموعة الحقيقية — …]`) + توقيعات كباتن — **معروضة للزوار** |
| 68 | الاقتباس في الرئيسية | `src/data/lists.ts:148` | «رحت لوحدي ومحستش إني لوحدي.» — `copy_strings` |
| 69 | فلاتر الرئيسية العشرة | `src/data/lists.ts:7-18` | فيها أسماء مناطق ثابتة لازم تتطابق مع `field_options` |
| 70 | روابط وسطر الذيل | `src/data/lists.ts:128,131` | `copy_strings` |
| 71 | فلاتر الخريطة | `src/data/areas.ts:108` | `copy_strings` |
| 72 | عناوين إيميلات الشغل | `src/lib/server/notify.ts:167-176` | ٨ عناوين ثابتة — وواحد فيهم **«سبوطة شغل — أول مرة بـ 60»** فيه السعر مكتوب بالإيد، بيكدب لو `work_first_time_price` اتغيّر. الوجهة: `notification_templates.subject_ar` |
| 73 | مسارات الإيميلات الافتراضية | `src/lib/server/notify.ts:155-164` | `notification_templates.default_path` |
| 74 | قالب HTML للإيميل وسطر «وصلتك الرسالة دي لأنك…» | `src/lib/server/notify.ts:120-137` | `copy_strings` |
| 75 | اسم قالب واتساب للمصادقة | `src/lib/server/whatsapp.ts:22` | `process.env.WHATSAPP_TEMPLATE_AUTH ?? 'nasbot_auth_code'` — متغيّر بيئة، يعني تغييره محتاج نشر. الوجهة: `settings.whatsapp_template_auth` |
| 76 | أسماء قوالب واتساب لباقي الإشعارات | `notification_templates.provider_template_id` | ✅ **دي في القاعدة أصلًا وتمام** |
| 77 | الأنواع الستة مكررة في الكود | `src/data/personas.ts:9-52` | نسخة تانية من `personality_types` مستعملة في `api.ts:561,1153,1303` و`map-db.ts:283` — لو المالك غيّر اسم أو لون نوع من اللوحة، الأماكن دي مش هتشوف التغيير |
| 78 | كل مصفوفات الخريطة | `src/data/areas.ts:24,94,105,111,125,132,139,146` | راجع A5 |

---

## (ج) ثغرات سجل التدقيق

**اللي بيتسجّل فعلًا** (كله من دوال القاعدة، وكله `after` من غير `before`):

| الفعل | الدالة |
|---|---|
| إلغاء حجز | `fn_cancel_booking` |
| كشف المجموعات | `fn_reveal` |
| اعتماد/رفض تحويل | `fn_approve_transfer` |
| استرداد | `fn_issue_refund` |
| تعديل محفظة | `fn_wallet_adjust` |
| تشغيل مطابقة | `fn_build_matching` · `fn_build_work_matching` |
| صرف/إرجاع/تفعيل جلسة كارت | `fn_redeem_pass` · `fn_revert_pass` · `fn_activate_pass` |
| حذف حساب من العضو نفسه | `fn_soft_delete_profile` |

**اللي مالوش سجل خالص:**

*فلوس:* تسجيل «اتدفع» لمستحق مكان (`venue_reports.paid_at`) · إنشاء/تعديل/حذف كوبون ·
إصدار كارت شغل مجاني يدوي · تعديل `settings.vodafone_number` أو `instapay_handle` أو
`commission_pct`.

*حظر وعقوبات:* **الحظر وفك الحظر** (`profiles.banned_at` — التريجر بيمنع اللي مالوش صلاحية،
بس ما بيسجّلش) · إضافة/حذف ملاحظة سلوك · قرار على بلاغ (`reports.status`/`action`).

*أدوار:* إضافة عضو للفريق · تغيير دوره · إيقافه · **حذفه** · تصفير TOTP.

*إعدادات:* أي عمود في `settings` · أي مفتاح ميزة · **فتح/قفل وضع الصيانة** · تعديل
`allow_roles` · إضافة/حذف كلمة ممنوعة.

*محتوى:* أي نص في `copy_strings` (فيه `copy_history` بس ده تاريخ نص مش سجل تدقيق — ومفيش
فيه مين غيّر لما التغيير يعدّي من الاستبدال الجماعي) · أسئلة واختيارات ونقاط اللعبة ·
أنواع الشخصيات · حقول التسجيل واختياراتها · **نشر نسخة موافقة جديدة** (فعل قانوني) ·
قوالب السبوطات · قوالب الرسايل.

*تشغيل:* إنشاء/تعديل/نسخ/تكرار سبوطة · تغيير حالة سبوطة · تسجيل الحضور · ترقية أو شطب من
قايمة الانتظار · نقل شخص بين المجموعات يدويًا · إضافة/حذف مكان · قبول/رفض طلب كابتن ·
تسجيل وقفل تنبيه مكان · إعادة محاولة رسالة · تسجيل حملة.

**والعمود `before` فاضي في كل الصفوف** — يعني حتى الأفعال المسجّلة ما ينفعش تعرف منها
الحالة القديمة.

---

## (د) انحراف الوثائق عن الواقع

### `ADMIN_GUIDE.md`

1. **خريطة اللوحة (`:88-107`) مفيهاش `/admin/team`** — والقسم موجود وشغال ومهم (إضافة مديرين من غير SQL).
2. **«حقول التسجيل — نموذج التسجيل نفسه: الحقول والاختيارات والموافقات» (`:93`)** — غلط. القسم ما بيأثرش على `/join` خالص (A4).
3. **«الخريطة — مواقع الأماكن وأسماء المناطق» (`:104`)** — غلط. مش بيأثر على الخريطة العامة (A5).
4. **«الإعدادات — … مفاتيح المزايا، والصيانة» (`:105`)** — الاتنين مالهمش أثر (A2 · A3).
5. **«الرسايل — قوالب الواتساب، طابور الإرسال، والإرسال الجماعي» (`:102`)** — واتساب مش شغال، والطابور بيتفضّى لـ `work_*` بس، والإرسال الجماعي بيسجّل مسودة (A13 · A36).
6. **«اللي مالوش صلاحية قسم، القسم ده مش هيبان له أصلًا» (`:109`)** — صحيح للواجهة، بس بيوحي بحماية مش موجودة: القاعدة بتسمح لأي دور يكتب في `settings` و`bookings` و`coupons` وغيرهم (A10 · A11).
7. **«357 نص، مقسومين على 19 شاشة» (`:135`)** — العدد الحقيقي 543 مفتاح على 22 شاشة (A41).
8. **«لو التغيير مبانش: استنى دقيقة — أقصى تأخير 30 ثانية» (`:373`)** — صحيح للنصوص بس. للإعدادات ومفاتيح المزايا التغيير مش هيبان لا بعد دقيقة ولا بعد سنة.

### `README.md`

9. `:75-101` «البيانات الوهمية (البديل الاحتياطي) في `src/data/`» — الوصف ده صحيح لـ `game.ts` و`copy-fallback.ts` بس. `lists.ts` و`areas.ts` و`personas.ts` **مش احتياطي — دول المصدر الوحيد** لقوايم التسجيل والخريطة والأنواع.
10. `:182-197` «قواعد المنتج المتبنية في الكود» — لازم يتقال صراحة أنهي قواعد **قابلة للتعديل من اللوحة فعلًا** وأنهي لأ، وإلا الوصف ده هو بالظبط اللي بيخلي المالك يفتكر إن الإعدادات شغالة.

### `DEPLOY_CHECKLIST.md`

11. **`§7` «المفروض تشوف 8 مهام»** — الحقيقة 12 مهمة `nasbot-*` بعد هجرات الشغل، وجدول المهام مفيهوش `nasbot-work-recurring` ولا `nasbot-work-passes` ولا `nasbot-work-venues` ولا `nasbot-work-metrics` (A39).
12. **`§11` «مسارات `/api/cron/[job]`: لسه متعملتش»** — غلط: `/api/cron/work-notify` موجود و**إجباري**، والـ checklist مفيهاش خطوة تشغيل `WORK_CRON.sql` خالص. ده معناه إن اللي هيمشي على الملف حرفيًا هينشر بإشعارات شغل ميتة (A29).
13. **`§11` «لسه محتاج سطر `manifest` … في `layout.tsx`»** — موجود فعلًا (`src/app/layout.tsx:30`). و«من غير أيقونات» برضه قديم: `src/app/icon.png` و`apple-icon.png` موجودين والـ manifest بيشاور عليهم.
14. **`§3` جدول المتغيرات** — مفيهوش `RESEND_API_KEY` كصف مستقل رغم إن `notify.ts` بيفضّله على SMTP، ومفيهوش تنبيه إن **`NEXT_PUBLIC_SITE_URL` بيدخل في روابط الإيميلات** (`notify.ts:149`) فلو غلط الناس هتوصلها روابط مكسورة.
15. **مفيش خطوة تقول «غيّر الدومين في `WORK_CRON.sql`»** — الملف مثبّت على `nasbot.vercel.app` بينما `§8` بيتكلم عن `nasbot.app`.

### `RUNBOOK.md`

16. **`§9` «دوال الحافة» `match` و`notify`** — **مش موجودين**. مفيش مجلد `supabase/functions` أصلًا (A38).
17. **`§10` «من غير `WHATSAPP_TOKEN` النظام بيشتغل وضع محاكاة … وبيعلّم الإشعار `sent`»** — غلط تمامًا. الصفوف بتفضل `queued` للأبد. الوصف ده هو اللي بيخلي حد يفتكر إن A13 مش مشكلة.
18. **`§9` جدول المهام** — ٨ مهام بس، ناقص الأربعة بتوع الشغل و`nasbot-work-notify`.
19. **`§4` «إضافة سبوطة» بـ SQL** — لسه صحيحة تقنيًا، بس بقت غير ضرورية: `/admin/sbotat` بيعمل نفس الحاجة بواجهة. لازم يتقال إن الـ SQL للطوارئ بس.
20. **`§11` «محوّلش → `nasbot-expire` بيلغيه لوحده بعد ساعة»** مقابل **`§9` «إلغاء الحجوزات اللي مدفعتش في 15 دقيقة»** — تناقض جوه نفس الملف. الحقيقي: `expires_at` بيتحط ساعة في `pay/create:147`، والمهمة بتشتغل كل ٥ دقايق.
21. **`§13` المراقبة السريعة** — كلها استعلامات يدوية. لازم يتقال بصراحة **«مفيش مراقبة تلقائية ولا تنبيهات»** (A30).

### `ADMIN_PLAN.md`

22. **`§4` الوسوم الستة (`copy` · `game` · `templates` · `settings` · `flags` · `fields`)** — واحد بس اتعمل (A14).
23. **`§5` «كل Server Action بتبدأ بـ `requirePermission`»** — مفيش Server Actions في المشروع (A20).
24. **`§6` المنطقة `fra1` و`verify` = typecheck + lint + build + test** — `vercel.json` مفيهوش `regions` و`verify` مفيهوش lint ولا test (A40).
25. **`§3` `/admin/content` «+ معاينة حية»** — مش موجودة (A44).
26. **`§3` `/admin/payments` «… المستحقات»** — التبويب مش موجود (A25).
27. **الخطة مفيهاش `/admin/shoghl` ولا `/admin/team`** — القسمين اتبنوا بعدها. محتاجة سطر يضمّهم.

---

## (هـ) شغال تمام

مش كل حاجة مكسورة — دي الحاجات اللي اتعملت صح ومحتاجة تفضل زي ما هي:

- **اللعبة موصولة من الآخر للآخر.** `/admin/game` بيكتب في القاعدة، و`src/lib/api.ts:1238`
  بيقرا منها، و`src/data/game.ts` احتياطي حقيقي. ده النموذج اللي المفروض باقي الأقسام تتعمل بيه.
- **النصوص.** `copy_strings` + `unstable_cache` بوسم `copy` + `revalidateCopy()` +
  `copy-fallback.ts` — الموقع بيفضل عربي سليم حتى من غير قاعدة. وتاريخ النص والرجوع لنسخة قديمة شغالين.
- **الفلوس اليدوية.** التأكيد والرفض عبر `fn_approve_transfer` بس (الحجز والدفعة بيتحركوا مع بعض)،
  والاسترداد عبر `fn_issue_refund` بفحص `payments.refund` جوه القاعدة، والمحفظة عبر
  `fn_wallet_adjust` بفحص `wallet.credit` ومنع الرصيد السالب. ده الجزء الأنضف في المشروع.
- **الإيصالات** برابط موقّع مؤقت (٥ دقايق) مش رابط عام.
- **كل الفلوس بالقروش** في القاعدة، وكل عرض بيعدّي على `money()` وكل إدخال بيتضرب في ١٠٠ —
  ملتزمين بيها في كل الصفحات من غير استثناء.
- **قسم الشغل كله (`/admin/shoghl`)** بيستعمل `.select()` بعد كل كتابة ويفحص `rejected(data)` —
  ٨ تبويبات بنمط واحد صح. الدالة المساعدة `rejected` في `page.tsx:338` هي اللي المفروض تتعمّم.
- **`/admin/team`** بيفحص الرفض، وبيمنع شيل آخر مالك نشط، وبيصفّر TOTP بأمان.
- **`/admin/reports`** أنضف صفحة قرارات: كل تصرّف بيرجّع رسالة الرفض الصح، والملاحظة إجبارية.
- **الصفحة الرئيسية** بتستعمل `count: 'exact', head: true` بدل ما تجيب الصفوف، وكل تايل
  الدور ما بيقراش جدوله بيرجّع `—` بدل ما يوقّع الصفحة. تصميم دفاعي ممتاز.
- **إدارة الجلسات** (٤ ساعات · خمول ٣٠ دقيقة · TOTP إجباري · حماية معدل على الدخول بالرقم
  وبالـ IP · منع إعادة استخدام خطوة TOTP) مكتوبة بالكامل وصح في `admin-auth.ts`.
- **الميدل وير** محدود على `/admin/:path*` بحزام أمان تاني جواه، وبيستعمل `getUser()` مش
  `getSession()`.
- **`fn_admin_run_work_recurring` · `fn_admin_build_venue_report` · `fn_admin_refresh_work_metrics`** —
  الأغلفة الثلاثة في الهجرة `0050` اتعملت صح بعد ما اتكشف إن `job_work_recurring` كانت
  `service_role` بس. **المراجعة الحالية لقيت حالة واحدة بس من نفس النوع لسه مفتوحة: `fn_reveal` (A1).**
- **CSV تقرير المكان** مفيهوش ولا اسم عضو ولا سعر الكرسي بالجملة — أرقام مجمّعة بس. قرار خصوصية صح.
- **البناء نضيف:** `npx next build` عدّى من غير تحذيرات، وكل صفحات اللوحة static وأحجامها معقولة.
