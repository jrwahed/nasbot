# REVIEW.md — المراجعة الشاملة لـ «نسبوط»

> مراجعة اكتشاف بس — **مفيش حاجة اتصلّحت**. اتعملت بـ ٤ مراجعين متخصصين،
> اتنين منهم شغّلوا Postgres حقيقي وطبّقوا الـ 71 هجرة واختبروا فعلًا،
> وواحد أخد 118 لقطة شاشة وبصّ عليها. التفاصيل الكاملة في أربع ملفات:
> `REVIEW_SECURITY.md` (S) · `REVIEW_DB.md` (D) · `REVIEW_ADMIN.md` (A) · `REVIEW_UI.md` (U).
>
> الملف ده الملخّص التنفيذي: أخطر الحاجات، الأحمر كامل، وقايمة «المفروض من اللوحة».

## العدّاد

| المصدر | 🔴 | 🟠 | 🟡 | 🔵 | إجمالي |
|---|---|---|---|---|---|
| الأمان (S) | 8 | 12 | 9 | 3 | 32 |
| القاعدة والفلوس (D) | 7 | 14 | 11 | 2 | 34 |
| اللوحة (A) | 17 | 18 | 8 | 2 | 45 |
| الواجهة والهوية (U) | 5 | 15 | 14 | 5 | 39 |
| **الإجمالي** | **37** | **59** | **42** | **12** | **150** |

> بعض النتايج اتكررت عند أكتر من مراجع (تأكيد مستقل): الإشعارات الواقفة (D5=A13)،
> و`fn_is_admin` الواسعة (S8=A10=A11)، وثغرة التبادل (S1/S2 = الباج اللي صلّحناه في api بس القاعدة لسه مفتوحة).

---

## أخطر ٥ حاجات — لازم تتصلّح قبل ما توزّع اللينك

**١. 🔴 ثغرة سحب فلوس (D1).** حجز مدفوع من الرصيد (كاش داخل = صفر) + إلغاء بدري = الموقع بيرجّع **٣٠٠ جنيه كاش** والرصيد ما بيرجعش. متكرر. أي عضو يحوّل رصيده لكاش. **ليه النهارده:** خسارة فلوس مباشرة.

**٢. 🔴 تزوير التبادل (S1/S2).** سطر SQL واحد من المتصفح بيدي أي عضو: صورة أي حد + تليفونه + إيميله + شات خاص — من غير موافقته. الدوال الصح موجودة بس الباب المباشر على `pair_affinity`/`work_affinity` مفتوح جنبها. **ليه النهارده:** انتهاك خصوصية وأمان شخصي.

**٣. 🔴 حجز ببلاش (S4) + فلوس سالبة (D7).** أي عضو يدخل `status='paid'` و`price_paid=0` من غير دفع. ومفيش قيد يمنع `amount = -500000`. **ليه النهارده:** كسر كامل لنظام الفلوس.

**٤. 🔴 الكشف مكسور (A1).** `fn_reveal` مش مصرّحة للمستخدمين — زرار «اعتمد التوزيع واكشفه» بيفشل في كل مرة. **ليه النهارده:** المجموعة عمرها ما هتتكشف، والمنتج كله بيقف عند الخطوة دي.

**٥. 🔴 دور support يتحكم في الفلوس (S8/A10/A11).** `fn_is_admin()` بقت «أي صف نشط في admin_users»، فأي دور — حتى support — يقدر يغيّر الأسعار ورقم فودافون كاش ويعتمد التحويلات. **ليه النهارده:** أي حد تضيفه للفريق بأقل دور يقدر يسرق.

---

## الأحمر كامل (٣٤ بند)

### أمان وخصوصية
| # | المشكلة | فين | من اللوحة/كود |
|---|---|---|---|
| S1 | تزوير التبادل — `pair_insert_own` بتقبل أي صف طول ما طرف واحد = أنا | `0009_rls.sql:157` | كود (هجرة) |
| S2 | نفس الثغرة في `work_affinity` + إشعار كداب | `0043:60` | كود (هجرة) |
| S3 | `profiles_mutual_read` بتفتح صف profiles كامل (تليفون·إيميل·رصيد) | `0009:` | كود (هجرة) |
| S4 | حجز مدفوع ببلاش — `bookings_own_insert` بتتحقق من profile_id بس | `0009:129` | كود (هجرة) |
| S5 | `captains` مقروء للزوار بما فيه `payout_method` | `0009:99` | كود (هجرة) |
| S6 | «حذف الحساب» بيعمل signOut وبس — `fn_soft_delete_profile` محدش بينديها | `me/page.tsx:278` | كود |
| S7 | عدّاد محاولات OTP بيتقرا-ويتكتب مش ذرّي + مفيش حد IP | `api/otp/verify` | كود |
| S8 | دوال الفلوس بتتحقق بـ `fn_is_admin` مش بالصلاحية | `admin-auth` + هجرات | كود (هجرة) |

### فلوس وقاعدة
| # | المشكلة | فين | من اللوحة/كود |
|---|---|---|---|
| D1 | الرصيد بيترد كاش عند الإلغاء المبكر | `fn_cancel_booking` + `pay/create:142` | كود |
| D2 | `coupons.used_count` عمره ما بيتزوّد — max_uses ديكور | `pay/create` | كود |
| D3 | `first_booking_only` عمره ما اتفحص | `pay/create` | كود |
| D4 | `job_purge` مستحيل ينجح (بيخالف قيد الرقم) — المحو ما بيحصلش | `job_purge` هجرة | كود (هجرة) |
| D5 | إشعارات غير الشغل عمرها ما بتتبعت | `notify.ts:257` | كود |
| D6 | `job_expire_bookings` بيلغي حجز صورته تحت المراجعة | هجرة | كود (هجرة) |
| D7 | مفيش قيد يمنع الفلوس السالبة (`amount=-500000` اتقبل) | جداول payments/bookings | كود (هجرة) |

### اللوحة
| # | المشكلة | فين | من اللوحة/كود |
|---|---|---|---|
| A1 | `fn_reveal` مش مصرّحة لـ authenticated — الكشف بيفشل | `0011`/`0028` + `matching:805` | كود (هجرة) |
| A2 | `feature_flags` محدش بيقراها في الموقع | `/admin/settings` | كود |
| A3 | زرار الصيانة بيكتب في جدول محدش بيقراه — الموقع بيفضل شغال | `/admin/settings` + middleware | كود |
| A4 | حقول التسجيل بتتعدّل والقاعدة، والتسجيل بيقرا من الكود | `/admin/profile-fields` + `lists.ts` | كود |
| A5 | `/admin/map` بيعدّل، والخريطة العامة من الكود | `/admin/map` + `areas.ts` | كود |
| A6 | حفظ الإعدادات من غير فحص رفض RLS — «اتحفظ ✓» كدابة | `/admin/settings` | كود |
| A7 | نفس المشكلة في محرر النصوص كله | `/admin/content` | كود |
| A8 | نفس المشكلة في كل محرّر اللعبة | `/admin/game` | كود |
| A9 | نفس المشكلة في ١٣ موضع بـ profile-fields (منهم «انشر نسخة موافقة») | `/admin/profile-fields` | كود |
| A10 | سياسة `settings` بـ `fn_is_admin` مش بالصلاحية | هجرة | كود (هجرة) |
| A11 | نفس الحكاية على ٩ جداول تانية | هجرة | كود (هجرة) |
| A13 | الطابور بيتصرّف لـ work% بس (= D5) | `notify.ts` | كود |
| A15 | `emergency_phone` بيتعدّل ومحدش بيقراه | `/admin/settings` | كود |
| A16 | ١٦ عمود settings معروضين وقابلين للتعديل ومحدش بيقراهم | `/admin/settings` | كود |
| A17 | ١٠ صفحات بتحمّل الجدول كله في المتصفح | جدول تحت في REVIEW_ADMIN | كود |

### الواجهة
| # | المشكلة | فين | من اللوحة/كود |
|---|---|---|---|
| U1 | تميمة LiveQ بتغطي المحتوى والزر الرئيسي على كل صفحة | `layout.tsx` + `LiveQ` | كود |
| U2 | مفيش error.tsx — «Application error» إنجليزي بيظهر | `src/app` | كود |
| U3 | `/join` فيها باسورد وتحته «مفيش باسورد» — تناقض | `join/page.tsx` | كود+لوحة |
| U4 | «انقليني لمجموعة بنات بس» عمرها ما بتظهر (isGirl ثابت false) | `my/[bookingId]/page.tsx:28` | كود |
| U5 | `/shoghl/pass` زر دفع من غير صندوق ضمان | `shoghl/pass` | كود |

---

## البرتقالي — ملخّص (٥٩ بند، التفاصيل في ملفات الـ REVIEW_*)

أهمهم المتكرر أو المؤثر على الفلوس/التجربة:
- **D8** حجوزات الكارت والحجز المتكرر بتتخطّى خصم الرصيد وتسجيل الإحالة وإشعار التأكيد (محفّز AFTER UPDATE بس).
- **الفهارس الناقصة** (D): ٦ أعمدة بتعمل Seq Scan عند ١٠ آلاف صف + ٥٥ مفتاح أجنبي من غير فهرس.
- **التباين** (U): «حذف الحساب» 2.22 · تلميح /join 2.50 · الشعار 2.95 — تحت الحد.
- **انحراف الهوية** (U): «رايحين معاك» في /me شكلها قايمة تعارف · «مفيش عضوية» مش موجودة في /rules · المارکي في الرئيسية بيناقض الكروت تحته.
- **الأداء** (A): ١٦ شاشة لوحة بتحمّل كل الصفوف في المتصفح.
- **سجل الحركة** (A): فجوات في تسجيل الأفعال الحساسة (فلوس، حظر، تغيير أدوار، تعديل إعدادات).

---

## «ده كان المفروض من اللوحة» — القايمة اللي هتوفّر عليك مطوّر

المراجعة لقت **٧٨ حاجة متحطوطة في الكود المفروض تتعدّل من اللوحة**. أهم المجموعات (القايمة الكاملة في `REVIEW_ADMIN.md` قسم ب):

1. **نص الضمان والقواعد الخمسة والفوتر و«مفيش عضوية»** — كلهم في `src/data/lists.ts` مش في `copy_strings`، فمش قابلين للتعديل من `/admin/content`. (U + A)
2. **المارکي بتاع الرئيسية** — نص ثابت في `lists.ts` بيسمّي ٣ سبوطات بأرقام أماكن.
3. **قوايم الانضمام** (المناطق · الاهتمامات · الأيام · الميزانيات · المستويات) في `lists.ts` — واللوحة فيها قسم بيعدّل جداول موازية محدش بيقراها (A4).
4. **إحداثيات الخريطة** في `areas.ts` — واللوحة بتعدّل `venues` (A5).
5. **١٦+٥ عمود في `settings`** معروضين وموصولين بلا شيء (العمولة · السن الأدنى · ساعات الشات · رقم الطوارئ · نوافذ الاسترداد…). (A15/A16)
6. **`feature_flags` وزرار الصيانة** — بيتكتبوا ومحدش بيقراهم (A2/A3).
7. **أسئلة اللعبة ونقاطها** — محررها في اللوحة بيكتب بصمت من غير تأكيد (A8).

---

## «شغال تمام» — علشان تعرف إيه اللي مش محتاج تقلق منه

- **التزامن سليم:** ٢٠ حجز متوازي على آخر مكان → واحد بس نجح، السعة اتحفظت (D).
- **الـ 67 جدول كلهم عليهم RLS مفعّل**، والـ 69 دالة `security definer` كلهم عليهم `search_path` (S).
- **سعر الجملة** (`wholesale_price`/`wholesale_seat_price`) مش قابل للوصول من أي مسار عضو — اتتبّع وثبت (S).
- **مفيش أسرار في المستودع ولا في تاريخ جيت** (S).
- **دخول اللوحة تحقّق بخطوتين حقيقي** مع منع تكرار وانتهاء جلسة وربط كوكي (S).
- **الدفع المكرّر و`fn_approve_transfer` المكرّر** ما بيتحسبوش مرتين (D).
- **تمرير أفقي 360: نضيف 38/38 صفحة.** الكلمات الممنوعة: صفر. الحركة بتحترم `prefers-reduced-motion` (U).
- **حسبة السعر الأساسية** (`price − discount = price_paid = payments.amount`) متقاسة وصح (D).
- **ثغرة `fn_venue_report`** اللي اكتشفناها امبارح متسدّة ومعاها اختبار (S/D).
