# REVIEW_UI.md — مراجعة الواجهة والهوية · نسبوط

مراجعة بصرية على **البناء الحقيقي** (`next build` + `next start -p 3900`)، مقودة بـ Playwright.
**118 لقطة** على `390 · 768 · 1440` في الوضعين، محفوظة في
`/tmp/claude-0/-home-user-nasbot/d7d4d29f-a528-5c49-a409-b10e9587977a/scratchpad/shots/`
واتفتحت وشُفت بعينيّ واحدة واحدة. كل صف تحت جاي من **لقطة** إلا اللي مكتوب جنبه «(من الكود مش من اللقطة)».

**ملاحظة عن البيانات:** البناء الأول كان بالمفاتيح اللي في التاسك (رابط سوبابيس حقيقي + مفتاح مزيّف).
`hasSupabase` بتبص على **وجود** المتغيرين بس (`src/lib/supabase.ts:24`)، فالتطبيق **ما رجعش** للبيانات الوهمية —
راح للقاعدة الحقيقية وفشل، والصفحات طلعت فاضية (ده نفسه بقى الصف **U14**، ولقطاته في `shots/dbfail/`).
علشان أراجع التخطيط، بنيت تاني **من غير** متغيرات سوبابيس ← `hasSupabase=false` ← `src/lib/api-mock.ts`.
اللقطات الـ118 كلها من البناء ده. المسارات اللي مصدرها الوهمي فيه صف واحد بس (زي `/shoghl/amaken`) مذكورة كده، مش كعيب.

---

## الجدول

| # | الخطورة | المجال | المشكلة | فين بالظبط | الأثر | الإصلاح المقترح | من اللوحة ولا كود؟ | الوقت |
|---|---|---|---|---|---|---|---|---|
| U1 | 🔴 | تخطيط | علامة الاستفهام الحية `LiveQ` مثبتة `position:fixed; left:16; bottom:16; z-40` على **كل** صفحة، فبتقعد فوق المحتوى. أسوأ حالة: في `/s/[slug]/pay` هي **مغطية رقم الـ3** في «لو أنت لغيت قبل ‪3‬ أيام» جوه صندوق الضمان — يعني بتخفي شرط الاسترجاع نفسه | `src/components/LiveQ.tsx:88-93` + `src/app/layout.tsx:58` · لقطة `extra_pay__390__night.png` (390 · ليلي) · وكمان `home__390__night.png` فوق زر «أنا جاي»، `game__390__night.png` فوق «اللي بعده»، `rules__390__night.png` فوق بطاقة القاعدة 5، `s-mystery`, `/me`, `/captains`, `/join`, `/chat` | الوعد الأساسي (الضمان) بيتقرا ناقص · الأزرار الرئيسية مغطّاة جزئيًا | خلي `z-index` أقل من `StickyCTA` (z-20 بدل z-40)، وزوّد `bottom` لفوق الشريط اللاصق، وأضف `padding-inline-start: 60px` على الحاويات اللي فيها نص جنبها. الأنضف: خليها تختفي (`opacity:0`) لما يبقى في `StickyCTA` أو صندوق ضمان في الشاشة | كود — `src/components/LiveQ.tsx` | 45 د |
| U2 | 🔴 | أخطاء | مفيش `error.tsx` ولا `global-error.tsx` ولا `loading.tsx` في `src/app` خالص. أي استثناء في العميل بيطلّع شاشة Next الافتراضية: **«Application error: a client-side exception has occurred»** — إنجليزي، على خلفية بيضا-كريمي، بتتجاهل `data-theme`، من غير شعار ولا طريق رجوع | حصلت فعلًا مرتين في الجولة: `not-found__390__night.png` و`shoghl-amaken__390__night.png` (390 · ليلي). التأكيد من الكود: `find src/app -name 'error.tsx'` بترجع فاضي | العضو بيشوف رسالة إنجليزية تقنية على موقع عربي بالكامل — كسر كامل للثقة اللي الموقع مبني عليها | ضيف `src/app/error.tsx` و`src/app/global-error.tsx` بنفس شكل `NotFoundView`: «؟» + سطر عامية + زر «رجّعني للرئيسية». النص من `copy_strings` بمفتاح جديد `error.crash.*` | كود (المكوّن) + من اللوحة: المحتوى ← `error.crash.*` بعد ما تتضاف | 1 س |
| U3 | 🔴 | نبرة/هوية | نفس الشاشة بتقول حاجتين متضاربتين: خطوة 1 عنوانها «رقمك» وفيها **حقل «الباسورد (٦ حروف أو أكتر)»**، وتحت الزر اللاصق بالظبط مكتوب **«مفيش باسورد. رقمك هو دخولك.»** — والـOTP على واتساب اللي في `COPY.md §4` مش موجود أصلًا | `/join` 390 ليلي — لقطة `join__390__night.png` (شريحة 00 وشريحة 02) · `src/app/join/page.tsx:381-390` · مفاتيح `join.label.password` · `join.text.password` · `join.text.1` | العضو بيقرا كذبة على الشاشة نفسها. ودي مش تفصيلة — «رقمك هو دخولك» هي وعد المنتج | قرار منتج أولًا: يا إما ترجع الـOTP، يا إما تشيل «مفيش باسورد» وتكتب سطر صادق (مثلًا «إيميل وباسورد، وبعدين رقمك للكابتن بس»). الأسرع دلوقتي: عدّل `join.text.1` علشان ما يكدّبش الحقل اللي فوقه | من اللوحة: المحتوى ← `join.text.1` (والقرار الكامل كود) | 20 د للنص · 3 س للـOTP |
| U4 | 🔴 | هوية/بنات بس | زر «انقليني لمجموعة بنات بس» **عمره ما بيظهر لحد**: الشرط `isGirl` معرّف `useState(false)` وما بيتحطّش بأي قيمة في أي مكان في الملف. جرّبت بجلسة `gender: female` ورا بعضها — الزر مش موجود | `src/app/my/[bookingId]/page.tsx:28` و`:200` · لقطة `myb_girls.png` (390 ليلي، جلسة بنت) · النص موجود ومستني في `group.text.1` | `/rules` بيوعد «أي بنت في أي مجموعة تانية تقدر تطلب تتنقل لمجموعة بنات بس **في أي وقت**» — والوعد ده مالوش زرار في المنتج كله. «بنات بس» بقت خيار مخفي مش طقس | اربط `isGirl` بنوع الجلسة (`getSession()?.gender === 'بنت'`) أو بـ `getMe()`. `requestGirlsOnly()` جاهزة أصلًا في `src/lib/api.ts` | كود — `src/app/my/[bookingId]/page.tsx` | 20 د |
| U5 | 🔴 | هوية/ضمان | `/shoghl/pass` فيها زر دفع **«اشتري الكارت»** من غير أي `GuaranteeBox` قبله. كلام الاسترجاع مدفون جوه سؤال في الأسئلة («لو ألغيت حجز؟») على بعد شاشة كاملة فوق. باقي شاشات الدفع (`/s/[slug]`, `/s/[slug]/pay`, `/shoghl/[slug]`) الضمان فيها موجود صح | `/shoghl/pass` 390 نهاري — لقطة `shoghl-pass__390__day.png` (شريحة 01) | القاعدة «الضمان قبل كل زر دفع» مكسورة على أغلى زر في الموقع (720 جنيه) | حط نفس `<GuaranteeBox>` بتاع `/s/[slug]` فوق «اشتري الكارت» بنص مخصوص للكارت | كود (المكوّن) + من اللوحة: المحتوى ← `shoghl.pass.guarantee` بعد ما يتضاف | 30 د |
| U6 | 🟠 | نماذج/قانوني | مربع الموافقة «موافق على **القواعد الخمس**» بييجي **متعلّم افتراضيًا** (`useState(true)`). الموافقة التانية (البيانات) فاضية — يعني الفرق مقصود مش سهو | `src/app/join/page.tsx:214` · لقطة `join__390__night.png` شريحة 02 | موافقة مسبقة على القواعد = موافقة مش حقيقية. وده بالظبط عكس «القواعد دي مش كلام — دي شرط الحجز» | خلّيه `useState(false)` زي التاني | كود — `src/app/join/page.tsx:214` | 2 د |
| U7 | 🟠 | الوضعان | `prefers-color-scheme` **متجاهَل تمامًا**. الوضع بيتحدد من `localStorage` أو من **ساعة الجهاز** بس. اختبرت: بمتصفح على `prefers-color-scheme: dark` الساعة 11 الصبح ← الموقع نهاري. ومن غير جافاسكريبت مفيش `data-theme` أصلًا فبيقع على `:root` = نهاري دايمًا | `src/lib/theme.ts:29-33` · `src/app/globals.css:27-46` (مفيش أي `@media (prefers-color-scheme: dark)`) · مقاس بـ Playwright `colorScheme:'dark'` ← `data-theme="day"` | اللي مفعّل الوضع الليلي على جهازه لسبب طبي (حساسية ضوء) بياخد شاشة كريمي بالنهار غصب عنه | في `themeInitScript` خلي الترتيب: المحفوظ ← `matchMedia('(prefers-color-scheme: dark)')` ← الساعة. وضيف بلوك `@media (prefers-color-scheme: dark){ :root:not([data-theme='day']){…} }` في `globals.css` علشان حالة بدون جافاسكريبت | كود — `src/lib/theme.ts` + `src/app/globals.css` | 40 د |
| U8 | 🟠 | محتوى | الشريط المتحرك في الرئيسية **نص ثابت مكتوب في الكود** بيسمّي 3 سبوطات بأرقامها («إحنا الرابع · فاضل 3»). في الوضع النهاري البطاقات المعروضة بقت «سبوطة شغل» و«شروق من الجبل» — والشريط لسه بيقول نفس التلاتة القدام. يعني الشريط بيكذّب اللي تحته على نفس الشاشة | `src/data/lists.ts:135` (`marqueeText`) · لقطة `home__390__day.png` شريحة 00 — قارن الشريط بالبطاقة اللي تحته | «فاضل 3» رقم كذّاب فوق كل الصفحة الرئيسية. أول حاجة تكسر ثقة العضو | ولّد نص الشريط من نفس `getSbotat()` اللي البطاقات بتقرا منها | كود — `src/data/lists.ts` + `src/components/Marquee.tsx` | 1 س |
| U9 | 🟠 | انحراف: تطبيق تعارف | قسم «رايحين معاك» في `/me` معروض كـ **قايمة أشخاص مفردة**: دايرة صورة + اسم + نوع + زرّين «**ابعتله**» و«**احجزوا سوا**» — تلات صفوف، كلهم بنات لعضو راجل. ده الشكل الحرفي لقايمة الماتشات في تطبيق تعارف، مش شكل نادي خروجات | `/me` 390 ليلي — لقطة `me__390__night.png` شريحة 00 | حتى لو المنطق ورا الشاشة سليم (اختيار متبادل بعد اللقاء)، **الشكل** بيقرا تعارف. ودي أخطر نقطة في المنتج كله | صغّر البصمة: صف واحد نصي بدل بطاقة لكل شخص، شيل الصورة الدايرية من هنا، وخلي الفعل الأساسي «احجزوا سوا» (مجموعة) والرسالة تانوية جوه الشات. وسمّي القسم بحاجة تخص السبوطة مش الشخص | كود — `src/app/me/page.tsx` + من اللوحة: المحتوى ← `me.*` لعناوين القسم | 2 س |
| U10 | 🟠 | تباين | «حذف الحساب» بلون `#8E2F1F` على `#14161A` = **2.22:1** (المطلوب 4.5). الملف عنده `--err-text: #F2A192` للوضع الليلي بس الزر ده مش بيستخدمه | `/me` 390 ليلي — لقطة `me__390__night.png` شريحة 01 · قياس: `#8e2f1f on #14161a` | إجراء خطير (حذف حساب) شبه غير مقروء في الوضع الليلي | استبدل اللون الصريح بـ `var(--err-text)` | كود — `src/app/me/page.tsx` | 5 د |
| U11 | 🟠 | تباين | سطر المساعدة تحت حقل الباسورد بلون `#55575C` (وده `muted-day`) على `#14161A` = **2.50:1** — يعني لون الوضع النهاري مستخدم في شاشة ليلي | `/join` 390 ليلي — لقطة `join__390__night.png` شريحة 00، تحت الحقل التالت · النص `join.text.password` | تعليمات الدخول مش مقروءة بالليل | استخدم `var(--muted)` بدل `#55575C` | كود — `src/app/join/page.tsx` (خاصية `hint` في `Field`) | 10 د |
| U12 | 🟠 | تخطيط | على **1440** البطاقات بتبقى عمودين جوه 40% من العرض، فبطاقة السبوطة بتضيق و**«300 جنيه» بتتقطع على سطرين** («300» فوق و«جنيه» تحت) والزر بيتزنق جنبها | `/` 1440 ليلي ونهاري — لقطة `home__1440__night.png` (تكبير `slices/price1440.png`) · `src/components/SbotaCard.tsx:88-91` | السعر — أهم رقم في البطاقة — مكسور على أوسع شاشة | زوّد `whitespace-nowrap` على حاوية السعر، وخلي صف السعر/الزر `flex-wrap` مع `gap` أو نزّل الحد الأدنى لعرض العمود | كود — `src/components/SbotaCard.tsx` | 15 د |
| U13 | 🟠 | حالات فاضية | `/me` و`/my/[bookingId]` **مالهومش حالة «مش داخل»** ولا حارس دخول. زائر من غير جلسة بيشوف صفحة ملف كاملة باسم ورصيد وكود إحالة وكشف مجموعة بسبعة أسامي. `/me/shoghl` لوحدها بتعمل `router.replace('/login')` | جربت بسياق متصفح من غير كوكي: `/me` و`/my/b1` رجعوا 200 وعرضوا كل حاجة (`slices/loggedout__me.png`) · `src/app/me/page.tsx` مفيهاش `isLoggedIn` خالص، قارنها بـ `src/app/me/shoghl/page.tsx:91` | مع القاعدة الحقيقية الصفحات دي هتطلع **فاضية من غير أي رسالة** بدل ما تقول «ادخل الأول» | ضيف نفس الحارس اللي في `/me/shoghl` للصفحتين | كود — `src/app/me/page.tsx` + `src/app/my/[bookingId]/page.tsx` | 20 د |
| U14 | 🟠 | حالات فاضية | لما `getSbotat()` تفشل (قاعدة واقعة / مفتاح غلط) بترجّع `[]`، والرئيسية بتعرض **«مفيش سبوطة بالفلتر ده الأسبوع ده. جرب فلتر تاني.»** — رسالة بتلوم الفلتر على عطل شبكة. وفي الحالة دي حتى شريط الفلاتر نفسه بيختفي، فالعضو مالوش أي فلتر يجربه | `src/lib/api.ts:121` (`if (error \|\| !data) return []`) · مفتاح `home.text.1` · لقطة `shots/dbfail/home.png` (390 ليلي، البناء بالمفتاح المزيّف) | العضو بيلف على الفلاتر ومش لاقي حاجة، والموقع مش بيقول إن في عطل | فرّق بين «مفيش نتيجة» و«مقدرناش نجيب»: خلي طبقة البيانات ترجّع خطأ صريح، وأضف نص تالت «الشبكة تعبانة — جرّب تاني» بزر إعادة | كود (`src/lib/api.ts`) + من اللوحة: المحتوى ← `home.text.1` والمفتاح الجديد | 1 س |
| U15 | 🟠 | تنقّل | رابط «الشغل» في الرأس بيظهر في **الوضع النهاري بس** (`showWork = theme === 'day'`). في الوضع الليلي مفيش أي مدخل لـ `/shoghl` من أي صفحة | `src/components/Header.tsx:26` · قارن `home__390__day.png` (فيه «الشغل») بـ `home__390__night.png` (مفيهاش) | نص المنتج بيختفي على نفسه من الساعة 5 المغرب للـ5 الصبح. حد بيخطط لأسبوعه بالليل مش هيعرف إن في سبوطات شغل أصلًا | سيب الرابط ظاهر دايمًا (لونه ممكن يبقى تانوي بالليل)، أو على الأقل حطّه في الذيل | كود — `src/components/Header.tsx` | 10 د |
| U16 | 🟠 | انحراف: موقع تذاكر | `/shoghl/pass` صفحة **باقتين بأسعار وشارات «وفر 80 جنيه» / «وفر 240 جنيه»** وزر «اشتري الكارت» — من غير كابتن، من غير مجموعة، من غير أي كلمة عن الناس. وتحتيها بالظبط سطر الذيل «مفيش عضوية» | `/shoghl/pass` 390 نهاري — لقطة `shoghl-pass__390__day.png` (الشريحتين) | الشكل ده = موقع تذاكر/اشتراك. والتناقض مع «مفيش عضوية» مرئي في نفس اللقطة | خلي الكارت يتقال بلغة النادي مش المتجر: بدل «وفر 240 جنيه» ← «اليوم بـ 90 بدل 120»، وحط سطر كابتن وصورة ترابيزة فوق الأسعار، وسطر صريح «الكارت مش عضوية — رصيد أيام وبس» | من اللوحة: المحتوى ← `shoghl.pass.*` · والأسعار من `/admin/settings` (أعمدة `work_*`) | 1 س |
| U17 | 🟡 | لمس | روابط الذيل الخمسة ارتفاعها **23.8px** (قِيست بـ `getBoundingClientRect`) — أقل من نص الحد الأدنى 44px. ظاهرة في كل صفحة فيها ذيل | `src/components/Footer.tsx:24-30` · مقاس على `/` و`/rules` و`/me` و`/shoghl` عند 390 | صعب تدوسها بصباعك، وهي المدخل الوحيد لـ«بقى كابتن» | ضيف `min-h-[44px] inline-grid place-items-center` على كل `<Link>` في الذيل | كود — `src/components/Footer.tsx` | 10 د |
| U18 | 🟡 | تنقّل | ٤ من ٥ روابط في الذيل بتوديك لنفس الصفحة: «القواعد» و«الأسئلة» و«مين إحنا» و«الشروط» كلها `/rules` | `src/components/Footer.tsx:8-14` (خريطة `HREFS`) · شفتها في `home__390__night.png` وباقي الصفحات | العضو بيدوس «الشروط» يلاقي القواعد — بيقرا كإهمال، ودي صفحة الثقة | يا تعمل الصفحات، يا تشيل الروابط اللي لسه مالهاش صفحة. النص كمان في الكود مش في اللوحة | كود — `src/data/lists.ts:129` + `src/components/Footer.tsx` | 20 د |
| U19 | 🟡 | تباين | نص مكان الصورة `#6B6455` على `#E2D2B4` بحجم 13px = **3.94:1** (والقيمتين من `DESIGN_TOKENS.md §10` حرفيًا، يعني التوكن نفسه بيسقط). ظاهر في 11 صفحة | `src/components/PhotoPlaceholder.tsx` · مقاس على `/` `/one` `/shoghl` `/shoghl/amaken` `/not-found` `/my/b1` في الوضعين | وصف الصورة المطلوبة باهت — ودي حاليًا محتوى الصور كله في الموقع | غمّق `photo-text` لـ `#5C5648` (يدي 4.55:1 على `#E2D2B4`) أو كبّر لـ 14px/600 | كود — `tailwind.config.ts` + `src/app/globals.css` (`--photo-text`) | 15 د |
| U20 | 🟡 | تباين | كلمة «لسه» على مناطق الخريطة `#6B6E76` على `#1E2128` بحجم 12px Rubik 900 = **3.16:1**. في الوضعين | `/map` 390 · `src/components/CairoMap.tsx` (`<text>` بتاع المناطق غير المزارة) — لقطة `map__390__day.png` و`map__390__night.png` | «لسه» هي المعلومة الوحيدة اللي بتقول إن المنطقة مش مفتوحة | فتّح لـ `#8B8FA0` على الأقل (4.6:1) — الباهت يفضل باهت بس مقروء | كود — `src/components/CairoMap.tsx` | 10 د |
| U21 | 🟡 | تباين | الشعار برتقالي `#F4632A` على كريمي `#FBF7EF` = **2.95:1**. للنص الكبير المطلوب 3.0 — يعني بيقع بشعرة، على 17 صفحة في الوضع النهاري | كل الصفحات النهارية عند 390 — أوضح في `home__390__day.png` (34px) و`login__390__day.png` (22px) | الشعار نفسه مش شعار مقروء تمامًا على الخلفية الأساسية. باقي النص البرتقالي متظبط أصلًا بـ `--accent-text: #BE4D21` (4.60:1) — الشعار بس اللي فاضل | خلي طبقة الشعار الممتلية في الوضع النهاري تستخدم `--accent-text` كمان (وسيب المفرغة كوبالت زي الملف) — هيدي 4.6:1 من غير ما يتغيّر الإحساس | كود — `src/components/Logo.tsx` | 15 د |
| U22 | 🟡 | نصوص | «موافق على» + `<b>`«القواعد الخمس»`</b>` بيتلزقوا من غير مسافة ← بيتقرا **«موافق علىالقواعد الخمس»** | `/join` 390 ليلي — لقطة `join__390__night.png` شريحة 02 · `src/app/join/page.tsx:734` · المفتاح `join.text.5` قيمته `"موافق على"` من غير مسافة في الآخر | خطأ إملائي مرئي في **نص الموافقة القانونية** | زوّد مسافة في آخر قيمة المفتاح | من اللوحة: المحتوى ← `join.text.5` | 1 د |
| U23 | 🟡 | تخطيط | نقطة السبوطة النابضة على الخريطة بتتراكب على اسم المنطقة — «المعادي» آخر حرف فيها تحت هالة النقطة، ونفس الحاجة في «التجمع» | `/map` 1440 ليلي — لقطة `map__1440__night.png` (تكبير `slices/map_overlap.png`) · نفس الحاجة عند 390 و768 | اسم المنطقة — المعلومة الأساسية على الخريطة — مقطوع | حرّك التسمية تحت كتلة المنطقة بدل ما تكون في نصها، أو أزح النقط لأعلى الكتلة | كود — `src/components/CairoMap.tsx` + `src/data/areas.ts` | 30 د |
| U24 | 🟡 | نبرة | كلمة **«مقعد / مقاعد / المقعد»** في 4 مفاتيح — دي نبرة تذاكر/طيران، مش عامية مصرية. و`COPY.md §17` نفسه كاتب «**6 كراسي**» | `shoghl.step1.body` («واحجز مقعد») · `shoghl.weekSub` («احجز مقعدك») · `shoghl.venue.desks` («مقاعد») · `shoghl.price.day` («شامل القهوة والمقعد والكابتن») — لقطة `shoghl__390__day.png` و`shoghl-amaken__390__day.png` | كلمة واحدة بتحوّل الترابيزة لصف كراسي في قاعة. ودي القسم اللي أصلًا أقرب للتجاري | البدائل بالعامية: «شوف سبوطات الشغل الأسبوع ده و**احجز كرسيك**. 6 بس على الترابيزة.» · «**احجز كرسيك** — اللابتوب عليك والباقي علينا.» · «**كراسي**» · «لليوم — شامل القهوة **والكرسي** والكابتن.» | من اللوحة: المحتوى ← `shoghl.step1.body` · `shoghl.weekSub` · `shoghl.venue.desks` · `shoghl.price.day` | 5 د |
| U25 | 🟡 | نبرة | نص للعضو بيتكلم عن **مراحل التطوير الداخلية**: «رفع صورة التحويل لكارت بيتفعّل في **المرحلة الجاية**. لحد ساعتها، حجز اليوم لوحده بـ 120 شغال عادي.» | المفتاح `shoghl.pass.transfer.soon` — بيظهر في مسار الدفع في `/shoghl/pass` | العضو مش عارف إن في «مراحل». الجملة بتقول له «الحاجة دي لسه مش خلصت» — بتصغّر الثقة | بالعامية ومن غير ما نفضح الجدول: «الكارت لسه بنجهّزه. دلوقتي احجز يومك لوحده بـ 120، ولما يفتح هنقولك.» | من اللوحة: المحتوى ← `shoghl.pass.transfer.soon` | 5 د |
| U26 | 🟡 | تخطيط | بطاقة المشاركة 9:16 في `/game/result` جواها **فراغ ~400px** في النص: الستيكر فوق، وصندوق السبوطة تحت، وما بينهم بلاطة سوده فاضية | `/game/result` 390 في الوضعين — لقطة `game-result__390__night.png` | البطاقة دي المفروض تتشارك على الستوري — دلوقتي شكلها ناقص | املا النص بجملة النوع بحجم كبير أو بعلامة استفهام كبيرة باهتة، أو قصّر البطاقة | كود — `src/components/TypeCard.tsx` | 30 د |
| U27 | 🟡 | نظام التصميم | أكتر من زر رئيسي برتقالي في الشاشة الواحدة (والقاعدة مكتوبة في تعليق `Buttons.tsx` نفسه: «واحد بس في كل شاشة»). العدّ الفعلي بـ `getComputedStyle` عند 390 ليلي: `/me` = **3** («ابعتله» ×3) · `/me/shoghl` = **2** («خد كارت» + «احفظ») · `/shoghl/pass` = **2** (طريقة التحويل المختارة + «اشتري الكارت») · `/shoghl` = **2** · `/` = **5** (الزر الكبير + 4 «أنا جاي») | لقطات `me__390__night.png` · `me-shoghl__390__night.png` · `shoghl-pass__390__day.png` · `src/components/Buttons.tsx:9` | العين مش عارفة تروح فين. في `/shoghl/pass` طريقة الدفع المختارة بتنافس زر الدفع نفسه | `/me`: خلي «ابعتله» ثانوي و«احجزوا سوا» هو الرئيسي (وده كمان بيصلّح U9). `/shoghl/pass`: خلي الطريقة المختارة حد برتقالي 2px بخلفية شفافة. `/` مقبولة لأن الملف نفسه فيه «أنا جاي» على كل بطاقة | كود — `src/app/me/page.tsx` · `src/app/shoghl/pass/page.tsx` · `src/app/me/shoghl/page.tsx` | 40 د |
| U28 | 🟡 | لمس | مساحات لمس أقل من 44px (مقيسة كلها بـ `getBoundingClientRect` عند 390): نقط الخريطة **27.7–35.9px** · فلتر «شغل» على الخريطة **57.9×36** · رابط «ادخل من هنا» في `/join` **76.2×22** · رابط الشعار في الرأس **62.7×27.2** (و**96.9×37.2** في الرئيسية) · عنوان بطاقة السبوطة `<a>` **314×29.9** | `/map` · `/join` · كل الصفحات (الشعار) · `src/components/SbotaCard.tsx:79` · `src/components/Header.tsx:30` | نقط الخريطة هي طريقة التصفح الأساسية في `/map` وصعب تتدوس | استخدم نفس حيلة `.nb-touch-44` (`::after` شفاف) اللي مطبقة على الفلاتر أصلًا، وزوّد `min-h-[44px]` على رابط الشعار و«ادخل من هنا»، وكبّر `hit-area` النقط في SVG بدايرة شفافة `r=22` | كود — `src/components/CairoMap.tsx` · `src/components/Header.tsx` · `src/app/join/page.tsx` | 45 د |
| U29 | 🟡 | محتوى/تشغيل | أهم نصوص العلامة **مش في `copy_strings`** فمش قابلة للتعديل من `/admin/content`: نص الضمان · القواعد الخمس بعناوينها ومتونها · سطر الذيل «مفيش عضوية» · روابط الذيل · نص الشريط المتحرك · الاقتباس «رحت لوحدي…» · توقيعات صور «اللي حصل» · رقم الطوارئ | `src/data/lists.ts:99-150` (`fiveRules` · `guaranteeText` · `footerLinks` · `footerLine` · `marqueeText` · `lastFriday` · `quote` · `emergencyPhone`) · `scripts/check-copy.mjs` بيستثني `src/data` عن قصد فالحارس ما بيمسكهاش | أي تعديل على الضمان أو رقم الطوارئ محتاج نشر جديد. ورقم الطوارئ دلوقتي رقم وهمي `+201000000000` معروض للعضو | نقّل الثمانية دول لـ `copy_strings` بنفس نمط باقي المفاتيح، والرقم لـ `settings` | كود (النقل) → بعدها من اللوحة: المحتوى ← `rules.*` `shared.guarantee` `footer.*` `home.marquee` · والرقم من `/admin/settings` | 2 س |
| U30 | 🟡 | انحراف: تذاكر | `/one` — لحظة «نديها واحدة؟» — بتعرض بطاقة سبوطة + زر «أنا جاي» + «لأ، وريني غيرها» وبس. **مفيش كابتن، مفيش ضمان، مفيش أي كلمة عن المجموعة.** الشاشة كلها برتقالي وسعر وزر | `/one` 390 في الوضعين — لقطة `one__390__night.png` و`one__390__day.png` | ودي أهم شاشة تحويل في الموقع. حاليًا بتقرا «عرض اليوم» مش «دي مجموعتك» | ضيف سطر كابتن واحد («الكابتن يوسف هيستقبلك») وسطر الضمان المختصر تحت الزر. النصوص موجودة أصلًا في `copy_strings` | كود (البنية) + من اللوحة: المحتوى ← `one.*` | 30 د |
| U31 | 🟡 | انحراف: نبرة | `/login` شاشة إيميل + باسورد + زر «دخول» على فراغ كبير — من غير أي حاجة تقول إن ده نادي: مفيش «مفيش عضوية»، مفيش كابتن، مفيش سطر عامية. وسطرها الوحيد «بالإيميل والباسورد اللي سجّلت بيهم» بينقض «رقمك هو دخولك» | `/login` 390 ليلي — لقطة `login__390__night.png` · المفتاح `login.subtitle` | أول شاشة لعضو راجع، وشكلها زي أي لوحة SaaS | حط سطر بالعامية تحت العنوان يفكّره بالنادي، والذيل بسطر «مفيش عضوية» | من اللوحة: المحتوى ← `login.subtitle` | 15 د |
| U32 | 🟡 | انحراف: هوية | **«مفيش عضوية» مكتوبة في مكان واحد بس: سطر الذيل** (و`metadata.description`). مش موجودة في `/rules` — الصفحة اللي بتشرح النادي — ولا في هيرو الرئيسية ولا في `/shoghl`. وفي `/shoghl/pass` الذيل بيقولها تحت صفحة بتبيع كارت 8 أيام بـ 720 جنيه | `grep -rn "مفيش عضوية" src/` بترجع سطرين بس: `src/data/lists.ts:131` و`src/app/layout.tsx:29` · لقطة `rules__390__night.png` (مفيهاش) و`shoghl-pass__390__day.png` (الذيل تحت الأسعار) | الوعد المميّز للنادي اتحوّل لكلام ذيل. والمكان الوحيد اللي بيقوله هو أكتر مكان بيناقضه | اكتبها كسطر أصيل في `/rules` تحت العنوان («مفيش عضوية ومفيش اشتراك — بتدفع السبوطة اللي رايحها وبس») وفي هيرو `/shoghl/pass` | من اللوحة: المحتوى ← `rules.text.8` والمفاتيح الجديدة | 20 د |
| U33 | 🟡 | الوضعان | `/shoghl` و`/shoghl/pass` و`/shoghl/[slug]` و`/shoghl/amaken` بيفرضوا الوضع النهاري (`WorkShell`) وبيخفوا زر التبديل — كويس ومتسق. لكن **`/me/shoghl` مش بتعمل كده**: بتتبع الوضع العام وبتطلع ليلي بزر تبديل شغال | لقطة `me-shoghl__390__night.png` (ليلي) مقابل `shoghl__390__night.png` (طالعة نهاري) · `src/components/work/WorkShell.tsx` مش مستخدمة في `src/app/me/shoghl/page.tsx` | العضو داخل من `/shoghl` النهاري لـ«الشغل بتاعي» فالشاشة بتقلب أسود فجأة | لفّ `/me/shoghl` بـ `WorkShell` زي أخواتها | كود — `src/app/me/shoghl/page.tsx` | 10 د |
| U34 | 🔵 | نظام التصميم | لون بره اللوحة: `#232732` لشريط النيل في الخريطة. مش في `DESIGN_TOKENS.md` ولا في `tailwind.config.ts` (أقرب توكنين `#1E2128` و`#2A2E36`). ده اللون الوحيد بره اللوحة في كل الموقع العام — كل الباقي في `/admin` بس | `src/components/CairoMap.tsx:47` | انحراف بسيط، بس هو أول شرخ في «كل لون له توكن» | استخدم `stroke-night #2A2E36` أو ضيف توكن `nile` رسمي في `DESIGN_TOKENS.md` | كود — `src/components/CairoMap.tsx` | 5 د |
| U35 | 🔵 | حركة/أداء | `framer-motion@12.0.6` في `dependencies` لكن **مش مستوردة في أي ملف** في `src/`. كل الحركة CSS خالص | `package.json:16` · `grep -rn "framer-motion" src/` بترجع فاضي | حزمة ميتة. وأول ما حد يستوردها هييجي معاها حركة مش محترمة `prefers-reduced-motion` بالتلقائي | امسحها من `package.json` | كود — `package.json` | 5 د |
| U36 | 🔵 | حركة | تحت `prefers-reduced-motion: reduce` القاعدة العامة بتخلي `animation-duration: 0.01ms; iteration-count: 1`، فالشريط المتحرك **بينط على `translateX(-50%)` ويقف هناك** — يعني بيعرض نص المحتوى من نصه («… فاضل 5 ▸ الجمعة · بعد ما الشمس تغيب …») بدل ما يبدأ من الأول | `src/app/globals.css:169-178` · لقطة `slices/reduced_top.png` (390 ليلي · `reducedMotion: reduce`) | بسيط بس المحتوى مبتور لمستخدم عنده الإعداد ده | ضيف `.animate-marquee { animation: none !important; transform: none !important; }` جوه بلوك `prefers-reduced-motion` | كود — `src/app/globals.css` | 5 د |
| U37 | 🔵 | الوضعان | الخريطة في `/map` وبطاقة المكان في `/my/[bookingId]` بيفضلوا `surface-night #1E2128` في الوضع النهاري — بلاطة سوده كبيرة على صفحة كريمي. ده مطابق لـ`DESIGN_TOKENS §7` («خلفية الخريطة المصغرة» = surface-night) فهو مقصود، بس شكله غريب | لقطة `map__390__day.png` و`my-booking__390__day.png` (تكبير `slices/myb_day_map.png`) | مش عيب تقني — بس أكبر عنصر في `/map` بيتجاهل الوضع اللي العضو اختاره | قرار تصميم: يا تعمل نسخة نهارية للخريطة (رملي + خطوط `sand-line`)، يا توثّق إنها مقصودة | كود — `src/components/CairoMap.tsx` + `DESIGN_TOKENS.md` | 1 س |
| U38 | 🔵 | تخطيط | على 1440 قايمة سبوطات الشغل شبكة عمودين، وبسبوطة واحدة بس البطاقة بتقعد يمين ونصف الصف فاضي — بيبان كأن حاجة وقعت. (البيانات الوهمية فيها سبوطة شغل واحدة، فالفراغ ده من المحتوى مش من التخطيط) | `/shoghl` 1440 نهاري — لقطة `shoghl__1440__day.png` شريحة 00 · نفس الحاجة في `/shoghl/amaken` (مكان واحد) | مش عيب مع بيانات حقيقية، بس يستاهل حماية | خلي الشبكة `auto-fit` بحد أدنى للعرض علشان العنصر الواحد يتمدّد أو يتوسّط | كود — `src/app/shoghl/page.tsx` | 15 د |
| U39 | 🔵 | تخطيط | `html` و`body` عليهم `overflow-x: hidden`. اختبار الـ360 عدّى نضيف (تحت)، وكمان **عدّى بعد ما شلت `overflow-x` بالجافاسكريبت** — يعني مفيش تجاوز حقيقي مخبّي دلوقتي. بس القاعدة دي هتخفي أي تجاوز يتضاف بكرة بدل ما يبان | `src/app/globals.css:65-77` | مفيش أثر حالي — ده تحذير صيانة | سيبها، بس ضيف اختبار Playwright بيقيس `scrollWidth` عند 360 على كل مسار في الـCI | كود — `tests/` | 30 د |

---

## (أ) اختبار التمرير الأفقي عند 360px

الاختبار: `document.documentElement.scrollWidth > window.innerWidth` عند `viewport: 360×800`، على **كل** الـ19 مسار **في الوضعين** (38 قياس).
وكمان اختبار تاني أقسى: شيلت `overflow-x:hidden` من `html` و`body` بالجافاسكريبت وقِست تاني — علشان أتأكد إن النتيجة مش مجرد قص.

| الصفحة | ليلي `scrollW/innerW` | نهاري `scrollW/innerW` | بعد شيل `overflow-x` | النتيجة |
|---|---|---|---|---|
| `/` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/one` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/map` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/rules` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/captains` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/join` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/login` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/me` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/me/shoghl` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/game` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/game/result` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/s/mystery` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/shoghl` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/shoghl/pass` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/shoghl/amaken` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/not-found` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/s/ehna-el-rabe3` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/shoghl/work-cafe-tagamo3` | 360 / 360 | 360 / 360 | 360 | ✅ |
| `/my/b1` | 360 / 360 | 360 / 360 | 360 | ✅ |

**38 من 38 عدّوا. مفيش تمرير أفقي في أي صفحة.**

العناصر الوحيدة اللي بتخرج بره الشاشة كلها **جوه شرايط تمرير أفقي مقصودة** (`.nb-scroll-x`):
الشريط المتحرك في `/` (بيمتد من `left:-94` لـ `right:1739` جوه حاويته)، ومعرض صور السبوطة في `/s/[slug]` و`/shoghl/[slug]` (الصور جاية من `flex: 0 0 300px`)، وشريط الفلاتر.

---

## (ب) قياسات التباين

الطريقة: JS في الصفحة نفسها — بيمشي على كل عنصر عنده نص مباشر، بياخد `color` (أو `fill` للـSVG)، وبيمشي لفوق في شجرة الآباء لحد أول خلفية غير شفافة (مع دمج الشفافيات)، وبيحسب WCAG 2.x.
المعيار: **4.5:1** للنص الصغير · **3:1** للكبير (≥24px أو ≥18.66px بوزن ≥700).
19 صفحة × وضعين × كل الأزواج الفريدة.

### أهم الأزواج — سليمة

| الزوج | النسبة | فين |
|---|---|---|
| `#FBF7EF` على `#14161A` | **16.95:1** | النص الأساسي · ليلي |
| `#14161A` على `#FBF7EF` | **16.95:1** | النص الأساسي · نهاري |
| `#14161A` على `#EFE3CF` | **14.28:1** | نص بطاقة السبوطة والستيكرات الرملية |
| `#C9C4B8` على `#14161A` | **10.41:1** | النص الثانوي · ليلي |
| `#55575C` على `#FBF7EF` | **6.77:1** | النص الثانوي · نهاري |
| `#14161A` على `#F4632A` | **5.75:1** | نص الأزرار الرئيسية والستيكرات البرتقالي |
| `#F4632A` على `#14161A` | **5.75:1** | `--accent-text` الليلي · «اقرأ القواعد» · «— نور» |
| `#55575C` على `#EFE3CF` | **5.70:1** | مزاج البطاقة |
| `#FBF7EF` على `#2B4CFF` | **5.49:1** | «بنات بس» · بطاقة «ليه المجموعة دي؟» |
| `#BE4D21` على `#FBF7EF` | **4.60:1** | `--accent-text` النهاري — **الحل الموجود للبرتقالي شغّال** |

### الساقطة

| النسبة | المطلوب | الزوج | الحجم/الوزن | فين | الصف |
|---|---|---|---|---|---|
| **2.22:1** | 4.5 | `#8E2F1F` على `#14161A` | 16 / 600 | «حذف الحساب» · `/me` ليلي | U10 |
| **2.50:1** | 4.5 | `#55575C` على `#14161A` | 13 / 400 | سطر الباسورد · `/join` ليلي | U11 |
| **2.95:1** | 3.0 | `#F4632A` على `#FBF7EF` | 34 و22 / 900 | الشعار · 17 صفحة نهاري | U21 |
| **3.16:1** | 4.5 | `#6B6E76` على `#1E2128` | 12 / 900 | «لسه» على الخريطة · الوضعين | U20 |
| **3.94:1** | 4.5 | `#6B6455` على `#E2D2B4` | 13 و11 / 400 | نص مكان الصورة · 11 صفحة · الوضعين | U19 |

### عن «البرتقالي على الكريمي» تحديدًا (الشك المذكور في التاسك)

مؤكَّد ومحلول جزئيًا: `#F4632A` على `#FBF7EF` = **2.95:1**.
`globals.css:41-43` بيستخدم نسخة أغمق `--accent-text: #BE4D21` في الوضع النهاري وبتدّي **4.60:1**، والليلي بيستخدم البرتقالي نفسه على الأسود (**5.75:1**).
**مسحت كل النصوص في الـ19 صفحة × الوضعين: مفيش ولا نص صغير واحد بلون `#F4632A` على خلفية كريمي أو رملي.** الاستثناء الوحيد هو **الشعار** (34px و22px بوزن 900) — نص كبير بس بيقع بشعرة تحت الـ3:1 (U21).

---

## (ج) الكلمات الممنوعة

`node scripts/check-copy.mjs` ← **✓ مفيش نص عربي في المكونات — كله جاي من قاعدة البيانات.** (خروج 0)

بحث عن الكلمات التسعة في `src/data/copy-fallback.ts` (539 مفتاح) و`scripts/copy-seed.json` (كل الصفوف):

| الكلمة | نتايج في نصوص المستخدم | ملاحظة |
|---|---|---|
| تعارف | **0** | |
| شريك | **0** | |
| إعجاب | **0** | |
| فعالية | **0** | |
| تذكرة | **0** | |
| باقة | **0** | |
| منصة | **0** | |
| مستخدم | **0** | |
| اكتشف | **0** | |

**صفر مخالفة في نصوص المستخدم.** وسّعت البحث على كل `src/` بالجذور (`شركا` · `تذاكر` · `باقات` · `اكتشاف` · `فعالي`) — كل النتايج في **تعليقات كود** أو في **`src/app/admin/**`** (أداة داخلية، بره نطاق المراجعة)، وأطرفها `src/app/admin/settings/page.tsx:953` اللي بيستخدم الكلمات دي كأمثلة لقائمة المنع نفسها.

الإيموجي: `😅` مرة واحدة بس، في `src/data/game.ts:113` («مش فاكر 😅») — بالظبط زي `COPY.md §0`. مسحت كل قيم النصوص بـ `\p{Extended_Pictographic}` ← مفيش غيرها.

### نصوص نبرتها بايظة (مش كلمة ممنوعة بس ريحتها رسمية)

| المفتاح | النص الحالي | البديل بالعامية | الصف |
|---|---|---|---|
| `shoghl.step1.body` | «شوف سبوطات الشغل الأسبوع ده واحجز **مقعد**.» | «… و**احجز كرسيك**.» | U24 |
| `shoghl.weekSub` | «احجز **مقعدك** — اللابتوب عليك، والباقي علينا.» | «احجز **كرسيك** — اللابتوب عليك، والباقي علينا.» | U24 |
| `shoghl.venue.desks` | «**مقاعد**» | «**كراسي**» | U24 |
| `shoghl.price.day` | «لليوم — شامل القهوة **والمقعد** والكابتن.» | «لليوم — شامل القهوة **والكرسي** والكابتن.» | U24 |
| `shoghl.pass.transfer.soon` | «رفع صورة التحويل لكارت بيتفعّل في **المرحلة الجاية**…» | «الكارت لسه بنجهّزه. دلوقتي احجز يومك لوحده بـ {{single}}، ولما يفتح هنقولك.» | U25 |
| `login.subtitle` | «بالإيميل والباسورد اللي سجّلت بيهم.» | (بينقض «رقمك هو دخولك») «ادخل بإيميلك — ورقمك يفضل للكابتن بس.» | U3 · U31 |
| `home.text.1` | «مفيش سبوطة بالفلتر ده الأسبوع ده. جرب فلتر تاني.» | صح كنص، بس بيتعرض غلط ساعة عطل الشبكة — محتاج نص تاني جنبه | U14 |
| `join.text.5` | «موافق على» (من غير مسافة في الآخر) | «موافق على » | U22 |

---

## (د) شغال تمام

1. **مفيش تمرير أفقي في أي صفحة عند 360px** — 38 قياس من 38، وكمان بعد ما شلت `overflow-x:hidden`. تخطيط الموبايل مضبوط فعلًا.
2. **مشكلة «البرتقالي على الكريمي» متحلّة صح** — `--accent-text: #BE4D21` بتدّي 4.60:1، ومفيش نص صغير واحد بالبرتقالي الخام على خلفية فاتحة في الـ19 صفحة.
3. **صفر كلمة ممنوعة** في نصوص المستخدم، و`check-copy.mjs` بيعدّي — كل نص معروض جاي من `copy_strings`.
4. **`prefers-reduced-motion` محترمة** — قِستها: `nb-pulse` و`nb-map-ring` بيتلغوا خالص، والباقي بينزل لـ 0.01ms/دورة واحدة. و`LiveQ` بتفحص `matchMedia` بنفسها (`LiveQ.tsx:39`).
5. **مفيش أي `#FFFFFF`، ولا `gradient`، ولا `box-shadow` ناعم** في كل `src/app` و`src/components` (بره اللوحة). `box-shadow` الوحيد هو `nb-pulse` المسموحة من الملف.
6. **دوران الستيكرات كله جوه المدى** — القيم المستخدمة `±2 · ±3 · ±4` بس. الـ`-8deg` و`-18deg` الوحيدين على `QIcon`/`LiveQ` وهما موثقين في `DESIGN_TOKENS §3`.
7. **مفيش لون بره اللوحة في الموقع العام** إلا `#232732` واحد (U34). كل الألوان الغريبة التانية في `src/app/admin/**` بس.
8. **الصور مخفية قبل اللقاء** — `/my/b1` بيعرض حروف أولى وأنواع وأسامي، صفر صور. القاعدة منفّذة بصريًا زي ما هي في `README §5`.
9. **الكابتن ظاهر فين ما يهم**: `/s/[slug]` (بطاقة كاملة) · `/my/b1` (أول عنصر بعد العنوان، بجملة البوابة) · `/shoghl` (في المتن) · `/shoghl/[slug]` · `/captains` · شات السبوطة (رسالة مثبتة من الكابتن).
10. **الضمان قبل زر الدفع في 3 من 4 مسارات** — `/s/[slug]` و`/s/[slug]/pay` و`/shoghl/[slug]` كلهم فيهم `GuaranteeBox` فوق الزر مباشرة. الناقص `/shoghl/pass` بس (U5).
11. **شات السبوطة على الهوية بالكامل** — رسالة الكابتن مثبتة بنبرة الملف، حروف أولى بدل الصور، سطر «بيتقفل السبت 10 بالليل» في الرأس.
12. **حالات فاضية بالعامية في `/me/shoghl`** — «مفيش كارت شغال دلوقتي.» · «مفيش سبوطات شغل متاحة دلوقتي — تعالى بعدين.» · «لسه فاضي. بعد سبوطة الشغل بنسألك عايز تشتغل مع مين…». دي أحسن حالات فاضية في الموقع، تستحق تتنسخ لباقي الصفحات.
13. **`/shoghl/*` بيقفل الوضع النهاري بأمانة** — `WorkShell` بيحط `data-theme="day"` قبل أول رسم وبيخفي زر التبديل، فمفيش وميض ولا زر ميّت. (ما عدا `/me/shoghl` — U33.)
14. **تخطيط 1440 مطابق للمواصفة** — الرئيسية خريطة يمين ~60% + بطاقات شمال في عمودين، وباقي الصفحات عمود متوسّط ~720px. اتأكدت من `home__1440__night.png` و`rules__1440__night.png`.
15. **`/not-found` على الهوية** — «؟» كبيرة + «السبوطة دي مش موجودة… بس دي موجودة» + بطاقة سبوطة حقيقية. بالظبط زي `COPY.md §21`.
16. **حلقة التركيز بالكيبورد موحّدة وواضحة** — `outline: 3px solid var(--orange)` على كل عنصر تفاعلي (`globals.css:152`).
